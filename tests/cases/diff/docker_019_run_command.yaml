name: docker_019_run_command
tags:
- lang:python
- lang:shell
- change:single_file
- context:import_chain
- distance:2hop
- difficulty:medium
repo:
  initial_files:
    requirements.txt: |
      flask==2.3.0
      gunicorn==21.2.0
      psycopg2-binary==2.9.9
      alembic==1.13.0
      sqlalchemy==2.0.23
    src/app.py: |
      from flask import Flask
      from sqlalchemy import create_engine
      import os
      app = Flask(__name__)
      engine = create_engine(os.environ['DATABASE_URL'])
    alembic/env.py: |
      from alembic import context
      from src.models import Base
      target_metadata = Base.metadata

      def run_migrations_online():
          connectable = context.config.attributes.get("connection", None)
          with connectable.connect() as connection:
              context.configure(connection=connection, target_metadata=target_metadata)
              with context.begin_transaction():
                  context.run_migrations()
    Dockerfile: |
      FROM python:3.11-slim
      COPY requirements.txt .
      RUN pip install -r requirements.txt
      COPY . .
      CMD ["python", "app.py"]
    scripts/ci_test.sh: |
      #!/bin/bash
      set -e
      pytest tests/ -v --cov=src
      coverage report --fail-under=80
    tests/conftest.py: |
      import pytest
      from src.app import app
      @pytest.fixture
      def client():
          return app.test_client()
    k8s/deployment.yaml: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: flask-app
      spec:
        template:
          spec:
            containers:
              - name: app
                image: flask-app:latest
                ports:
                  - containerPort: 8000
  changed_files:
    requirements.txt: |
      flask==2.3.0
      gunicorn==21.2.0
      psycopg2-binary==2.9.9
      alembic==1.13.0
      sqlalchemy==2.0.23
    src/app.py: |
      from flask import Flask
      from sqlalchemy import create_engine
      import os
      app = Flask(__name__)
      engine = create_engine(os.environ['DATABASE_URL'])
    alembic/env.py: |
      from alembic import context
      from src.models import Base
      target_metadata = Base.metadata

      def run_migrations_online():
          connectable = context.config.attributes.get("connection", None)
          with connectable.connect() as connection:
              context.configure(connection=connection, target_metadata=target_metadata)
              with context.begin_transaction():
                  context.run_migrations()
    Dockerfile: |
      FROM python:3.12-slim-bookworm AS base
      ENV PYTHONDONTWRITEBYTECODE=1
      ENV PYTHONUNBUFFERED=1

      # Install system dependencies
      RUN apt-get update && apt-get install -y --no-install-recommends \
          libpq-dev \
          curl \
          && rm -rf /var/lib/apt/lists/* \
          && apt-get clean

      FROM base AS dependencies
      WORKDIR /app
      COPY requirements.txt .

      # Install Python dependencies with optimizations
      RUN pip install --no-cache-dir --upgrade pip && \
          pip install --no-cache-dir -r requirements.txt && \
          find /usr/local -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true

      FROM base AS production
      WORKDIR /app

      # Copy dependencies from builder
      COPY --from=dependencies /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
      COPY --from=dependencies /usr/local/bin /usr/local/bin

      # Copy application code
      COPY src/ ./src/
      COPY alembic/ ./alembic/
      COPY alembic.ini ./

      # Create non-root user and set permissions
      RUN useradd --create-home --shell /bin/bash appuser && \
          chown -R appuser:appuser /app

      USER appuser
      EXPOSE 8000

      CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000", "src.app:app"]
    scripts/ci_test.sh: |
      #!/bin/bash
      set -e
      pytest tests/ -v --cov=src
      coverage report --fail-under=80
    tests/conftest.py: |
      import pytest
      from src.app import app
      @pytest.fixture
      def client():
          return app.test_client()
    k8s/deployment.yaml: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: flask-app
      spec:
        template:
          spec:
            containers:
              - name: app
                image: flask-app:latest
                ports:
                  - containerPort: 8000
  commit_message: Update files
fragments:
- id: dockerfile
  selector:
    path: Dockerfile
    anchor: RUN apt-get update && apt-get install -y --no-install-recommends \
- id: dockerfile_1
  selector:
    path: Dockerfile
    anchor: libpq-dev
- id: dockerfile_2
  selector:
    path: Dockerfile
    anchor: COPY alembic/ ./alembic/
- id: dockerfile_3
  selector:
    path: Dockerfile
    anchor: CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000", "src.app:app"]
- id: requirements
  selector:
    path: requirements.txt
    anchor: psycopg2-binary==2.9.9
- id: requirements_1
  selector:
    path: requirements.txt
    anchor: alembic==1.13.0
- id: no_ci_test
  selector:
    path: scripts/ci_test.sh
- id: no_conftest
  selector:
    path: tests/conftest.py
- id: no_deployment
  selector:
    path: k8s/deployment.yaml
- id: no_global_0
  selector:
    anchor: pytest tests/ -v
- id: no_global_1
  selector:
    anchor: coverage report --fail-under
- id: no_global_2
  selector:
    anchor: '@pytest.fixture'
- id: no_global_3
  selector:
    anchor: app.test_client
- id: no_global_4
  selector:
    anchor: 'kind: Deployment'
- id: no_global_5
  selector:
    anchor: flask-app:latest
oracle:
  required:
  - dockerfile
  - dockerfile_1
  - dockerfile_2
  - dockerfile_3
  - requirements
  - requirements_1
  allowed: []
  forbidden:
  - no_ci_test
  - no_conftest
  - no_deployment
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
