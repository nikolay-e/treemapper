name: comprehensive_021_quality_simple_function_call
repo:
  initial_files:
    src/utils.py: |
      def double_value(x):
          return x * 2

      def triple_value(x):
          return x * 3

      def negate(x):
          return -x

      def absolute(x):
          return abs(x)
    src/main.py: |
      def bar():
          pass

      def process_batch(items):
          return [item for item in items]

      def summarize(results):
          return len(results)
    garbage_lazy_evaluator.py: |
      GARBAGE_COMP_021_THUNK_CACHE_A = "memoize"
      GARBAGE_COMP_021_FORCE_EVAL_B = "strict"

      class GarbageLazyEvaluator:
          GARBAGE_COMP_021_DEPTH_LIMIT_C = True
          def evaluate(self):
              pass
    garbage_coroutine_pool.py: |
      GARBAGE_COMP_021_POOL_STRATEGY_D = "elastic"
      GARBAGE_COMP_021_YIELD_POINT_E = "cooperative"

      class GarbageCoroutinePool:
          GARBAGE_COMP_021_STACK_SIZE_F = False
          def spawn(self):
              pass
  changed_files:
    src/utils.py: |
      def double_value(x):
          return x * 2

      def triple_value(x):
          return x * 3

      def negate(x):
          return -x

      def absolute(x):
          return abs(x)
    src/main.py: |
      from src.utils import double_value

      def bar():
          return double_value(42)

      def process_batch(items):
          return [item for item in items]

      def summarize(results):
          return len(results)
    garbage_lazy_evaluator.py: |
      GARBAGE_COMP_021_THUNK_CACHE_A = "memoize"
      GARBAGE_COMP_021_FORCE_EVAL_B = "strict"

      class GarbageLazyEvaluator:
          GARBAGE_COMP_021_DEPTH_LIMIT_C = True
          def evaluate(self):
              pass
    garbage_coroutine_pool.py: |
      GARBAGE_COMP_021_POOL_STRATEGY_D = "elastic"
      GARBAGE_COMP_021_YIELD_POINT_E = "cooperative"

      class GarbageCoroutinePool:
          GARBAGE_COMP_021_STACK_SIZE_F = False
          def spawn(self):
              pass
  commit_message: Update files
fragments:
- id: main_file
  selector:
    path: src/main.py
- id: req_global_0
  selector:
    anchor: from src.utils import double_value
- id: req_global_1
  selector:
    anchor: return double_value(42)
- id: no_global_0
  selector:
    anchor: GARBAGE_COMP_021_DEPTH_LIMIT_C
- id: no_global_1
  selector:
    anchor: GARBAGE_COMP_021_FORCE_EVAL_B
- id: no_global_2
  selector:
    anchor: GARBAGE_COMP_021_POOL_STRATEGY_D
- id: no_global_3
  selector:
    anchor: GARBAGE_COMP_021_STACK_SIZE_F
- id: no_global_4
  selector:
    anchor: GARBAGE_COMP_021_THUNK_CACHE_A
- id: no_global_5
  selector:
    anchor: GARBAGE_COMP_021_YIELD_POINT_E
- id: no_global_6
  selector:
    anchor: garbage_coroutine_pool.py
- id: no_global_7
  selector:
    anchor: garbage_lazy_evaluator.py
oracle:
  required:
  - main_file
  - req_global_0
  - req_global_1
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
