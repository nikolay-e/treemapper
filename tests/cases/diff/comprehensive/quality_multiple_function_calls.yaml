name: quality_multiple_function_calls
initial:
  utils.py: |
    def validate(x):
        return x > 0

    def transform(x):
        return x * 2

    def save(x):
        return f"saved: {x}"
  main.py: |
    def process(): pass
changed:
  main.py: |
    from utils import validate, transform, save

    def process(data):
        if validate(data):
            result = transform(data)
            return save(result)
assertions:
  must_include:
  - def validate
options:
  commit_message: Add processing
