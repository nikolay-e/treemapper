name: cicd_and_docs_009_gha_needs
tags:
- lang:typescript
- change:single_file
- change:config
- context:same_file
- distance:0hop
- difficulty:easy
repo:
  initial_files:
    .github/workflows/ci.yml: |
      name: CI
      on: [push, pull_request]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm run build
        test:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm test
        deploy:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: ./deploy.sh
    package.json: |
      {
        "name": "pipeline-app",
        "version": "1.0.0",
        "scripts": {
          "build": "webpack --mode production",
          "test": "vitest run",
          "deploy": "./scripts/deploy.sh"
        }
      }
    src/pipeline/stages.ts: |
      export interface PipelineStage {
        name: string;
        dependsOn: string[];
        run: () => Promise<void>;
      }

      export function createPipeline(stages: PipelineStage[]): void {
        console.log('Creating pipeline with', stages.length, 'stages');
      }
    garbage_orchestrator.ts: |
      const GARBAGE_CICD_DOCS_009_ORCHESTRATOR_MARKER_A = "kubernetes";
      const GARBAGE_CICD_DOCS_009_NAMESPACE_MARKER_B = "default";

      class GarbageOrchestrator {
        static GARBAGE_CICD_DOCS_009_REPLICAS_MARKER_C = 3;
        deploy(): void {}
      }
    garbage_scheduler.ts: |
      const GARBAGE_CICD_DOCS_009_SCHEDULER_MARKER_D = "cron";
      const GARBAGE_CICD_DOCS_009_INTERVAL_MARKER_E = "0 * * * *";

      class GarbageSchedulerService {
        static GARBAGE_CICD_DOCS_009_TIMEOUT_MARKER_F = 300;
        schedule(): void {}
      }
  changed_files:
    .github/workflows/ci.yml: |
      name: CI
      on: [push, pull_request]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm run build
        test:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm test
        deploy:
          runs-on: ubuntu-latest
          needs: [build, test]
          steps:
            - uses: actions/checkout@v4
            - run: ./deploy.sh
    package.json: |
      {
        "name": "pipeline-app",
        "version": "1.0.0",
        "scripts": {
          "build": "webpack --mode production",
          "test": "vitest run",
          "deploy": "./scripts/deploy.sh"
        }
      }
    src/pipeline/stages.ts: |
      export interface PipelineStage {
        name: string;
        dependsOn: string[];
        run: () => Promise<void>;
      }

      export function createPipeline(stages: PipelineStage[]): void {
        console.log('Creating pipeline with', stages.length, 'stages');
      }
    garbage_orchestrator.ts: |
      const GARBAGE_CICD_DOCS_009_ORCHESTRATOR_MARKER_A = "kubernetes";
      const GARBAGE_CICD_DOCS_009_NAMESPACE_MARKER_B = "default";

      class GarbageOrchestrator {
        static GARBAGE_CICD_DOCS_009_REPLICAS_MARKER_C = 3;
        deploy(): void {}
      }
    garbage_scheduler.ts: |
      const GARBAGE_CICD_DOCS_009_SCHEDULER_MARKER_D = "cron";
      const GARBAGE_CICD_DOCS_009_INTERVAL_MARKER_E = "0 * * * *";

      class GarbageSchedulerService {
        static GARBAGE_CICD_DOCS_009_TIMEOUT_MARKER_F = 300;
        schedule(): void {}
      }
  commit_message: Update files
fragments:
- id: ci_file
  selector:
    path: .github/workflows/ci.yml
- id: req_global_0
  selector:
    anchor: 'needs: [build, test]'
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_009_INTERVAL_MARKER_E
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_009_NAMESPACE_MARKER_B
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_009_ORCHESTRATOR_MARKER_A
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_009_REPLICAS_MARKER_C
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_009_SCHEDULER_MARKER_D
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_009_TIMEOUT_MARKER_F
- id: no_global_6
  selector:
    anchor: garbage_orchestrator.ts
- id: no_global_7
  selector:
    anchor: garbage_scheduler.ts
oracle:
  required:
  - ci_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
