name: rust_040_drop_trait
tags:
- lang:rust
- change:single_file
- change:new_function
- change:signature
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    src/lib.rs: |
      pub mod connection;
      pub mod file_handle;
      pub mod transaction;
      pub mod logger;
    src/connection.rs: |
      use std::sync::atomic::{AtomicUsize, Ordering};

      static CONNECTION_COUNT: AtomicUsize = AtomicUsize::new(0);

      pub struct Connection {
          host: String,
          port: u16,
          connected: bool,
          id: usize,
      }

      impl Connection {
          pub fn new(host: &str, port: u16) -> Self {
              let id = CONNECTION_COUNT.fetch_add(1, Ordering::SeqCst);
              println!("Creating connection {} to {}:{}", id, host, port);
              Self {
                  host: host.to_string(),
                  port,
                  connected: true,
                  id,
              }
          }

          pub fn disconnect(&mut self) {
              if self.connected {
                  println!("Disconnecting connection {} from {}:{}", self.id, self.host, self.port);
                  self.connected = false;
              }
          }

          pub fn is_connected(&self) -> bool {
              self.connected
          }

          pub fn host(&self) -> &str {
              &self.host
          }

          pub fn port(&self) -> u16 {
              self.port
          }

          pub fn id(&self) -> usize {
              self.id
          }
      }

      pub fn active_connections() -> usize {
          CONNECTION_COUNT.load(Ordering::SeqCst)
      }
    src/file_handle.rs: |
      use std::fs::File;
      use std::io::{self, Write, Read};
      use std::path::PathBuf;

      pub struct FileHandle {
          file: File,
          path: PathBuf,
          bytes_written: usize,
      }

      impl FileHandle {
          pub fn create(path: &str) -> io::Result<Self> {
              let file = File::create(path)?;
              Ok(Self {
                  file,
                  path: PathBuf::from(path),
                  bytes_written: 0,
              })
          }

          pub fn write_data(&mut self, data: &[u8]) -> io::Result<()> {
              self.file.write_all(data)?;
              self.bytes_written += data.len();
              Ok(())
          }

          pub fn path(&self) -> &str {
              self.path.to_str().unwrap_or("")
          }

          pub fn bytes_written(&self) -> usize {
              self.bytes_written
          }
      }

      impl Drop for FileHandle {
          fn drop(&mut self) {
              let _ = self.file.flush();
              println!("Closing file: {} ({} bytes written)", self.path(), self.bytes_written);
          }
      }
    src/transaction.rs: |
      pub enum TransactionState {
          Active,
          Committed,
          RolledBack,
      }

      pub struct Transaction {
          id: String,
          state: TransactionState,
          operations: Vec<String>,
      }

      impl Transaction {
          pub fn begin(id: &str) -> Self {
              println!("BEGIN transaction {}", id);
              Self {
                  id: id.to_string(),
                  state: TransactionState::Active,
                  operations: Vec::new(),
              }
          }

          pub fn execute(&mut self, op: &str) {
              self.operations.push(op.to_string());
          }

          pub fn commit(&mut self) {
              self.state = TransactionState::Committed;
              println!("COMMIT transaction {} ({} ops)", self.id, self.operations.len());
          }
      }

      impl Drop for Transaction {
          fn drop(&mut self) {
              if matches!(self.state, TransactionState::Active) {
                  println!("ROLLBACK transaction {} (auto)", self.id);
                  self.state = TransactionState::RolledBack;
              }
          }
      }
    src/logger.rs: |
      pub struct Logger {
          prefix: String,
          level: LogLevel,
      }

      #[derive(Clone, Copy, PartialEq, PartialOrd)]
      pub enum LogLevel {
          Debug,
          Info,
          Warn,
          Error,
      }

      impl Logger {
          pub fn new(prefix: &str, level: LogLevel) -> Self {
              Self {
                  prefix: prefix.to_string(),
                  level,
              }
          }

          pub fn info(&self, msg: &str) {
              if self.level <= LogLevel::Info {
                  println!("[{}][INFO] {}", self.prefix, msg);
              }
          }

          pub fn error(&self, msg: &str) {
              if self.level <= LogLevel::Error {
                  println!("[{}][ERROR] {}", self.prefix, msg);
              }
          }

          pub fn with_context(&self, host: &str, port: u16) -> String {
              format!("[{}] {}:{}", self.prefix, host, port)
          }
      }
  changed_files:
    src/lib.rs: |
      pub mod connection;
      pub mod file_handle;
      pub mod transaction;
      pub mod logger;
    src/connection.rs: |
      use std::sync::atomic::{AtomicUsize, Ordering};
      use std::time::{Duration, Instant};

      static CONNECTION_COUNT: AtomicUsize = AtomicUsize::new(0);
      static ACTIVE_CONNECTIONS: AtomicUsize = AtomicUsize::new(0);

      pub struct Connection {
          host: String,
          port: u16,
          connected: bool,
          id: usize,
          created_at: Instant,
      }

      impl Connection {
          pub fn new(host: &str, port: u16) -> Self {
              let id = CONNECTION_COUNT.fetch_add(1, Ordering::SeqCst);
              ACTIVE_CONNECTIONS.fetch_add(1, Ordering::SeqCst);
              println!("Creating connection {} to {}:{}", id, host, port);
              Self {
                  host: host.to_string(),
                  port,
                  connected: true,
                  id,
                  created_at: Instant::now(),
              }
          }

          pub fn is_connected(&self) -> bool {
              self.connected
          }

          pub fn host(&self) -> &str {
              &self.host
          }

          pub fn port(&self) -> u16 {
              self.port
          }

          pub fn id(&self) -> usize {
              self.id
          }

          pub fn uptime(&self) -> Duration {
              self.created_at.elapsed()
          }
      }

      impl Drop for Connection {
          fn drop(&mut self) {
              if self.connected {
                  println!("Auto-disconnecting connection {} from {}:{}", self.id, self.host, self.port);
                  self.connected = false;
              }
              ACTIVE_CONNECTIONS.fetch_sub(1, Ordering::SeqCst);
              println!("Connection {} dropped after {:?}", self.id, self.uptime());
          }
      }

      pub fn total_connections() -> usize {
          CONNECTION_COUNT.load(Ordering::SeqCst)
      }

      pub fn active_connections() -> usize {
          ACTIVE_CONNECTIONS.load(Ordering::SeqCst)
      }

      pub struct ConnectionPool {
          connections: Vec<Connection>,
          max_size: usize,
          host: String,
          port: u16,
      }

      impl ConnectionPool {
          pub fn new(host: &str, port: u16, max_size: usize) -> Self {
              Self {
                  connections: Vec::new(),
                  max_size,
                  host: host.to_string(),
                  port,
              }
          }

          pub fn acquire(&mut self) -> Option<&Connection> {
              if self.connections.len() < self.max_size {
                  self.connections.push(Connection::new(&self.host, self.port));
                  self.connections.last()
              } else {
                  self.connections.iter().find(|c| c.is_connected())
              }
          }

          pub fn size(&self) -> usize {
              self.connections.len()
          }

          pub fn active(&self) -> usize {
              self.connections.iter().filter(|c| c.is_connected()).count()
          }

          pub fn host(&self) -> &str {
              &self.host
          }

          pub fn port(&self) -> u16 {
              self.port
          }
      }

      impl Drop for ConnectionPool {
          fn drop(&mut self) {
              println!(
                  "Dropping ConnectionPool for {}:{} with {} connections",
                  self.host,
                  self.port,
                  self.connections.len()
              );
          }
      }

      pub struct ScopedConnection<'a> {
          pool: &'a mut ConnectionPool,
          connection_index: usize,
      }

      impl<'a> ScopedConnection<'a> {
          pub fn new(pool: &'a mut ConnectionPool) -> Option<Self> {
              let idx = pool.connections.len();
              if idx < pool.max_size {
                  pool.connections.push(Connection::new(&pool.host, pool.port));
                  Some(Self {
                      pool,
                      connection_index: idx,
                  })
              } else {
                  None
              }
          }

          pub fn connection(&self) -> &Connection {
              &self.pool.connections[self.connection_index]
          }
      }

      impl<'a> Drop for ScopedConnection<'a> {
          fn drop(&mut self) {
              println!(
                  "ScopedConnection releasing connection {}",
                  self.connection_index
              );
          }
      }

      pub trait Resource {
          fn cleanup(&mut self);
          fn is_valid(&self) -> bool;
      }

      impl Resource for Connection {
          fn cleanup(&mut self) {
              if self.connected {
                  println!("Cleaning up connection {}", self.id);
                  self.connected = false;
              }
          }

          fn is_valid(&self) -> bool {
              self.connected
          }
      }

      pub struct ResourceGuard<T: Resource> {
          resource: T,
      }

      impl<T: Resource> ResourceGuard<T> {
          pub fn new(resource: T) -> Self {
              Self { resource }
          }

          pub fn get(&self) -> &T {
              &self.resource
          }

          pub fn get_mut(&mut self) -> &mut T {
              &mut self.resource
          }
      }

      impl<T: Resource> Drop for ResourceGuard<T> {
          fn drop(&mut self) {
              self.resource.cleanup();
          }
      }
    src/file_handle.rs: |
      use std::fs::File;
      use std::io::{self, Write, Read};
      use std::path::PathBuf;

      pub struct FileHandle {
          file: File,
          path: PathBuf,
          bytes_written: usize,
      }

      impl FileHandle {
          pub fn create(path: &str) -> io::Result<Self> {
              let file = File::create(path)?;
              Ok(Self {
                  file,
                  path: PathBuf::from(path),
                  bytes_written: 0,
              })
          }

          pub fn write_data(&mut self, data: &[u8]) -> io::Result<()> {
              self.file.write_all(data)?;
              self.bytes_written += data.len();
              Ok(())
          }

          pub fn path(&self) -> &str {
              self.path.to_str().unwrap_or("")
          }

          pub fn bytes_written(&self) -> usize {
              self.bytes_written
          }
      }

      impl Drop for FileHandle {
          fn drop(&mut self) {
              let _ = self.file.flush();
              println!("Closing file: {} ({} bytes written)", self.path(), self.bytes_written);
          }
      }
    src/transaction.rs: |
      pub enum TransactionState {
          Active,
          Committed,
          RolledBack,
      }

      pub struct Transaction {
          id: String,
          state: TransactionState,
          operations: Vec<String>,
      }

      impl Transaction {
          pub fn begin(id: &str) -> Self {
              println!("BEGIN transaction {}", id);
              Self {
                  id: id.to_string(),
                  state: TransactionState::Active,
                  operations: Vec::new(),
              }
          }

          pub fn execute(&mut self, op: &str) {
              self.operations.push(op.to_string());
          }

          pub fn commit(&mut self) {
              self.state = TransactionState::Committed;
              println!("COMMIT transaction {} ({} ops)", self.id, self.operations.len());
          }
      }

      impl Drop for Transaction {
          fn drop(&mut self) {
              if matches!(self.state, TransactionState::Active) {
                  println!("ROLLBACK transaction {} (auto)", self.id);
                  self.state = TransactionState::RolledBack;
              }
          }
      }
    src/logger.rs: |
      pub struct Logger {
          prefix: String,
          level: LogLevel,
      }

      #[derive(Clone, Copy, PartialEq, PartialOrd)]
      pub enum LogLevel {
          Debug,
          Info,
          Warn,
          Error,
      }

      impl Logger {
          pub fn new(prefix: &str, level: LogLevel) -> Self {
              Self {
                  prefix: prefix.to_string(),
                  level,
              }
          }

          pub fn info(&self, msg: &str) {
              if self.level <= LogLevel::Info {
                  println!("[{}][INFO] {}", self.prefix, msg);
              }
          }

          pub fn error(&self, msg: &str) {
              if self.level <= LogLevel::Error {
                  println!("[{}][ERROR] {}", self.prefix, msg);
              }
          }

          pub fn with_context(&self, host: &str, port: u16) -> String {
              format!("[{}] {}:{}", self.prefix, host, port)
          }
      }
  commit_message: Update files
fragments:
- id: connection
  selector:
    path: src/connection.rs
    anchor: use std::time::{Duration, Instant};
- id: connection_1
  selector:
    path: src/connection.rs
    anchor: ACTIVE_CONNECTIONS.fetch_add(1, Ordering::SeqCst);
- id: connection_2
  selector:
    path: src/connection.rs
    anchor: impl Drop for Connection {
- id: connection_3
  selector:
    path: src/connection.rs
    anchor: pub struct ConnectionPool {
- id: connection_4
  selector:
    path: src/connection.rs
    anchor: 'pub struct ResourceGuard<T: Resource> {'
- id: no_file_handle
  selector:
    path: src/file_handle.rs
- id: no_transaction
  selector:
    path: src/transaction.rs
- id: no_logger
  selector:
    path: src/logger.rs
- id: no_global_0
  selector:
    anchor: FileHandle
- id: no_global_1
  selector:
    anchor: write_data
- id: no_global_2
  selector:
    anchor: bytes_written
- id: no_global_3
  selector:
    anchor: TransactionState
- id: no_global_4
  selector:
    anchor: BEGIN transaction
- id: no_global_5
  selector:
    anchor: ROLLBACK
- id: no_global_6
  selector:
    anchor: struct Logger
- id: no_global_7
  selector:
    anchor: LogLevel
- id: no_global_8
  selector:
    anchor: with_context
oracle:
  required:
  - connection
  - connection_1
  - connection_2
  - connection_3
  - connection_4
  allowed: []
  forbidden:
  - no_file_handle
  - no_transaction
  - no_logger
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
  - no_global_8
