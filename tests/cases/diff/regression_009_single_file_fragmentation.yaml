name: regression_009_single_file_fragmentation
tags:
- lang:python
- change:single_file
- change:new_function
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    src/auth/login.py: |
      from hashlib import sha256

      def hash_password(password):
          return sha256(password.encode()).hexdigest()

      def verify_password(password, password_hash):
          return hash_password(password) == password_hash

      def create_session(user_id):
          import uuid
          return str(uuid.uuid4())

      def validate_session(session_id):
          return len(session_id) == 36
  changed_files:
    src/auth/login.py: |
      from hashlib import sha256
      import secrets

      def hash_password(password, salt=None):
          if salt is None:
              salt = secrets.token_hex(16)
          return f"{salt}:{sha256((salt + password).encode()).hexdigest()}"

      def verify_password(password, password_hash):
          salt, hashed = password_hash.split(":")
          return hash_password(password, salt) == password_hash

      def create_session(user_id):
          import uuid
          return str(uuid.uuid4())

      def validate_session(session_id):
          return len(session_id) == 36
  commit_message: Update files
fragments:
- id: login_file
  selector:
    path: login.py
- id: req_global_0
  selector:
    anchor: hash_password
- id: req_global_1
  selector:
    anchor: verify_password
- id: no_global_0
  selector:
    anchor: GARBAGE_REG_009_A
oracle:
  required:
  - login_file
  - req_global_0
  - req_global_1
  allowed: []
  forbidden:
  - no_global_0
