name: csharp_020_enumerable_extension
tags:
- lang:csharp
- change:single_file
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    Extensions/EnumerableExtensions.cs: |
      namespace MyApp.Extensions;

      public static class EnumerableExtensions
      {
          public static IEnumerable<T> WhereNotNull<T>(this IEnumerable<T?> source) where T : class
          {
              return source.Where(x => x != null)!;
          }

          public static IEnumerable<IEnumerable<T>> Batch<T>(this IEnumerable<T> source, int size)
          {
              var batch = new List<T>(size);
              foreach (var item in source)
              {
                  batch.Add(item);
                  if (batch.Count == size)
                  {
                      yield return batch;
                      batch = new List<T>(size);
                  }
              }
              if (batch.Count > 0)
                  yield return batch;
          }
      }
    Services/BatchProcessor.cs: |
      namespace MyApp.Services;

      public class BatchProcessor { }
    Unrelated/HealthMonitor.cs: |
      namespace MyApp.Unrelated;

      public class HealthMonitor
      {
          private const string GARBAGE_CS_020_HEALTH_MONITOR_A = "health_monitor_marker";

          public bool IsHealthy() => GARBAGE_CS_020_HEALTH_MONITOR_A.Length > 0;
      }
    Unrelated/ConfigWatcher.cs: |
      namespace MyApp.Unrelated;

      public class ConfigWatcher
      {
          public const string GARBAGE_CS_020_CONFIG_WATCHER_B = "config_watcher_marker";

          public void Watch(string path) => _ = GARBAGE_CS_020_CONFIG_WATCHER_B + path;
      }
  changed_files:
    Extensions/EnumerableExtensions.cs: |
      namespace MyApp.Extensions;

      public static class EnumerableExtensions
      {
          public static IEnumerable<T> WhereNotNull<T>(this IEnumerable<T?> source) where T : class
          {
              return source.Where(x => x != null)!;
          }

          public static IEnumerable<IEnumerable<T>> Batch<T>(this IEnumerable<T> source, int size)
          {
              var batch = new List<T>(size);
              foreach (var item in source)
              {
                  batch.Add(item);
                  if (batch.Count == size)
                  {
                      yield return batch;
                      batch = new List<T>(size);
                  }
              }
              if (batch.Count > 0)
                  yield return batch;
          }
      }
    Services/BatchProcessor.cs: |
      using MyApp.Extensions;
      using MyApp.Models;

      namespace MyApp.Services;

      public class BatchProcessor
      {
          public void ProcessUsers(IEnumerable<User?> users)
          {
              var validUsers = users.WhereNotNull();

              foreach (var batch in validUsers.Batch(100))
              {
                  ProcessBatch(batch);
              }
          }

          private void ProcessBatch(IEnumerable<User> batch)
          {
              foreach (var user in batch)
              {
                  Console.WriteLine(user.Name);
              }
          }
      }
    Unrelated/HealthMonitor.cs: |
      namespace MyApp.Unrelated;

      public class HealthMonitor
      {
          private const string GARBAGE_CS_020_HEALTH_MONITOR_A = "health_monitor_marker";

          public bool IsHealthy() => GARBAGE_CS_020_HEALTH_MONITOR_A.Length > 0;
      }
    Unrelated/ConfigWatcher.cs: |
      namespace MyApp.Unrelated;

      public class ConfigWatcher
      {
          public const string GARBAGE_CS_020_CONFIG_WATCHER_B = "config_watcher_marker";

          public void Watch(string path) => _ = GARBAGE_CS_020_CONFIG_WATCHER_B + path;
      }
  commit_message: Update files
fragments:
- id: batchprocessor_file
  selector:
    path: Services/BatchProcessor.cs
- id: req_global_0
  selector:
    anchor: using MyApp.Extensions;
- id: req_global_1
  selector:
    anchor: public void ProcessUsers(IEnumerable<User?> users)
- id: no_global_0
  selector:
    anchor: GARBAGE_CS_020_CONFIG_WATCHER_B
- id: no_global_1
  selector:
    anchor: GARBAGE_CS_020_HEALTH_MONITOR_A
- id: no_global_2
  selector:
    anchor: GARBAGE_CS_020_HEALTH_MONITOR_A.Length
- id: no_global_3
  selector:
    anchor: ConfigWatcher.cs
- id: no_global_4
  selector:
    anchor: HealthMonitor.cs
oracle:
  required:
  - batchprocessor_file
  - req_global_0
  - req_global_1
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
