name: merging_003_overlapping_ranges
initial:
  overlapping.py: |
    class BigClass:
        def method1(self):
            x = 1
            y = 2
            return x + y

        def method2(self):
            a = 1
            b = 2
            return a * b
changed:
  overlapping.py: |
    class BigClass:
        def method1(self):
            x = 10
            y = 20
            return x + y

        def method2(self):
            a = 100
            b = 200
            return a * b
assertions:
  must_include:
  - method1
  - method2
options:
  commit_message: Change both methods in class
