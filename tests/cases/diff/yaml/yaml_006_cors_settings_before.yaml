config/api.yaml: |
  cors:
    allowed_origins:
      - http://localhost:3000
    allow_credentials: true
src/middleware.py: |
  def setup_cors(app, origins: list[str], credentials: bool = True):
      for origin in origins:
          app.allow_origin(origin, credentials=credentials)
garbage_search_indexer.py: |
  GARBAGE_YAML_006_SEARCH_MARKER_A = "search"
  GARBAGE_YAML_006_INDEX_MARKER_B = True

  class SearchIndexer:
      GARBAGE_YAML_006_INDEXER_CLASS_C = "indexer"

      def reindex(self, collection: str) -> int:
          return 0
garbage_audit_logger.py: |
  GARBAGE_YAML_006_AUDIT_MARKER_D = "audit"
  GARBAGE_YAML_006_TRAIL_MARKER_E = 42

  class AuditLogger:
      GARBAGE_YAML_006_LOGGER_CLASS_F = True

      def log_action(self, user_id: str, action: str) -> None:
          pass
