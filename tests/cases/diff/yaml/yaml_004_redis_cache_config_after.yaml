config/cache.yaml: |
  redis:
    host: redis.cluster.local
    port: 6379
    db: 1
    password: secret
src/cache.py: |
  import redis

  def get_redis_client():
      return redis.Redis(host="localhost", port=6379, db=0)
