initial:
  config/resilience.yaml: |
    retry:
      max_attempts: 3
      backoff_factor: 2
  src/retry.py: |
    import time

    def with_retry(func, max_attempts: int = 3, backoff: float = 2.0):
        def wrapper(*args, **kwargs):
            for attempt in range(max_attempts):
                try:
                    return func(*args, **kwargs)
                except Exception:
                    if attempt < max_attempts - 1:
                        time.sleep(backoff ** attempt)
                    else:
                        raise
        return wrapper
  garbage_config_loader.py: |
    GARBAGE_YAML_017_CONFIG_MARKER_A = "config"
    GARBAGE_YAML_017_LOADER_MARKER_B = True

    class ConfigLoader:
        GARBAGE_YAML_017_LOADER_CLASS_C = "loader"

        def load(self, path: str) -> dict:
            return {}
  garbage_dependency_injector.py: |
    GARBAGE_YAML_017_INJECT_MARKER_D = "inject"
    GARBAGE_YAML_017_CONTAINER_MARKER_E = 42

    class DependencyInjector:
        GARBAGE_YAML_017_INJECTOR_CLASS_F = True

        def register(self, name: str, factory: object) -> None:
            pass
changed:
  config/resilience.yaml: |
    retry:
      max_attempts: 5
      backoff_factor: 1.5
      jitter: true
  src/retry.py: |
    import time

    def with_retry(func, max_attempts: int = 3, backoff: float = 2.0):
        def wrapper(*args, **kwargs):
            for attempt in range(max_attempts):
                try:
                    return func(*args, **kwargs)
                except Exception:
                    if attempt < max_attempts - 1:
                        time.sleep(backoff ** attempt)
                    else:
                        raise
        return wrapper
  garbage_config_loader.py: |
    GARBAGE_YAML_017_CONFIG_MARKER_A = "config"
    GARBAGE_YAML_017_LOADER_MARKER_B = True

    class ConfigLoader:
        GARBAGE_YAML_017_LOADER_CLASS_C = "loader"

        def load(self, path: str) -> dict:
            return {}
  garbage_dependency_injector.py: |
    GARBAGE_YAML_017_INJECT_MARKER_D = "inject"
    GARBAGE_YAML_017_CONTAINER_MARKER_E = 42

    class DependencyInjector:
        GARBAGE_YAML_017_INJECTOR_CLASS_F = True

        def register(self, name: str, factory: object) -> None:
            pass
must_include_files:
- config/resilience.yaml
must_include_content:
- |2
    max_attempts: 5
    backoff_factor: 1.5
    jitter: true
must_not_include:
- GARBAGE_YAML_017_CONFIG_MARKER_A
- GARBAGE_YAML_017_LOADER_MARKER_B
- GARBAGE_YAML_017_LOADER_CLASS_C
- GARBAGE_YAML_017_INJECT_MARKER_D
- GARBAGE_YAML_017_CONTAINER_MARKER_E
- GARBAGE_YAML_017_INJECTOR_CLASS_F
- ConfigLoader
- DependencyInjector
- garbage_config_loader.py
- garbage_dependency_injector.py
add_garbage_files: false
skip_garbage_check: true
