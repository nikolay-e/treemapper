config/cache.yaml: |
  cache:
    default_ttl: 300
    max_size: 1000
src/caching.py: |
  from functools import lru_cache

  def cached(ttl: int = 300):
      def decorator(func):
          return lru_cache(maxsize=1000)(func)
      return decorator
garbage_compression_util.py: |
  GARBAGE_YAML_008_COMPRESS_MARKER_A = "compress"
  GARBAGE_YAML_008_GZIP_MARKER_B = True

  class CompressionUtil:
      GARBAGE_YAML_008_UTIL_CLASS_C = "compress"

      def compress(self, data: bytes) -> bytes:
          return data
garbage_serializer.py: |
  GARBAGE_YAML_008_SERIAL_MARKER_D = "serial"
  GARBAGE_YAML_008_FORMAT_MARKER_E = 42

  class DataSerializer:
      GARBAGE_YAML_008_SERIALIZER_CLASS_F = True

      def serialize(self, obj: dict) -> str:
          return ""
