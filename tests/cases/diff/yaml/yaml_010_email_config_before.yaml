config/email.yaml: |
  email:
    smtp_host: localhost
    smtp_port: 25
    from_address: noreply@example.com
src/mailer.py: |
  import smtplib

  class Mailer:
      def __init__(self, host: str, port: int, from_addr: str):
          self.host = host
          self.port = port
          self.from_addr = from_addr

      def send(self, to: str, subject: str, body: str):
          pass
garbage_scheduler_cron.py: |
  GARBAGE_YAML_010_CRON_MARKER_A = "cron"
  GARBAGE_YAML_010_SCHEDULE_MARKER_B = True

  class CronScheduler:
      GARBAGE_YAML_010_SCHEDULER_CLASS_C = "cron"

      def add_job(self, expression: str, task: str) -> None:
          pass
garbage_event_bus.py: |
  GARBAGE_YAML_010_EVENT_MARKER_D = "event"
  GARBAGE_YAML_010_BUS_MARKER_E = 42

  class EventBus:
      GARBAGE_YAML_010_BUS_CLASS_F = True

      def publish(self, topic: str, payload: dict) -> None:
          pass
