name: yaml_015_security_headers
tags:
- lang:rust
- change:bug_fix
- change:single_file
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    src/cache/ttl_store.rs: |
      use std::collections::HashMap;
      use std::time::{Duration, Instant};

      struct Entry<V> {
          value: V,
          expires_at: Instant,
      }

      pub struct TtlStore<V> {
          map: HashMap<String, Entry<V>>,
          default_ttl: Duration,
      }

      impl<V> TtlStore<V> {
          pub fn new(default_ttl: Duration) -> Self {
              TtlStore { map: HashMap::new(), default_ttl }
          }

          pub fn insert(&mut self, key: String, value: V) {
              let entry = Entry {
                  value,
                  expires_at: Instant::now() + self.default_ttl,
              };
              self.map.insert(key, entry);
          }

          pub fn get(&self, key: &str) -> Option<&V> {
              self.map.get(key)
                  .filter(|e| e.expires_at > Instant::now())  // BUG: should be >= not >
                  .map(|e| &e.value)
          }

          pub fn remove_expired(&mut self) {
              let now = Instant::now();
              self.map.retain(|_, e| e.expires_at > now);
          }
      }
    garbage/eviction.rs: |
      // GARBAGE_YAML15_EVICT_A = "eviction_marker"
    garbage/persistence.rs: |
      // GARBAGE_YAML15_PERS_B = "persistence_marker"
    garbage/replication.rs: |
      // GARBAGE_YAML15_REPL_C = "replication_marker"
  changed_files:
    src/cache/ttl_store.rs: |
      use std::collections::HashMap;
      use std::time::{Duration, Instant};

      struct Entry<V> {
          value: V,
          expires_at: Instant,
      }

      pub struct TtlStore<V> {
          map: HashMap<String, Entry<V>>,
          default_ttl: Duration,
      }

      impl<V> TtlStore<V> {
          pub fn new(default_ttl: Duration) -> Self {
              TtlStore { map: HashMap::new(), default_ttl }
          }

          pub fn insert(&mut self, key: String, value: V) {
              let entry = Entry {
                  value,
                  expires_at: Instant::now() + self.default_ttl,
              };
              self.map.insert(key, entry);
          }

          pub fn get(&self, key: &str) -> Option<&V> {
              self.map.get(key)
                  .filter(|e| e.expires_at >= Instant::now())
                  .map(|e| &e.value)
          }

          pub fn remove_expired(&mut self) {
              let now = Instant::now();
              self.map.retain(|_, e| e.expires_at >= now);
          }
      }
    garbage/eviction.rs: |
      // GARBAGE_YAML15_EVICT_A = "eviction_marker"
    garbage/persistence.rs: |
      // GARBAGE_YAML15_PERS_B = "persistence_marker"
    garbage/replication.rs: |
      // GARBAGE_YAML15_REPL_C = "replication_marker"
  commit_message: "fix: use >= for TTL expiry check to include entries expiring exactly now"
fragments:
- id: get_filter_fix
  selector:
    path: src/cache/ttl_store.rs
    anchor: ".filter(|e| e.expires_at >= Instant::now())"
- id: remove_expired_fix
  selector:
    path: src/cache/ttl_store.rs
    anchor: "self.map.retain(|_, e| e.expires_at >= now);"
- id: insert_method
  selector:
    path: src/cache/ttl_store.rs
    anchor: "pub fn insert(&mut self, key: String, value: V) {"
- id: no_garbage_a
  selector:
    anchor: GARBAGE_YAML15_EVICT_A
- id: no_garbage_b
  selector:
    anchor: GARBAGE_YAML15_PERS_B
- id: no_garbage_c
  selector:
    anchor: GARBAGE_YAML15_REPL_C
oracle:
  required:
  - get_filter_fix
  - remove_expired_fix
  allowed:
  - insert_method
  forbidden:
  - no_garbage_a
  - no_garbage_b
  - no_garbage_c
