# Scenario: Service.php starts using a LoggingTrait — adds `use LoggingTrait`
# and calls $this->log() and $this->logError() inside a new process() method.
#
# Diff: Service.php changes from an empty class to one that mixes in
# LoggingTrait and uses log()/logError() for processing lifecycle messages.
#
# Reviewer questions:
#   Q1: "What methods does LoggingTrait provide?"
#       -> src/Traits/LoggingTrait.php (UNCHANGED) defines log(), logError(),
#          logWarning(), getLogEntries(), clearLog(). Reviewer verifies that
#          Service calls valid trait methods and understands the log format.
#   Q2: "Does log() have side effects (stdout, storage)?"
#       -> LoggingTrait.php shows log() echoes to stdout and appends to
#          $logEntries array. Reviewer assesses whether that's appropriate
#          for a Service class.
#
# Not needed:
#   - src/Traits/CacheTrait.php: provides caching methods (get/set/flush).
#     Same Traits namespace, same "trait with private array storage" pattern.
#     But Service doesn't use CacheTrait — no `use CacheTrait` statement.
#   - src/Traits/EventEmitterTrait.php: another trait with emit/on methods.
#     Same package, same pattern. Third trait amplifies the sibling trap.
#   - src/Services/NotificationService.php: another service that ALSO uses
#     LoggingTrait. HARDEST TRAP: same `use LoggingTrait`, same log()/logError()
#     calls. Direct structural overlap. But a sibling consumer doesn't help
#     review Service.php's usage.
#   - src/Services/QueueService.php: yet another LoggingTrait user.
#     Same trait usage, different service.
#   - src/Formatters/JsonFormatter.php: formats data as JSON strings.
#     Formatting utility, no connection to Service or LoggingTrait.

max_enrichment: 8.6
initial:
  src/Services/Service.php: |
    <?php
    namespace App\Services;

    class Service {
    }
  src/Traits/LoggingTrait.php: |
    <?php
    namespace App\Traits;

    trait LoggingTrait {
        private array $logEntries = [];

        public function log(string $message, string $level = 'INFO'): void {
            $entry = sprintf("[%s] %s: %s", date('Y-m-d H:i:s'), $level, $message);
            $this->logEntries[] = $entry;
            echo $entry . "\n";
        }

        public function logError(string $message): void {
            $this->log($message, 'ERROR');
        }

        public function logWarning(string $message): void {
            $this->log($message, 'WARNING');
        }

        public function getLogEntries(): array {
            return $this->logEntries;
        }

        public function clearLog(): void {
            $this->logEntries = [];
        }
    }
  src/Traits/CacheTrait.php: |
    <?php
    namespace App\Traits;

    trait CacheTrait {
        private array $cache = [];

        public function cacheGet(string $key): mixed {
            return $this->cache[$key] ?? null;
        }

        public function cacheSet(string $key, mixed $value, int $ttl = 3600): void {
            $this->cache[$key] = ['value' => $value, 'expires' => time() + $ttl];
        }

        public function cacheFlush(): void {
            $this->cache = [];
        }
    }
  src/Traits/EventEmitterTrait.php: |
    <?php
    namespace App\Traits;

    trait EventEmitterTrait {
        private array $listeners = [];

        public function on(string $event, callable $callback): void {
            $this->listeners[$event][] = $callback;
        }

        public function emit(string $event, array $data = []): void {
            foreach ($this->listeners[$event] ?? [] as $callback) {
                $callback($data);
            }
        }

        public function removeListeners(string $event): void {
            unset($this->listeners[$event]);
        }
    }
  src/Services/NotificationService.php: |
    <?php
    namespace App\Services;

    use App\Traits\LoggingTrait;

    class NotificationService {
        use LoggingTrait;

        private array $recipients = [];

        public function addRecipient(string $email): void {
            $this->recipients[] = $email;
            $this->log("Added recipient: $email");
        }

        public function send(string $message): void {
            foreach ($this->recipients as $email) {
                $this->log("Sending to $email: $message");
            }
            if (empty($this->recipients)) {
                $this->logError("No recipients configured");
            }
        }
    }
  src/Services/QueueService.php: |
    <?php
    namespace App\Services;

    use App\Traits\LoggingTrait;

    class QueueService {
        use LoggingTrait;

        private array $queue = [];

        public function push(mixed $item): void {
            $this->queue[] = $item;
            $this->log("Item added to queue, size: " . count($this->queue));
        }

        public function pop(): mixed {
            if (empty($this->queue)) {
                $this->logError("Cannot pop from empty queue");
                return null;
            }
            return array_shift($this->queue);
        }
    }
  src/Formatters/JsonFormatter.php: |
    <?php
    namespace App\Formatters;

    class JsonFormatter {
        public static function format(array $data): string {
            return json_encode($data, JSON_PRETTY_PRINT);
        }

        public static function decode(string $json): array {
            return json_decode($json, true) ?? [];
        }
    }
changed:
  src/Services/Service.php: |
    <?php
    namespace App\Services;

    use App\Traits\LoggingTrait;

    class Service {
        use LoggingTrait;

        public function process(array $data): array {
            $this->log("Processing started with " . count($data) . " items");
            $result = array_filter($data, fn($item) => $item !== null);
            if (count($result) < count($data)) {
                $this->logError("Filtered out " . (count($data) - count($result)) . " null items");
            }
            $this->log("Processing completed, " . count($result) . " valid items");
            return $result;
        }
    }
must_include_files:
  # Q1+Q2: LoggingTrait defines log(), logError() — the methods Service now calls
  - src/Traits/LoggingTrait.php
must_include_content_from:
  src/Traits/LoggingTrait.php:
    - "public function log(string $message, string $level = 'INFO'): void"
    - "public function logError(string $message): void"
    - "$this->logEntries[] = $entry"
must_not_include:
  # CacheTrait.php: another trait, same namespace, not used by Service
  - "trait CacheTrait"
  - "cacheGet"
  - "cacheFlush"
  # EventEmitterTrait.php: another trait in same package
  - "trait EventEmitterTrait"
  - "emit"
  - "removeListeners"
  # NotificationService.php: USES THE SAME TRAIT — sibling consumer
  - "class NotificationService"
  - "addRecipient"
  # QueueService.php: another LoggingTrait user
  - "class QueueService"
  - "Cannot pop from empty queue"
  # JsonFormatter.php: formatting utility, no connection
  - "class JsonFormatter"
  - "JSON_PRETTY_PRINT"
must_not_include_files:
  - src/Traits/CacheTrait.php
  - src/Traits/EventEmitterTrait.php
  - src/Services/NotificationService.php
  - src/Services/QueueService.php
  - src/Formatters/JsonFormatter.php
max_fragments: 5
max_files: 3
add_garbage_files: false
skip_garbage_check: true
