# Scenario: DataTransformer.php adds a return type declaration using the
# ApiResponse class — transformPayload() returns a new ApiResponse(200, $result).
#
# Diff: DataTransformer.php changes from an empty class to one that imports
# App\Http\ApiResponse via `use` and returns a typed ApiResponse from
# transformPayload().
#
# Reviewer questions:
#   Q1: "What is the ApiResponse class? What constructor does it expect?"
#       -> src/Http/ApiResponse.php (UNCHANGED) defines __construct(int $statusCode,
#          array $payload). Reviewer verifies the call new ApiResponse(200, $result)
#          matches the constructor signature.
#   Q2: "What methods does ApiResponse expose for downstream consumers?"
#       -> ApiResponse.php defines getStatusCode():int and getPayload():array.
#          Reviewer understands the interface returned by transformPayload().
#
# Not needed:
#   - src/Logging/FileLogger.php: file-based logger in a different namespace.
#     No connection to DataTransformer or ApiResponse.
#   - src/Queue/JobRunner.php: job queue runner in a different namespace.
#     No connection to the Http namespace.
#   - src/Services/ReportGenerator.php: also imports ApiResponse and returns it.
#     DIRECT structural edge (same class, same return type) but generates reports.
#   - src/Http/ErrorResponse.php: sibling response class in the same namespace.
#     Same "response" domain, similar structure, but not imported by DataTransformer.
#   - src/Middleware/ResponseMiddleware.php: wraps ApiResponse objects with headers.
#     References ApiResponse but is middleware — different concern.

max_enrichment: 15.2
initial:
  src/Services/DataTransformer.php: |
    <?php
    namespace App\Services;

    class DataTransformer {
    }
  src/Http/ApiResponse.php: |
    <?php
    namespace App\Http;

    class ApiResponse {
        private int $statusCode;
        private array $payload;

        public function __construct(int $statusCode, array $payload) {
            $this->statusCode = $statusCode;
            $this->payload = $payload;
        }

        public function getStatusCode(): int {
            return $this->statusCode;
        }

        public function getPayload(): array {
            return $this->payload;
        }

        public function isSuccessful(): bool {
            return $this->statusCode >= 200 && $this->statusCode < 300;
        }

        public function toArray(): array {
            return ['status' => $this->statusCode, 'data' => $this->payload];
        }
    }
  src/Logging/FileLogger.php: |
    <?php
    namespace App\Logging;

    class FileLogger {
        private string $logFilePath;

        public function __construct(string $logFilePath = '/var/log/app.log') {
            $this->logFilePath = $logFilePath;
        }

        public function writeInfo(string $message): void {
            file_put_contents($this->logFilePath, "[INFO] $message\n", FILE_APPEND);
        }

        public function writeError(string $message): void {
            file_put_contents($this->logFilePath, "[ERROR] $message\n", FILE_APPEND);
        }

        public function clear(): void {
            file_put_contents($this->logFilePath, '');
        }
    }
  src/Queue/JobRunner.php: |
    <?php
    namespace App\Queue;

    class JobRunner {
        private array $pendingJobs = [];

        public function enqueueJob(string $jobClass, array $params = []): void {
            $this->pendingJobs[] = ['class' => $jobClass, 'params' => $params];
        }

        public function processNextJob(): bool {
            if (empty($this->pendingJobs)) {
                return false;
            }
            $job = array_shift($this->pendingJobs);
            return true;
        }

        public function pendingCount(): int {
            return count($this->pendingJobs);
        }
    }
  src/Services/ReportGenerator.php: |
    <?php
    namespace App\Services;

    use App\Http\ApiResponse;

    class ReportGenerator {
        public function generateMonthlyReport(array $data): ApiResponse {
            $summary = array_map(fn($row) => $row['total'], $data);
            return new ApiResponse(200, ['summary' => $summary, 'count' => count($data)]);
        }

        public function generateDailyReport(string $date, array $entries): ApiResponse {
            return new ApiResponse(200, ['date' => $date, 'entries' => $entries]);
        }
    }
  src/Http/ErrorResponse.php: |
    <?php
    namespace App\Http;

    class ErrorResponse {
        private int $statusCode;
        private string $message;
        private array $details;

        public function __construct(int $statusCode, string $message, array $details = []) {
            $this->statusCode = $statusCode;
            $this->message = $message;
            $this->details = $details;
        }

        public function getStatusCode(): int {
            return $this->statusCode;
        }

        public function getMessage(): string {
            return $this->message;
        }

        public function toArray(): array {
            return ['error' => $this->message, 'details' => $this->details];
        }
    }
  src/Middleware/ResponseMiddleware.php: |
    <?php
    namespace App\Middleware;

    use App\Http\ApiResponse;

    class ResponseMiddleware {
        public function handle(ApiResponse $response): array {
            return [
                'status' => $response->getStatusCode(),
                'headers' => ['Content-Type' => 'application/json'],
                'body' => json_encode($response->getPayload()),
            ];
        }

        public function addCorsHeaders(array $response): array {
            $response['headers']['Access-Control-Allow-Origin'] = '*';
            return $response;
        }
    }

changed:
  src/Services/DataTransformer.php: |
    <?php
    namespace App\Services;

    use App\Http\ApiResponse;

    class DataTransformer {
        public function transformPayload(array $data): ApiResponse {
            $result = array_map(fn($x) => $x * 2, $data);
            return new ApiResponse(200, $result);
        }
    }
  src/Http/ApiResponse.php: |
    <?php
    namespace App\Http;

    class ApiResponse {
        private int $statusCode;
        private array $payload;

        public function __construct(int $statusCode, array $payload) {
            $this->statusCode = $statusCode;
            $this->payload = $payload;
        }

        public function getStatusCode(): int {
            return $this->statusCode;
        }

        public function getPayload(): array {
            return $this->payload;
        }

        public function isSuccessful(): bool {
            return $this->statusCode >= 200 && $this->statusCode < 300;
        }

        public function toArray(): array {
            return ['status' => $this->statusCode, 'data' => $this->payload];
        }
    }

must_include_files:
  # Q1+Q2: ApiResponse.php defines the return type class used by transformPayload()
  - src/Http/ApiResponse.php
must_include_content_from:
  src/Http/ApiResponse.php:
    - "public function __construct(int $statusCode, array $payload)"
    - "public function getStatusCode(): int"
    - "public function getPayload(): array"
  src/Services/DataTransformer.php:
    - "use App\\Http\\ApiResponse"
    - "new ApiResponse(200, $result)"
must_not_include:
  # src/Logging/FileLogger.php: logging utility in a different namespace
  - "class FileLogger"
  - "logFilePath"
  - writeInfo
  # src/Queue/JobRunner.php: queue runner in a different namespace
  - "class JobRunner"
  - enqueueJob
  - pendingCount
  # src/Services/ReportGenerator.php: HARDEST TRAP — also imports ApiResponse
  # and returns new ApiResponse(200, ...) — same class, same pattern
  - ReportGenerator
  - generateMonthlyReport
  - generateDailyReport
  # src/Http/ErrorResponse.php: sibling in Http namespace — domain trap
  - ErrorResponse
  - getMessage
  - "'error' =>"
  # src/Middleware/ResponseMiddleware.php: references ApiResponse
  - ResponseMiddleware
  - addCorsHeaders
  - "Access-Control-Allow-Origin"
must_not_include_files:
  - src/Logging/FileLogger.php
  - src/Queue/JobRunner.php
  - src/Services/ReportGenerator.php
  - src/Http/ErrorResponse.php
  - src/Middleware/ResponseMiddleware.php
max_fragments: 5
max_files: 3
add_garbage_files: false
skip_garbage_check: true
