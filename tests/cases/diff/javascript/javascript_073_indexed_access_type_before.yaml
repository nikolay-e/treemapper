schema.ts: |
  export interface Schema {
      users: { id: string; name: string }[];
      posts: { id: string; title: string; content: string }[];
  }
accessor.ts: |
  console.log('initial');
garbage_analytics.ts: |
  const GARBAGE_JS_073_ANALYTICS_MARKER_A = "analytics";
  const GARBAGE_JS_073_TRACKER_MARKER_B = true;

  class AnalyticsTracker {
      static GARBAGE_JS_073_TRACKER_CLASS_C = "tracker";

      track(event: string, payload: Record<string, unknown>): void {
          // noop
      }
  }
garbage_caching.ts: |
  const GARBAGE_JS_073_CACHE_MARKER_D = "caching";
  const GARBAGE_JS_073_TTL_MARKER_E = 3600;

  class CacheManager {
      static GARBAGE_JS_073_MANAGER_CLASS_F = true;

      get(key: string): string | null {
          return null;
      }
  }
