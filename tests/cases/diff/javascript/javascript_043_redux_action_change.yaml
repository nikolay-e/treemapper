initial:
  actions.ts: |
    export const INCREMENT = 'INCREMENT';
    export const DECREMENT = 'DECREMENT';

    export const increment = () => ({ type: INCREMENT });
    export const decrement = () => ({ type: DECREMENT });
  reducer.ts: |
    import { INCREMENT, DECREMENT } from './actions';

    const initialState = { count: 0 };

    export function counterReducer(state = initialState, action: any) {
        switch (action.type) {
            case INCREMENT:
                return { count: state.count + 1 };
            case DECREMENT:
                return { count: state.count - 1 };
            default:
                return state;
        }
    }
  garbage_migration.ts: |
    const GARBAGE_JS_043_MIGRATION_MARKER_A = "migration";
    const GARBAGE_JS_043_SCHEMA_MARKER_B = true;

    class SchemaMigrator {
        static GARBAGE_JS_043_MIGRATOR_CLASS_C = "migrator";

        migrate(version: number): void {
            // noop
        }
    }
  garbage_backup.ts: |
    const GARBAGE_JS_043_BACKUP_MARKER_D = "backup";
    const GARBAGE_JS_043_SNAPSHOT_MARKER_E = 7;

    class BackupManager {
        static GARBAGE_JS_043_BACKUP_CLASS_F = true;

        createSnapshot(name: string): string {
            return name;
        }
    }
changed:
  actions.ts: |
    export const INCREMENT = 'INCREMENT';
    export const DECREMENT = 'DECREMENT';
    export const RESET = 'RESET';
    export const SET = 'SET';

    export const increment = () => ({ type: INCREMENT });
    export const decrement = () => ({ type: DECREMENT });
    export const reset = () => ({ type: RESET });
    export const setValue = (value: number) => ({ type: SET, payload: value });
  reducer.ts: |
    import { INCREMENT, DECREMENT } from './actions';

    const initialState = { count: 0 };

    export function counterReducer(state = initialState, action: any) {
        switch (action.type) {
            case INCREMENT:
                return { count: state.count + 1 };
            case DECREMENT:
                return { count: state.count - 1 };
            default:
                return state;
        }
    }
  garbage_migration.ts: |
    const GARBAGE_JS_043_MIGRATION_MARKER_A = "migration";
    const GARBAGE_JS_043_SCHEMA_MARKER_B = true;

    class SchemaMigrator {
        static GARBAGE_JS_043_MIGRATOR_CLASS_C = "migrator";

        migrate(version: number): void {
            // noop
        }
    }
  garbage_backup.ts: |
    const GARBAGE_JS_043_BACKUP_MARKER_D = "backup";
    const GARBAGE_JS_043_SNAPSHOT_MARKER_E = 7;

    class BackupManager {
        static GARBAGE_JS_043_BACKUP_CLASS_F = true;

        createSnapshot(name: string): string {
            return name;
        }
    }
must_include_files:
- actions.ts
must_include_content:
- INCREMENT = 'INCREMENT'
- DECREMENT = 'DECREMENT'
must_not_include:
- GARBAGE_JS_043_BACKUP_CLASS_F
- GARBAGE_JS_043_BACKUP_MARKER_D
- GARBAGE_JS_043_MIGRATION_MARKER_A
- GARBAGE_JS_043_MIGRATOR_CLASS_C
- GARBAGE_JS_043_SCHEMA_MARKER_B
- GARBAGE_JS_043_SNAPSHOT_MARKER_E
- garbage_backup.ts
- garbage_migration.ts
add_garbage_files: false
skip_garbage_check: true
