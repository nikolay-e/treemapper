useApi.ts: |
  import { useState, useEffect } from 'react';

  export function useApi<T>(url: string) {
      const [data, setData] = useState<T | null>(null);
      const [loading, setLoading] = useState(true);
      const [error, setError] = useState<Error | null>(null);

      useEffect(() => {
          fetch(url)
              .then(res => res.json())
              .then(setData)
              .catch(setError)
              .finally(() => setLoading(false));
      }, [url]);

      return { data, loading, error };
  }
UserList.tsx: |
  console.log('initial');
garbage_email.ts: |
  const GARBAGE_JS_063_EMAIL_MARKER_A = "email";
  const GARBAGE_JS_063_SMTP_MARKER_B = true;

  class EmailDispatcher {
      static GARBAGE_JS_063_DISPATCHER_CLASS_C = "dispatcher";

      send(to: string, subject: string, body: string): void {
          // noop
      }
  }
garbage_logging.ts: |
  const GARBAGE_JS_063_LOG_MARKER_D = "logging";
  const GARBAGE_JS_063_LEVEL_MARKER_E = 5;

  class LogAggregator {
      static GARBAGE_JS_063_AGGREGATOR_CLASS_F = true;

      append(level: string, message: string): void {
          // noop
      }
  }
