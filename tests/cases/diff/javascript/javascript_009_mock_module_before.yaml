api.ts: |
  export async function fetchUser(id: string) {
      const response = await fetch(`/api/users/${id}`);
      return response.json();
  }
service.ts: |
  import { fetchUser } from './api';

  export async function getUserName(id: string): Promise<string> {
      const user = await fetchUser(id);
      return user.name;
  }
service.test.ts: |
  console.log('initial');
garbage_monitoring.ts: |
  const GARBAGE_JS_009_ALERT_MARKER_A = "monitoring";
  const GARBAGE_JS_009_DISPATCH_MARKER_B = true;

  class AlertDispatcher {
      static GARBAGE_JS_009_DISPATCHER_CLASS_C = "dispatcher";

      fire(alert: string): void {
          // noop
      }
  }
garbage_profiling.ts: |
  const GARBAGE_JS_009_TRACE_MARKER_D = "profiling";
  const GARBAGE_JS_009_SPAN_MARKER_E = 42;

  class TraceProfiler {
      static GARBAGE_JS_009_PROFILER_CLASS_F = true;

      begin(name: string): void {
          // noop
      }
  }
