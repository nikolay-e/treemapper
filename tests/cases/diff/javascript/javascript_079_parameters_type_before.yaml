api.ts: |
  export function fetchData(url: string, options: { method: string; headers: Record<string, string> }) {
      return fetch(url, options);
  }
wrapper.ts: |
  console.log('initial');
garbage_auditing.ts: |
  const GARBAGE_JS_079_AUDIT_MARKER_A = "auditing";
  const GARBAGE_JS_079_TRAIL_MARKER_B = true;

  class AuditLogger {
      static GARBAGE_JS_079_LOGGER_CLASS_C = "logger";

      log(action: string, userId: string): void {
          // noop
      }
  }
garbage_compression.ts: |
  const GARBAGE_JS_079_COMPRESS_MARKER_D = "compression";
  const GARBAGE_JS_079_RATIO_MARKER_E = 9;

  class DataCompressor {
      static GARBAGE_JS_079_COMPRESSOR_CLASS_F = true;

      compress(data: string): string {
          return data;
      }
  }
