types.ts: |
  export interface User {
      id: string;
      name: string;
      email: string;
  }

  export type UserRole = 'admin' | 'user' | 'guest';
service.ts: |
  console.log('initial');
garbage_feature_flags.ts: |
  const GARBAGE_JS_016_FLAG_MARKER_A = "flags";
  const GARBAGE_JS_016_TOGGLE_MARKER_B = true;

  class FeatureToggle {
      static GARBAGE_JS_016_TOGGLE_CLASS_C = "toggle";

      isEnabled(flag: string): boolean {
          return false;
      }
  }
garbage_auditing.ts: |
  const GARBAGE_JS_016_AUDIT_MARKER_D = "audit";
  const GARBAGE_JS_016_TRAIL_MARKER_E = 42;

  class AuditTrail {
      static GARBAGE_JS_016_TRAIL_CLASS_F = true;

      log(action: string, userId: string): void {
          // noop
      }
  }
