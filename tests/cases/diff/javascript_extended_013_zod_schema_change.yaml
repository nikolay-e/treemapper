name: javascript_extended_013_zod_schema_change
repo:
  initial_files:
    schemas.ts: |
      import { z } from 'zod';

      export const UserSchema = z.object({
          id: z.string(),
          name: z.string(),
          email: z.string().email(),
      });

      export type User = z.infer<typeof UserSchema>;
    validator.ts: |
      import { UserSchema, User } from './schemas';

      export function validateUser(data: unknown): User {
          return UserSchema.parse(data);
      }
    src/schemas/productSchema.ts: |
      import { z } from 'zod';

      export const ProductSchema = z.object({
          id: z.string().uuid(),
          name: z.string().min(1).max(200),
          price: z.number().positive(),
          category: z.enum(['electronics', 'clothing', 'food']),
      });

      export type Product = z.infer<typeof ProductSchema>;

      export function validateProduct(data: unknown): Product {
          return ProductSchema.parse(data);
      }
    src/services/userRepository.ts: |
      import { User } from '../schemas';

      const store: User[] = [];

      export function saveUser(user: User): void {
          store.push(user);
      }

      export function findUserById(id: string): User | undefined {
          return store.find(u => u.id === id);
      }

      export function findUsersByRole(role: string): User[] {
          return store.filter(u => (u as any).role === role);
      }
    src/controllers/userController.ts: |
      import { Request, Response } from 'express';
      import { validateUser } from '../validator';

      export function createUserHandler(req: Request, res: Response): void {
          try {
              const user = validateUser(req.body);
              res.status(201).json(user);
          } catch (err) {
              res.status(400).json({ error: 'Validation failed' });
          }
      }
  changed_files:
    schemas.ts: |
      import { z } from 'zod';

      export const UserSchema = z.object({
          id: z.string().uuid(),
          name: z.string().min(1).max(100),
          email: z.string().email(),
          age: z.number().int().positive().optional(),
          role: z.enum(['admin', 'user', 'guest']),
      });

      export type User = z.infer<typeof UserSchema>;
    validator.ts: |
      import { UserSchema, User } from './schemas';

      export function validateUser(data: unknown): User {
          return UserSchema.parse(data);
      }
    src/schemas/productSchema.ts: |
      import { z } from 'zod';

      export const ProductSchema = z.object({
          id: z.string().uuid(),
          name: z.string().min(1).max(200),
          price: z.number().positive(),
          category: z.enum(['electronics', 'clothing', 'food']),
      });

      export type Product = z.infer<typeof ProductSchema>;

      export function validateProduct(data: unknown): Product {
          return ProductSchema.parse(data);
      }
    src/services/userRepository.ts: |
      import { User } from '../schemas';

      const store: User[] = [];

      export function saveUser(user: User): void {
          store.push(user);
      }

      export function findUserById(id: string): User | undefined {
          return store.find(u => u.id === id);
      }

      export function findUsersByRole(role: string): User[] {
          return store.filter(u => (u as any).role === role);
      }
    src/controllers/userController.ts: |
      import { Request, Response } from 'express';
      import { validateUser } from '../validator';

      export function createUserHandler(req: Request, res: Response): void {
          try {
              const user = validateUser(req.body);
              res.status(201).json(user);
          } catch (err) {
              res.status(400).json({ error: 'Validation failed' });
          }
      }
  commit_message: Update files
fragments:
- id: schema_definition
  selector:
    path: schemas.ts
    anchor: UserSchema = z.object({
- id: uuid_constraint
  selector:
    path: schemas.ts
    anchor: z.string().uuid()
- id: role_enum
  selector:
    path: schemas.ts
    anchor: z.enum(['admin', 'user', 'guest'])
- id: user_type
  selector:
    path: schemas.ts
    anchor: type User = z.infer<typeof UserSchema>
- id: validator_consumer
  selector:
    path: validator.ts
    anchor: UserSchema.parse(data)
- id: user_repository_consumer
  selector:
    path: src/services/userRepository.ts
    anchor: "import { User } from '../schemas'"
- id: no_product_schema
  selector:
    path: src/schemas/productSchema.ts
    anchor: ProductSchema = z.object({
- id: no_validate_product
  selector:
    path: src/schemas/productSchema.ts
    anchor: validateProduct
- id: no_user_controller
  selector:
    path: src/controllers/userController.ts
    anchor: createUserHandler
oracle:
  required:
  - schema_definition
  - uuid_constraint
  - role_enum
  - user_type
  - validator_consumer
  - user_repository_consumer
  allowed: []
  forbidden:
  - no_product_schema
  - no_validate_product
  - no_user_controller
