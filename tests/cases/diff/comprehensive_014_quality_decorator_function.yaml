name: comprehensive_014_quality_decorator_function
repo:
  initial_files:
    src/decorators.py: |
      import functools
      import time

      def log_calls(func):
          @functools.wraps(func)
          def wrapper(*args, **kwargs):
              print(f"Calling {func.__name__}")
              start = time.time()
              result = func(*args, **kwargs)
              elapsed = time.time() - start
              print(f"{func.__name__} took {elapsed:.3f}s")
              return result
          return wrapper

      def retry(max_attempts=3):
          def decorator(func):
              @functools.wraps(func)
              def wrapper(*args, **kwargs):
                  for attempt in range(max_attempts):
                      try:
                          return func(*args, **kwargs)
                      except Exception:
                          if attempt == max_attempts - 1:
                              raise
              return wrapper
          return decorator
    src/main.py: |
      def greet(name):
          pass

      def process_order(order_id):
          return f"Processing {order_id}"

      def send_notification(user, message):
          return f"Sent '{message}' to {user}"
    garbage_middleware_chain.py: |
      GARBAGE_COMP_014_PRIORITY_ORDER_A = "fifo"
      GARBAGE_COMP_014_ERROR_HANDLER_B = "fallthrough"

      class GarbageMiddlewareChain:
          GARBAGE_COMP_014_SHORT_CIRCUIT_C = True
          def execute(self):
              pass
    garbage_plugin_loader.py: |
      GARBAGE_COMP_014_DISCOVERY_PATH_D = "entrypoints"
      GARBAGE_COMP_014_LAZY_IMPORT_E = "deferred"

      class GarbagePluginLoader:
          GARBAGE_COMP_014_SANDBOX_MODE_F = False
          def load(self):
              pass
  changed_files:
    src/decorators.py: |
      import functools
      import time

      def log_calls(func):
          @functools.wraps(func)
          def wrapper(*args, **kwargs):
              print(f"Calling {func.__name__}")
              start = time.time()
              result = func(*args, **kwargs)
              elapsed = time.time() - start
              print(f"{func.__name__} took {elapsed:.3f}s")
              return result
          return wrapper

      def retry(max_attempts=3):
          def decorator(func):
              @functools.wraps(func)
              def wrapper(*args, **kwargs):
                  for attempt in range(max_attempts):
                      try:
                          return func(*args, **kwargs)
                      except Exception:
                          if attempt == max_attempts - 1:
                              raise
              return wrapper
          return decorator
    src/main.py: |
      from src.decorators import log_calls, retry

      @log_calls
      def greet(name):
          return f"Hello, {name}!"

      @retry(max_attempts=3)
      def process_order(order_id):
          return f"Processing {order_id}"

      def send_notification(user, message):
          return f"Sent '{message}' to {user}"
    garbage_middleware_chain.py: |
      GARBAGE_COMP_014_PRIORITY_ORDER_A = "fifo"
      GARBAGE_COMP_014_ERROR_HANDLER_B = "fallthrough"

      class GarbageMiddlewareChain:
          GARBAGE_COMP_014_SHORT_CIRCUIT_C = True
          def execute(self):
              pass
    garbage_plugin_loader.py: |
      GARBAGE_COMP_014_DISCOVERY_PATH_D = "entrypoints"
      GARBAGE_COMP_014_LAZY_IMPORT_E = "deferred"

      class GarbagePluginLoader:
          GARBAGE_COMP_014_SANDBOX_MODE_F = False
          def load(self):
              pass
  commit_message: Update files
fragments:
- id: main_file
  selector:
    path: src/main.py
- id: req_global_0
  selector:
    anchor: from src.decorators import log_calls, retry
- id: req_global_1
  selector:
    anchor: return f"Hello, {name}!"
- id: no_global_0
  selector:
    anchor: GARBAGE_COMP_014_DISCOVERY_PATH_D
- id: no_global_1
  selector:
    anchor: GARBAGE_COMP_014_ERROR_HANDLER_B
- id: no_global_2
  selector:
    anchor: GARBAGE_COMP_014_LAZY_IMPORT_E
- id: no_global_3
  selector:
    anchor: GARBAGE_COMP_014_PRIORITY_ORDER_A
- id: no_global_4
  selector:
    anchor: GARBAGE_COMP_014_SANDBOX_MODE_F
- id: no_global_5
  selector:
    anchor: GARBAGE_COMP_014_SHORT_CIRCUIT_C
- id: no_global_6
  selector:
    anchor: garbage_middleware_chain.py
- id: no_global_7
  selector:
    anchor: garbage_plugin_loader.py
oracle:
  required:
  - main_file
  - req_global_0
  - req_global_1
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
