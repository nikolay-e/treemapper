name: cicd_and_docs_068_sql_stored_procedure
tags:
- lang:python
- lang:sql
- change:single_file
- context:cross_module
- distance:2hop
- difficulty:medium
repo:
  initial_files:
    sql/procedures/calculate_order_total.sql: |
      CREATE OR REPLACE FUNCTION calculate_order_total(order_id INTEGER)
      RETURNS DECIMAL AS $$
      DECLARE
          total DECIMAL;
      BEGIN
          SELECT SUM(quantity * price) INTO total
          FROM order_items
          WHERE order_items.order_id = calculate_order_total.order_id;

          RETURN COALESCE(total, 0);
      END;
      $$ LANGUAGE plpgsql;
    sql/procedures/calculate_shipping_cost.sql: |
      CREATE OR REPLACE FUNCTION calculate_shipping_cost(weight DECIMAL)
      RETURNS DECIMAL AS $$
      BEGIN
          IF weight <= 1.0 THEN
              RETURN 5.99;
          ELSIF weight <= 5.0 THEN
              RETURN 9.99;
          ELSE
              RETURN 14.99;
          END IF;
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    src/services/order_service.py: |
      from decimal import Decimal
      from typing import Optional

      class OrderService:
          def __init__(self, db):
              self.db = db

          def get_order_total(self, order_id: int) -> Decimal:
              result = self.db.execute(
                  "SELECT calculate_order_total(%s)",
                  [order_id]
              )
              return Decimal(str(result[0][0]))

          def update_order_total(self, order_id: int) -> None:
              total = self.get_order_total(order_id)
              self.db.execute(
                  "UPDATE orders SET total = %s WHERE id = %s",
                  [total, order_id]
              )
    src/services/shipping_service.py: |
      from decimal import Decimal

      class ShippingService:
          def __init__(self, db):
              self.db = db

          def get_shipping_cost(self, weight: Decimal) -> Decimal:
              result = self.db.execute(
                  "SELECT calculate_shipping_cost(%s)",
                  [weight]
              )
              return Decimal(str(result[0][0]))
  changed_files:
    sql/procedures/calculate_order_total.sql: |
      CREATE OR REPLACE FUNCTION calculate_order_total(
          p_order_id INTEGER,
          p_include_tax BOOLEAN DEFAULT FALSE,
          p_include_shipping BOOLEAN DEFAULT FALSE
      )
      RETURNS DECIMAL AS $$
      DECLARE
          v_subtotal DECIMAL;
          v_tax_rate DECIMAL := 0.10;
          v_shipping_cost DECIMAL := 9.99;
          v_total DECIMAL;
      BEGIN
          -- Calculate subtotal from order items
          SELECT SUM(quantity * unit_price) INTO v_subtotal
          FROM order_items
          WHERE order_id = p_order_id;

          v_subtotal := COALESCE(v_subtotal, 0);
          v_total := v_subtotal;

          -- Add tax if requested
          IF p_include_tax THEN
              v_total := v_total * (1 + v_tax_rate);
          END IF;

          -- Add shipping if requested
          IF p_include_shipping AND v_subtotal > 0 THEN
              v_total := v_total + v_shipping_cost;
          END IF;

          RETURN ROUND(v_total, 2);
      END;
      $$ LANGUAGE plpgsql STABLE;

      -- Helper function to get order with tax
      CREATE OR REPLACE FUNCTION get_order_total_with_tax(p_order_id INTEGER)
      RETURNS DECIMAL AS $$
      BEGIN
          RETURN calculate_order_total(p_order_id, TRUE, FALSE);
      END;
      $$ LANGUAGE plpgsql STABLE;
    sql/procedures/calculate_shipping_cost.sql: |
      CREATE OR REPLACE FUNCTION calculate_shipping_cost(weight DECIMAL)
      RETURNS DECIMAL AS $$
      BEGIN
          IF weight <= 1.0 THEN
              RETURN 5.99;
          ELSIF weight <= 5.0 THEN
              RETURN 9.99;
          ELSE
              RETURN 14.99;
          END IF;
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    src/services/order_service.py: |
      from decimal import Decimal
      from typing import Optional

      class OrderService:
          def __init__(self, db):
              self.db = db

          def get_order_total(self, order_id: int) -> Decimal:
              result = self.db.execute(
                  "SELECT calculate_order_total(%s)",
                  [order_id]
              )
              return Decimal(str(result[0][0]))

          def update_order_total(self, order_id: int) -> None:
              total = self.get_order_total(order_id)
              self.db.execute(
                  "UPDATE orders SET total = %s WHERE id = %s",
                  [total, order_id]
              )
    src/services/shipping_service.py: |
      from decimal import Decimal

      class ShippingService:
          def __init__(self, db):
              self.db = db

          def get_shipping_cost(self, weight: Decimal) -> Decimal:
              result = self.db.execute(
                  "SELECT calculate_shipping_cost(%s)",
                  [weight]
              )
              return Decimal(str(result[0][0]))
  commit_message: "db: extend calculate_order_total with tax and shipping parameters"
fragments:
- id: procedure_changed
  selector:
    path: sql/procedures/calculate_order_total.sql
- id: new_signature
  selector:
    anchor: p_include_tax BOOLEAN DEFAULT FALSE
- id: calling_service
  selector:
    path: src/services/order_service.py
    anchor: SELECT calculate_order_total(%s)
- id: no_shipping_procedure
  selector:
    path: sql/procedures/calculate_shipping_cost.sql
    anchor: calculate_shipping_cost
- id: no_shipping_service
  selector:
    path: src/services/shipping_service.py
    anchor: ShippingService
oracle:
  required:
  - procedure_changed
  - new_signature
  - calling_service
  allowed: []
  forbidden:
  - no_shipping_procedure
  - no_shipping_service
