# Scenario: Config class starts using DSL module for metaprogrammed accessors and validators
# Changed: lib/config.rb — added require_relative, include DSL, define_accessors/validators, initializer, valid? method
# Reviewer questions: Does define_method correctly generate getters/setters? Are validators called with correct method names?
# Trap files:
#   - lib/serializer.rb: Attribute serialization via method_missing — "dynamic methods" pattern overlap but no DSL import
#   - lib/schema_builder.rb: Schema definition DSL — "define_method/class_eval" vocabulary overlap but independent module
max_enrichment: 9.1
initial:
  lib/dsl.rb: |
    module DSL
      def self.included(base)
        base.extend(ClassMethods)
      end

      module ClassMethods
        def define_accessors(*names)
          names.each do |name|
            define_method(name) do
              instance_variable_get("@#{name}")
            end

            define_method("#{name}=") do |value|
              instance_variable_set("@#{name}", value)
            end
          end
        end

        def define_validators(*names)
          names.each do |name|
            define_method("validate_#{name}") do
              !send(name).nil?
            end
          end
        end
      end
    end
  lib/config.rb: |
    class Config
      def initialize
        @data = {}
      end
    end
  lib/serializer.rb: |
    class AttributeSerializer
      def initialize(object)
        @object = object
        @serialized = {}
      end

      def method_missing(name, *args)
        if name.to_s.start_with?('serialize_')
          attr_name = name.to_s.sub('serialize_', '')
          @serialized[attr_name] = @object.send(attr_name)
        else
          super
        end
      end

      def respond_to_missing?(name, include_private = false)
        name.to_s.start_with?('serialize_') || super
      end

      def to_hash
        @serialized
      end

      def to_json
        @serialized.to_s
      end
    end
  lib/schema_builder.rb: |
    class SchemaBuilder
      def initialize(&block)
        @fields = []
        instance_eval(&block) if block_given?
      end

      def string(name, required: false)
        @fields << { name: name, type: :string, required: required }
      end

      def integer(name, min: nil, max: nil)
        @fields << { name: name, type: :integer, min: min, max: max }
      end

      def boolean(name, default: false)
        @fields << { name: name, type: :boolean, default: default }
      end

      def validate(data)
        @fields.all? do |field|
          value = data[field[:name]]
          next false if field[:required] && value.nil?
          next true if value.nil?
          validate_type(value, field[:type])
        end
      end

      private

      def validate_type(value, type)
        case type
        when :string then value.is_a?(String)
        when :integer then value.is_a?(Integer)
        when :boolean then [true, false].include?(value)
        end
      end
    end
changed:
  lib/dsl.rb: |
    module DSL
      def self.included(base)
        base.extend(ClassMethods)
      end

      module ClassMethods
        def define_accessors(*names)
          names.each do |name|
            define_method(name) do
              instance_variable_get("@#{name}")
            end

            define_method("#{name}=") do |value|
              instance_variable_set("@#{name}", value)
            end
          end
        end

        def define_validators(*names)
          names.each do |name|
            define_method("validate_#{name}") do
              !send(name).nil?
            end
          end
        end
      end
    end
  lib/config.rb: |
    require_relative 'dsl'

    class Config
      include DSL

      define_accessors :host, :port, :debug, :timeout
      define_validators :host, :port

      def initialize(host: 'localhost', port: 3000, debug: false, timeout: 30)
        @host = host
        @port = port
        @debug = debug
        @timeout = timeout
      end

      def valid?
        validate_host && validate_port
      end
    end
  lib/serializer.rb: |
    class AttributeSerializer
      def initialize(object)
        @object = object
        @serialized = {}
      end

      def method_missing(name, *args)
        if name.to_s.start_with?('serialize_')
          attr_name = name.to_s.sub('serialize_', '')
          @serialized[attr_name] = @object.send(attr_name)
        else
          super
        end
      end

      def respond_to_missing?(name, include_private = false)
        name.to_s.start_with?('serialize_') || super
      end

      def to_hash
        @serialized
      end

      def to_json
        @serialized.to_s
      end
    end
  lib/schema_builder.rb: |
    class SchemaBuilder
      def initialize(&block)
        @fields = []
        instance_eval(&block) if block_given?
      end

      def string(name, required: false)
        @fields << { name: name, type: :string, required: required }
      end

      def integer(name, min: nil, max: nil)
        @fields << { name: name, type: :integer, min: min, max: max }
      end

      def boolean(name, default: false)
        @fields << { name: name, type: :boolean, default: default }
      end

      def validate(data)
        @fields.all? do |field|
          value = data[field[:name]]
          next false if field[:required] && value.nil?
          next true if value.nil?
          validate_type(value, field[:type])
        end
      end

      private

      def validate_type(value, type)
        case type
        when :string then value.is_a?(String)
        when :integer then value.is_a?(Integer)
        when :boolean then [true, false].include?(value)
        end
      end
    end
must_include_files:
- lib/config.rb
must_include_content:
- |
  define_accessors :host, :port, :debug, :timeout
must_not_include:
- AttributeSerializer
- SchemaBuilder
- method_missing
- validate_type
- serializer.rb
- schema_builder.rb
must_not_include_files:
- lib/serializer.rb
- lib/schema_builder.rb
max_files: 4
add_garbage_files: false
skip_garbage_check: true
