# Scenario: User model starts using Callbacks module for before_save/after_save lifecycle hooks
# Changed: app/models/user.rb — added require_relative, include Callbacks, before_save/after_save declarations, save with callbacks
# Reviewer questions: Does run_before_callbacks execute in correct order? Does after_save fire only on successful persist?
# Trap files:
#   - lib/event_bus.rb: Pub/sub event system — "callbacks/hooks" vocabulary overlap but no Callbacks module import
#   - lib/validator.rb: Validation pipeline — "before_save" domain overlap but independent validation chain
initial:
  lib/callbacks.rb: |
    module Callbacks
      def self.included(base)
        base.extend(ClassMethods)
      end

      module ClassMethods
        def before_save(*methods)
          @before_save_callbacks ||= []
          @before_save_callbacks.concat(methods)
        end

        def after_save(*methods)
          @after_save_callbacks ||= []
          @after_save_callbacks.concat(methods)
        end

        def before_save_callbacks
          @before_save_callbacks || []
        end

        def after_save_callbacks
          @after_save_callbacks || []
        end
      end

      def run_before_callbacks
        self.class.before_save_callbacks.each { |m| send(m) }
      end

      def run_after_callbacks
        self.class.after_save_callbacks.each { |m| send(m) }
      end
    end
  app/models/user.rb: |
    class User
      def save
        true
      end
    end
  lib/event_bus.rb: |
    class EventBus
      def initialize
        @subscribers = Hash.new { |h, k| h[k] = [] }
      end

      def subscribe(event, &handler)
        @subscribers[event] << handler
      end

      def publish(event, data = {})
        @subscribers[event].each { |handler| handler.call(data) }
      end

      def unsubscribe(event)
        @subscribers.delete(event)
      end

      def subscriber_count(event)
        @subscribers[event].size
      end

      def clear_all
        @subscribers.clear
      end
    end
  lib/validator.rb: |
    class ValidationPipeline
      def initialize
        @rules = []
      end

      def add_rule(field, &block)
        @rules << { field: field, check: block }
      end

      def validate(record)
        errors = []
        @rules.each do |rule|
          value = record.send(rule[:field])
          unless rule[:check].call(value)
            errors << "#{rule[:field]} is invalid"
          end
        end
        errors
      end

      def valid?(record)
        validate(record).empty?
      end

      def rule_count
        @rules.size
      end
    end
changed:
  lib/callbacks.rb: |
    module Callbacks
      def self.included(base)
        base.extend(ClassMethods)
      end

      module ClassMethods
        def before_save(*methods)
          @before_save_callbacks ||= []
          @before_save_callbacks.concat(methods)
        end

        def after_save(*methods)
          @after_save_callbacks ||= []
          @after_save_callbacks.concat(methods)
        end

        def before_save_callbacks
          @before_save_callbacks || []
        end

        def after_save_callbacks
          @after_save_callbacks || []
        end
      end

      def run_before_callbacks
        self.class.before_save_callbacks.each { |m| send(m) }
      end

      def run_after_callbacks
        self.class.after_save_callbacks.each { |m| send(m) }
      end
    end
  app/models/user.rb: |
    require_relative '../../lib/callbacks'

    class User
      include Callbacks

      attr_accessor :email, :name, :updated_at

      before_save :normalize_email, :set_updated_at
      after_save :send_notification

      def initialize(email:, name:)
        @email = email
        @name = name
      end

      def save
        run_before_callbacks
        result = persist
        run_after_callbacks if result
        result
      end

      private

      def normalize_email
        @email = @email.downcase.strip if @email
      end

      def set_updated_at
        @updated_at = Time.now
      end

      def send_notification
        true
      end

      def persist
        true
      end
    end
  lib/event_bus.rb: |
    class EventBus
      def initialize
        @subscribers = Hash.new { |h, k| h[k] = [] }
      end

      def subscribe(event, &handler)
        @subscribers[event] << handler
      end

      def publish(event, data = {})
        @subscribers[event].each { |handler| handler.call(data) }
      end

      def unsubscribe(event)
        @subscribers.delete(event)
      end

      def subscriber_count(event)
        @subscribers[event].size
      end

      def clear_all
        @subscribers.clear
      end
    end
  lib/validator.rb: |
    class ValidationPipeline
      def initialize
        @rules = []
      end

      def add_rule(field, &block)
        @rules << { field: field, check: block }
      end

      def validate(record)
        errors = []
        @rules.each do |rule|
          value = record.send(rule[:field])
          unless rule[:check].call(value)
            errors << "#{rule[:field]} is invalid"
          end
        end
        errors
      end

      def valid?(record)
        validate(record).empty?
      end

      def rule_count
        @rules.size
      end
    end
must_include_files:
- app/models/user.rb
must_include_content:
- |
  before_save :normalize_email, :set_updated_at
must_not_include:
- EventBus
- ValidationPipeline
- subscriber_count
- rule_count
- event_bus.rb
- validator.rb
must_not_include_files:
- lib/event_bus.rb
- lib/validator.rb
max_files: 4
add_garbage_files: false
skip_garbage_check: true
