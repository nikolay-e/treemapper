# Scenario: Adding RSpec tests for User model's valid? and adult? methods
# Changed: spec/user_spec.rb — added require_relative, RSpec.describe with nested describe/it blocks testing validation and age
# Reviewer questions: Does the spec cover both truthy and falsy paths? Are edge cases (nil age, boundary 18) tested?
# Trap files:
#   - spec/support/factory.rb: Test factory helper — "spec/test setup" domain overlap but no User assertions
#   - lib/age_calculator.rb: Age calculation utility — "age/adult" vocabulary overlap but no User dependency
max_enrichment: 6.2
initial:
  lib/user.rb: |
    class User
      attr_accessor :email, :name, :age

      def initialize(email:, name: nil, age: nil)
        @email = email
        @name = name
        @age = age
      end

      def valid?
        valid_email? && valid_age?
      end

      def adult?
        @age && @age >= 18
      end

      private

      def valid_email?
        @email =~ /\A[^@\s]+@[^@\s]+\z/
      end

      def valid_age?
        @age.nil? || (@age.is_a?(Integer) && @age > 0 && @age < 150)
      end
    end
  spec/user_spec.rb: |
    describe 'placeholder' do
    end
  spec/support/factory.rb: |
    module TestFactory
      def self.build(type, overrides = {})
        defaults = default_attributes(type)
        attrs = defaults.merge(overrides)
        Object.const_get(type.to_s.capitalize).new(**attrs)
      end

      def self.default_attributes(type)
        case type
        when :user
          { email: 'default@example.com', name: 'Test User', age: 25 }
        when :product
          { name: 'Widget', price: 9.99, active: true }
        when :order
          { total: 0, status: :pending }
        else
          {}
        end
      end

      def self.build_list(type, count, overrides = {})
        count.times.map { |i| build(type, overrides.merge(name: "#{type}_#{i}")) }
      end
    end
  lib/age_calculator.rb: |
    class AgeCalculator
      def initialize(birth_date)
        @birth_date = birth_date
      end

      def age_at(date = Time.now)
        years = date.year - @birth_date.year
        if date.month < @birth_date.month ||
           (date.month == @birth_date.month && date.day < @birth_date.day)
          years -= 1
        end
        years
      end

      def minor?(date = Time.now)
        age_at(date) < 18
      end

      def senior?(date = Time.now)
        age_at(date) >= 65
      end

      def age_group(date = Time.now)
        age = age_at(date)
        case age
        when 0..12 then :child
        when 13..17 then :teenager
        when 18..64 then :adult
        else :senior
        end
      end
    end
changed:
  lib/user.rb: |
    class User
      attr_accessor :email, :name, :age

      def initialize(email:, name: nil, age: nil)
        @email = email
        @name = name
        @age = age
      end

      def valid?
        valid_email? && valid_age?
      end

      def adult?
        @age && @age >= 18
      end

      private

      def valid_email?
        @email =~ /\A[^@\s]+@[^@\s]+\z/
      end

      def valid_age?
        @age.nil? || (@age.is_a?(Integer) && @age > 0 && @age < 150)
      end
    end
  spec/user_spec.rb: |
    require_relative '../lib/user'

    RSpec.describe User do
      describe '#valid?' do
        it 'returns truthy for valid email' do
          user = User.new(email: 'test@example.com')
          expect(user.valid?).to be_truthy
        end

        it 'returns falsy for invalid email' do
          user = User.new(email: 'invalid')
          expect(user.valid?).to be_falsy
        end

        it 'returns falsy for negative age' do
          user = User.new(email: 'test@example.com', age: -1)
          expect(user.valid?).to be_falsy
        end
      end

      describe '#adult?' do
        it 'returns true for age >= 18' do
          user = User.new(email: 'a@b.com', age: 18)
          expect(user.adult?).to be true
        end

        it 'returns false for age < 18' do
          user = User.new(email: 'a@b.com', age: 17)
          expect(user.adult?).to be false
        end
      end
    end
  spec/support/factory.rb: |
    module TestFactory
      def self.build(type, overrides = {})
        defaults = default_attributes(type)
        attrs = defaults.merge(overrides)
        Object.const_get(type.to_s.capitalize).new(**attrs)
      end

      def self.default_attributes(type)
        case type
        when :user
          { email: 'default@example.com', name: 'Test User', age: 25 }
        when :product
          { name: 'Widget', price: 9.99, active: true }
        when :order
          { total: 0, status: :pending }
        else
          {}
        end
      end

      def self.build_list(type, count, overrides = {})
        count.times.map { |i| build(type, overrides.merge(name: "#{type}_#{i}")) }
      end
    end
  lib/age_calculator.rb: |
    class AgeCalculator
      def initialize(birth_date)
        @birth_date = birth_date
      end

      def age_at(date = Time.now)
        years = date.year - @birth_date.year
        if date.month < @birth_date.month ||
           (date.month == @birth_date.month && date.day < @birth_date.day)
          years -= 1
        end
        years
      end

      def minor?(date = Time.now)
        age_at(date) < 18
      end

      def senior?(date = Time.now)
        age_at(date) >= 65
      end

      def age_group(date = Time.now)
        age = age_at(date)
        case age
        when 0..12 then :child
        when 13..17 then :teenager
        when 18..64 then :adult
        else :senior
        end
      end
    end
must_include_files:
- spec/user_spec.rb
must_include_content:
- |
  user = User.new(email: 'test@example.com')
must_not_include:
- TestFactory
- AgeCalculator
- build_list
- age_group
- factory.rb
- age_calculator.rb
must_not_include_files:
- spec/support/factory.rb
- lib/age_calculator.rb
max_files: 4
add_garbage_files: false
skip_garbage_check: true
