# Scenario: User model gains association to Order model with order management methods
# Changed: app/models/user.rb — added require_relative, orders accessor, add_order, total_spent, pending_orders methods
# Reviewer questions: Does total_spent correctly filter by :completed status? Does add_order set user_id?
# Trap files:
#   - app/models/invoice.rb: Invoice generation — "order/total" vocabulary overlap but no User dependency
#   - app/models/shipping.rb: Shipping calculator — "order fulfillment" domain but independent model
max_enrichment: 8.6
initial:
  app/models/order.rb: |
    class Order
      attr_accessor :id, :user_id, :total, :status, :items

      def initialize(id: nil, user_id: nil, total: 0, status: :pending)
        @id = id
        @user_id = user_id
        @total = total
        @status = status
        @items = []
      end

      def add_item(name, price, quantity: 1)
        @items << { name: name, price: price, quantity: quantity }
        @total += price * quantity
      end

      def complete
        @status = :completed
      end
    end
  app/models/user.rb: |
    class User
      attr_accessor :id, :name
      def initialize(id: nil, name: nil)
        @id = id
        @name = name
      end
    end
  app/models/invoice.rb: |
    class Invoice
      attr_accessor :id, :order_id, :amount, :due_date, :paid

      def initialize(order_id:, amount:, due_date:)
        @id = rand(10000)
        @order_id = order_id
        @amount = amount
        @due_date = due_date
        @paid = false
      end

      def mark_paid
        @paid = true
      end

      def overdue?
        !@paid && @due_date < Time.now
      end

      def total_with_tax(rate: 0.1)
        @amount * (1 + rate)
      end

      def formatted_amount
        "$#{'%.2f' % @amount}"
      end
    end
  app/models/shipping.rb: |
    class ShippingCalculator
      RATES = {
        standard: 5.99,
        express: 12.99,
        overnight: 24.99
      }.freeze

      def initialize(weight:, destination:)
        @weight = weight
        @destination = destination
      end

      def calculate(method: :standard)
        base = RATES.fetch(method, RATES[:standard])
        base + weight_surcharge
      end

      def estimated_days(method: :standard)
        case method
        when :standard then 5..7
        when :express then 2..3
        when :overnight then 1..1
        end
      end

      private

      def weight_surcharge
        return 0 if @weight <= 1.0
        (@weight - 1.0).ceil * 1.50
      end
    end
changed:
  app/models/order.rb: |
    class Order
      attr_accessor :id, :user_id, :total, :status, :items

      def initialize(id: nil, user_id: nil, total: 0, status: :pending)
        @id = id
        @user_id = user_id
        @total = total
        @status = status
        @items = []
      end

      def add_item(name, price, quantity: 1)
        @items << { name: name, price: price, quantity: quantity }
        @total += price * quantity
      end

      def complete
        @status = :completed
      end
    end
  app/models/user.rb: |
    require_relative 'order'

    class User
      attr_accessor :id, :name, :orders

      def initialize(id: nil, name: nil)
        @id = id
        @name = name
        @orders = []
      end

      def add_order(order)
        order.user_id = @id
        @orders << order
      end

      def total_spent
        @orders.select { |o| o.status == :completed }.sum(&:total)
      end

      def pending_orders
        @orders.select { |o| o.status == :pending }
      end
    end
  app/models/invoice.rb: |
    class Invoice
      attr_accessor :id, :order_id, :amount, :due_date, :paid

      def initialize(order_id:, amount:, due_date:)
        @id = rand(10000)
        @order_id = order_id
        @amount = amount
        @due_date = due_date
        @paid = false
      end

      def mark_paid
        @paid = true
      end

      def overdue?
        !@paid && @due_date < Time.now
      end

      def total_with_tax(rate: 0.1)
        @amount * (1 + rate)
      end

      def formatted_amount
        "$#{'%.2f' % @amount}"
      end
    end
  app/models/shipping.rb: |
    class ShippingCalculator
      RATES = {
        standard: 5.99,
        express: 12.99,
        overnight: 24.99
      }.freeze

      def initialize(weight:, destination:)
        @weight = weight
        @destination = destination
      end

      def calculate(method: :standard)
        base = RATES.fetch(method, RATES[:standard])
        base + weight_surcharge
      end

      def estimated_days(method: :standard)
        case method
        when :standard then 5..7
        when :express then 2..3
        when :overnight then 1..1
        end
      end

      private

      def weight_surcharge
        return 0 if @weight <= 1.0
        (@weight - 1.0).ceil * 1.50
      end
    end
must_include_files:
- app/models/user.rb
must_include_content:
- |
  order.user_id = @id
must_not_include:
- Invoice
- ShippingCalculator
- mark_paid
- weight_surcharge
- invoice.rb
- shipping.rb
must_not_include_files:
- app/models/invoice.rb
- app/models/shipping.rb
max_files: 4
add_garbage_files: false
skip_garbage_check: true
