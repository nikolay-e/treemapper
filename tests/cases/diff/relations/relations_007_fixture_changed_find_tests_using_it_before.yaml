tests/conftest.py: |
  import pytest

  @pytest.fixture
  def mock_api():
      return {"status": "ok"}
tests/test_client.py: |
  def test_fetch(mock_api):
      assert mock_api["status"] == "ok"
tests/test_sync.py: |
  def test_sync(mock_api):
      assert mock_api["status"] == "ok"
garbage_profiler.py: |
  GARBAGE_REL_007_PROFILER_MARKER_A = "profiler"
  GARBAGE_REL_007_SAMPLE_MARKER_B = True

  class PerformanceProfiler:
      GARBAGE_REL_007_PERF_CLASS_C = "cpu"

      def start_trace(self, label: str) -> None:
          pass
garbage_healthcheck.py: |
  GARBAGE_REL_007_HEALTH_MARKER_D = "healthcheck"
  GARBAGE_REL_007_PING_MARKER_E = 200

  class HealthChecker:
      GARBAGE_REL_007_CHECKER_CLASS_F = True

      def check(self, endpoint: str) -> bool:
          return True
