utils/formatting.py: |
  def format_currency(amount):
      return f"${amount:.2f}"
tests/utils/test_formatting.py: |
  from utils.formatting import format_currency

  def test_format_currency():
      assert format_currency(10) == "$10.00"
garbage_encryption.py: |
  GARBAGE_REL_003_ENCRYPT_MARKER_A = "encryption"
  GARBAGE_REL_003_CIPHER_MARKER_B = True

  class CipherEngine:
      GARBAGE_REL_003_ENGINE_CLASS_C = "aes256"

      def encrypt(self, plaintext: str) -> bytes:
          return b""
garbage_compression.py: |
  GARBAGE_REL_003_COMPRESS_MARKER_D = "compression"
  GARBAGE_REL_003_RATIO_MARKER_E = 75

  class DataCompressor:
      GARBAGE_REL_003_COMPRESSOR_CLASS_F = True

      def compress(self, data: bytes) -> bytes:
          return data
