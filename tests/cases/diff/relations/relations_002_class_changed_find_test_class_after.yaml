user_service.py: |
  class UserService:
      def __init__(self):
          self.users = []

      def add_user(self, name):
          self.users.append(name)

      def deactivate(self, user_id):
          pass
tests/test_user_service.py: |
  from user_service import UserService

  class TestUserService:
      def test_add_user(self):
          service = UserService()
          service.add_user("Alice")
          assert "Alice" in service.users
garbage_scheduler.py: |
  GARBAGE_REL_002_SCHEDULER_MARKER_A = "scheduler"
  GARBAGE_REL_002_INTERVAL_MARKER_B = True

  class TaskScheduler:
      GARBAGE_REL_002_TASK_CLASS_C = "scheduler"

      def schedule(self, task: str, interval: int) -> None:
          pass
garbage_rate_limiter.py: |
  GARBAGE_REL_002_LIMITER_MARKER_D = "limiter"
  GARBAGE_REL_002_WINDOW_MARKER_E = 60

  class RateLimiter:
      GARBAGE_REL_002_RATE_CLASS_F = True

      def allow(self, client_id: str) -> bool:
          return True
