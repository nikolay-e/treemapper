# Scenario: conftest.py fixture mock_api adds "version" field — tests using
#   the fixture must be pulled so reviewer can verify compatibility.
#   Only tests/conftest.py is changed.
#
# Diff shows:
#   - return {"status": "ok"}
#   + return {"status": "ok", "version": "2.0"}
#
# Reviewer questions:
#   Q1: "Do existing tests destructure or iterate mock_api keys?"
#       -> tests/test_client.py accesses mock_api["status"] == "ok" — unaffected.
#          Reviewer must verify no test does `assert len(mock_api) == 1`.
#
#   Q2: "Does test_sync rely on the exact shape of mock_api?"
#       -> tests/test_sync.py also accesses mock_api["status"] — unaffected.
#
# Not needed for comprehension:
#   - tests/test_auth.py: Same test directory but doesn't use mock_api fixture.
#     Has its own local test data. No fixture dependency.
#
#   - tests/helpers/api_factory.py: Creates API test doubles with
#     create_mock_response. Shares "mock" and "api" vocabulary. But conftest
#     uses a plain dict, not the factory. No dependency.
#
#   - tests/test_cache.py: Another test module in same directory. Tests
#     caching behavior with a local cache_client fixture, not mock_api.
#
#   - tests/conftest_fixtures.py: Contains other shared fixtures like
#     db_session, temp_dir. Same conftest pattern but doesn't define
#     mock_api and isn't changed.

max_enrichment: 7.1
initial:
  tests/conftest.py: |
    import pytest

    @pytest.fixture
    def mock_api():
        return {"status": "ok"}

    @pytest.fixture
    def db_connection():
        return {"host": "localhost", "port": 5432, "database": "test_db"}
  tests/test_client.py: |
    def test_fetch(mock_api):
        assert mock_api["status"] == "ok"

    def test_api_response_format(mock_api):
        assert isinstance(mock_api, dict)
        assert "status" in mock_api
  tests/test_sync.py: |
    def test_sync(mock_api):
        assert mock_api["status"] == "ok"

    def test_sync_retry(mock_api):
        result = mock_api.get("status", "unknown")
        assert result == "ok"
  tests/test_auth.py: |
    import pytest

    def test_login_success():
        credentials = {"username": "admin", "password": "secret123"}
        assert credentials["username"] == "admin"

    def test_login_failure():
        credentials = {"username": "unknown", "password": "wrong"}
        assert credentials["username"] != "admin"

    def test_token_expiry():
        token = {"expires_in": 3600, "type": "bearer"}
        assert token["expires_in"] > 0
  tests/helpers/api_factory.py: |
    class ApiFactory:
        @staticmethod
        def create_mock_response(status_code=200, body=None):
            return {
                "status_code": status_code,
                "body": body or {},
                "headers": {"content-type": "application/json"},
            }

        @staticmethod
        def create_error_response(message="Internal Server Error"):
            return ApiFactory.create_mock_response(500, {"error": message})
  tests/test_cache.py: |
    import pytest

    @pytest.fixture
    def cache_client():
        return {"backend": "redis", "ttl": 300, "prefix": "test_"}

    def test_cache_hit(cache_client):
        assert cache_client["backend"] == "redis"

    def test_cache_ttl(cache_client):
        assert cache_client["ttl"] == 300

    def test_cache_prefix(cache_client):
        assert cache_client["prefix"].startswith("test")
  tests/conftest_fixtures.py: |
    import pytest
    import tempfile

    @pytest.fixture
    def temp_dir():
        with tempfile.TemporaryDirectory() as d:
            yield d

    @pytest.fixture
    def db_session():
        session = {"engine": "postgresql", "pool_size": 5}
        yield session

    @pytest.fixture
    def app_config():
        return {"debug": True, "testing": True, "secret_key": "test-secret"}
changed:
  tests/conftest.py: |
    import pytest

    @pytest.fixture
    def mock_api():
        return {"status": "ok", "version": "2.0"}

    @pytest.fixture
    def db_connection():
        return {"host": "localhost", "port": 5432, "database": "test_db"}
  tests/test_client.py: |
    def test_fetch(mock_api):
        assert mock_api["status"] == "ok"

    def test_api_response_format(mock_api):
        assert isinstance(mock_api, dict)
        assert "status" in mock_api
  tests/test_sync.py: |
    def test_sync(mock_api):
        assert mock_api["status"] == "ok"

    def test_sync_retry(mock_api):
        result = mock_api.get("status", "unknown")
        assert result == "ok"
  tests/test_auth.py: |
    import pytest

    def test_login_success():
        credentials = {"username": "admin", "password": "secret123"}
        assert credentials["username"] == "admin"

    def test_login_failure():
        credentials = {"username": "unknown", "password": "wrong"}
        assert credentials["username"] != "admin"

    def test_token_expiry():
        token = {"expires_in": 3600, "type": "bearer"}
        assert token["expires_in"] > 0
  tests/helpers/api_factory.py: |
    class ApiFactory:
        @staticmethod
        def create_mock_response(status_code=200, body=None):
            return {
                "status_code": status_code,
                "body": body or {},
                "headers": {"content-type": "application/json"},
            }

        @staticmethod
        def create_error_response(message="Internal Server Error"):
            return ApiFactory.create_mock_response(500, {"error": message})
  tests/test_cache.py: |
    import pytest

    @pytest.fixture
    def cache_client():
        return {"backend": "redis", "ttl": 300, "prefix": "test_"}

    def test_cache_hit(cache_client):
        assert cache_client["backend"] == "redis"

    def test_cache_ttl(cache_client):
        assert cache_client["ttl"] == 300

    def test_cache_prefix(cache_client):
        assert cache_client["prefix"].startswith("test")
  tests/conftest_fixtures.py: |
    import pytest
    import tempfile

    @pytest.fixture
    def temp_dir():
        with tempfile.TemporaryDirectory() as d:
            yield d

    @pytest.fixture
    def db_session():
        session = {"engine": "postgresql", "pool_size": 5}
        yield session

    @pytest.fixture
    def app_config():
        return {"debug": True, "testing": True, "secret_key": "test-secret"}

must_include_files:
  - tests/conftest.py

must_include_content_from:
  tests/conftest.py:
    - 'return {"status": "ok", "version": "2.0"}'

must_not_include:
  # --- tests/test_auth.py: doesn't use mock_api fixture ---
  - test_login_success
  - test_login_failure
  - test_token_expiry
  # --- tests/helpers/api_factory.py: creates mock responses, not mock_api ---
  - ApiFactory
  - create_mock_response
  - create_error_response
  # --- tests/test_cache.py: uses its own cache_client fixture ---
  - cache_client
  - test_cache_hit
  - test_cache_ttl
  # --- tests/conftest_fixtures.py: other fixtures, not mock_api ---
  - temp_dir
  - db_session
  - app_config

must_not_include_files:
  - tests/test_auth.py
  - tests/helpers/api_factory.py
  - tests/test_cache.py
  - tests/conftest_fixtures.py

max_fragments: 5
max_files: 4
add_garbage_files: false
skip_garbage_check: true
