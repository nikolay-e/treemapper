name: relations_011_fixture_changed_find_tests_using_it
initial:
  tests/conftest.py: |
    import pytest

    @pytest.fixture
    def mock_api():
        return {"status": "ok"}
  tests/test_client.py: |
    def test_fetch(mock_api):
        assert mock_api["status"] == "ok"
  tests/test_sync.py: |
    def test_sync(mock_api):
        assert mock_api["status"] == "ok"
changed:
  tests/conftest.py: |
    import pytest

    @pytest.fixture
    def mock_api():
        return {"status": "ok", "version": "2.0"}
assertions:
  must_include:
  - conftest.py
options:
  commit_message: Update mock_api fixture
