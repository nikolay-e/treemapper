name: javascript_001_input_decorator
tags:
- lang:typescript
- change:single_file
- context:import_chain
- distance:2hop
- difficulty:easy
repo:
  initial_files:
    user.model.ts: |
      export interface User {
          id: string;
          name: string;
          email: string;
      }
    user-card.component.ts: |
      console.log('initial');
    user-list.component.ts: |
      import { Component } from '@angular/core';

      @Component({
          selector: 'app-user-list',
          template: `<ul><li *ngFor="let u of users">{{ u.id }}</li></ul>`
      })
      export class UserListComponent {
          users: any[] = [];
      }
    user.service.ts: |
      import { Injectable } from '@angular/core';
      import { HttpClient } from '@angular/common/http';

      @Injectable({ providedIn: 'root' })
      export class UserService {
          constructor(private http: HttpClient) {}

          getUsers() {
              return this.http.get('/api/users');
          }
      }
  changed_files:
    user.model.ts: |
      export interface User {
          id: string;
          name: string;
          email: string;
      }
    user-card.component.ts: |
      import { Component, Input } from '@angular/core';
      import { User } from './user.model';

      @Component({
          selector: 'app-user-card',
          template: `
              <div class="card">
                  <h3>{{ user.name }}</h3>
                  <p>{{ user.email }}</p>
              </div>
          `
      })
      export class UserCardComponent {
          @Input() user!: User;
      }
    user-list.component.ts: |
      import { Component } from '@angular/core';

      @Component({
          selector: 'app-user-list',
          template: `<ul><li *ngFor="let u of users">{{ u.id }}</li></ul>`
      })
      export class UserListComponent {
          users: any[] = [];
      }
    user.service.ts: |
      import { Injectable } from '@angular/core';
      import { HttpClient } from '@angular/common/http';

      @Injectable({ providedIn: 'root' })
      export class UserService {
          constructor(private http: HttpClient) {}

          getUsers() {
              return this.http.get('/api/users');
          }
      }
  commit_message: Update files
fragments:
- id: user_model
  selector:
    path: user.model.ts
    anchor: interface User {
- id: user_model_1
  selector:
    path: user.model.ts
    anchor: 'name: string'
- id: user_model_2
  selector:
    path: user.model.ts
    anchor: 'email: string'
- id: no_user_list_component
  selector:
    path: user-list.component.ts
- id: no_user_service
  selector:
    path: user.service.ts
- id: no_global_0
  selector:
    anchor: UserListComponent
- id: no_global_1
  selector:
    anchor: UserService
- id: no_global_2
  selector:
    anchor: getUsers
oracle:
  required:
  - user_model
  - user_model_1
  - user_model_2
  allowed: []
  forbidden:
  - no_user_list_component
  - no_user_service
  - no_global_0
  - no_global_1
  - no_global_2
