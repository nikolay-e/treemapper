must_include_files:
  - pyproject.toml
  - src/cli.py
must_include_content:
  - |
    [project.scripts]
    myapp = "src.cli:main"
    myapp-server = "src.server:run"

    [project.optional-dependencies]
    dev = ["pytest", "ruff"]
  - |
    def main():
        cli(obj={})
must_not_include:
  - GARBAGE_CICD_015_PLUGIN_MARKER_A
  - GARBAGE_CICD_015_HOOK_MARKER_B
  - GARBAGE_CICD_015_ENABLED_MARKER_C
  - GARBAGE_CICD_015_CACHE_MARKER_D
  - GARBAGE_CICD_015_SIZE_MARKER_E
  - GARBAGE_CICD_015_TTL_MARKER_F
  - GarbagePluginManager
  - GarbageCacheBackend
  - garbage_plugin.py
  - garbage_cache.py
