name: go_009_defer_cleanup
repo:
  initial_files:
    internal/database/connection.go: |
      package database

      import (
          "database/sql"
          "log"
      )

      type Connection struct {
          db *sql.DB
      }

      func NewConnection(dsn string) (*Connection, error) {
          db, err := sql.Open("postgres", dsn)
          if err != nil {
              return nil, err
          }
          return &Connection{db: db}, nil
      }

      func (c *Connection) Close() error {
          log.Println("Closing database connection")
          return c.db.Close()
      }

      func (c *Connection) Query(query string) (*sql.Rows, error) {
          return c.db.Query(query)
      }
    internal/service/user_service.go: |
      package service

      import (
          "myproject/internal/database"
      )

      type UserService struct {
          conn *database.Connection
      }

      func NewUserService(conn *database.Connection) *UserService {
          return &UserService{conn: conn}
      }

      func (s *UserService) process() {
          defer s.conn.Close()
          rows, _ := s.conn.Query("SELECT * FROM users")
          defer rows.Close()
      }
    cmd/main.go: |
      package main

      import (
          "log"
          "myproject/internal/database"
          "myproject/internal/service"
      )

      func main() {
          conn, err := database.NewConnection("postgres://localhost/mydb")
          if err != nil {
              log.Fatal(err)
          }
          defer conn.Close()

          svc := service.NewUserService(conn)
          _ = svc
      }
    internal/cache/redis.go: |
      package cache

      import "time"

      type RedisCache struct {
          host string
          port int
          ttl  time.Duration
      }

      func NewRedisCache(host string, port int, ttl time.Duration) *RedisCache {
          return &RedisCache{host: host, port: port, ttl: ttl}
      }

      func (rc *RedisCache) Get(key string) (string, error) {
          return "", nil
      }

      func (rc *RedisCache) Set(key, value string) error {
          return nil
      }
    internal/queue/worker.go: |
      package queue

      import "context"

      type QueueWorker struct {
          concurrency int
          handler     func(ctx context.Context, msg []byte) error
      }

      func NewQueueWorker(concurrency int, handler func(ctx context.Context, msg []byte) error) *QueueWorker {
          return &QueueWorker{concurrency: concurrency, handler: handler}
      }

      func (w *QueueWorker) Start(ctx context.Context) error {
          return nil
      }
  changed_files:
    internal/database/connection.go: |
      package database

      import (
          "context"
          "database/sql"
          "log"
          "time"
      )

      type Connection struct {
          db     *sql.DB
          logger *log.Logger
      }

      func NewConnection(dsn string, logger *log.Logger) (*Connection, error) {
          db, err := sql.Open("postgres", dsn)
          if err != nil {
              return nil, err
          }
          db.SetMaxOpenConns(25)
          db.SetMaxIdleConns(5)
          db.SetConnMaxLifetime(5 * time.Minute)
          return &Connection{db: db, logger: logger}, nil
      }

      func (c *Connection) Close() error {
          c.logger.Println("Closing database connection")
          return c.db.Close()
      }

      func (c *Connection) Query(query string) (*sql.Rows, error) {
          return c.db.Query(query)
      }

      func (c *Connection) QueryContext(ctx context.Context, query string, args ...interface{}) (*sql.Rows, error) {
          return c.db.QueryContext(ctx, query, args...)
      }

      func (c *Connection) Transaction(ctx context.Context, fn func(*sql.Tx) error) error {
          tx, err := c.db.BeginTx(ctx, nil)
          if err != nil {
              return err
          }

          defer func() {
              if p := recover(); p != nil {
                  tx.Rollback()
                  c.logger.Printf("Transaction rolled back due to panic: %v", p)
                  panic(p)
              }
          }()

          if err := fn(tx); err != nil {
              if rbErr := tx.Rollback(); rbErr != nil {
                  c.logger.Printf("Rollback failed: %v", rbErr)
              }
              return err
          }

          return tx.Commit()
      }

      func (c *Connection) WithTimeout(ctx context.Context, timeout time.Duration, fn func(context.Context) error) error {
          ctx, cancel := context.WithTimeout(ctx, timeout)
          defer cancel()
          return fn(ctx)
      }

      func (c *Connection) FlushLogs() {
          c.logger.Println("Flushing database logs")
      }
    internal/service/user_service.go: |
      package service

      import (
          "myproject/internal/database"
      )

      type UserService struct {
          conn *database.Connection
      }

      func NewUserService(conn *database.Connection) *UserService {
          return &UserService{conn: conn}
      }

      func (s *UserService) process() {
          defer s.conn.Close()
          rows, _ := s.conn.Query("SELECT * FROM users")
          defer rows.Close()
      }
    cmd/main.go: |
      package main

      import (
          "log"
          "myproject/internal/database"
          "myproject/internal/service"
      )

      func main() {
          conn, err := database.NewConnection("postgres://localhost/mydb")
          if err != nil {
              log.Fatal(err)
          }
          defer conn.Close()

          svc := service.NewUserService(conn)
          _ = svc
      }
    internal/cache/redis.go: |
      package cache

      import "time"

      type RedisCache struct {
          host string
          port int
          ttl  time.Duration
      }

      func NewRedisCache(host string, port int, ttl time.Duration) *RedisCache {
          return &RedisCache{host: host, port: port, ttl: ttl}
      }

      func (rc *RedisCache) Get(key string) (string, error) {
          return "", nil
      }

      func (rc *RedisCache) Set(key, value string) error {
          return nil
      }
    internal/queue/worker.go: |
      package queue

      import "context"

      type QueueWorker struct {
          concurrency int
          handler     func(ctx context.Context, msg []byte) error
      }

      func NewQueueWorker(concurrency int, handler func(ctx context.Context, msg []byte) error) *QueueWorker {
          return &QueueWorker{concurrency: concurrency, handler: handler}
      }

      func (w *QueueWorker) Start(ctx context.Context) error {
          return nil
      }
  commit_message: "refactor(database): add logger param, connection pooling, transactions"
fragments:
- id: changed_connection
  selector:
    path: internal/database/connection.go
- id: new_connection_signature
  selector:
    anchor: func NewConnection(dsn string, logger *log.Logger) (*Connection, error) {
- id: new_transaction_method
  selector:
    anchor: func (c *Connection) Transaction(ctx context.Context, fn func(*sql.Tx) error) error {
- id: defer_rollback_on_panic
  selector:
    anchor: c.logger.Printf("Transaction rolled back due to panic
- id: caller_service_uses_connection
  selector:
    anchor: "conn *database.Connection"
- id: caller_service_defers_close
  selector:
    anchor: defer s.conn.Close()
- id: caller_main_creates_connection
  selector:
    anchor: conn, err := database.NewConnection("postgres://localhost/mydb")
- id: unrelated_redis_cache
  selector:
    anchor: func NewRedisCache(host string, port int, ttl time.Duration) *RedisCache {
- id: unrelated_queue_worker
  selector:
    anchor: func (w *QueueWorker) Start(ctx context.Context) error {
oracle:
  required:
  - changed_connection
  - new_connection_signature
  - new_transaction_method
  - defer_rollback_on_panic
  - caller_service_uses_connection
  - caller_service_defers_close
  - caller_main_creates_connection
  allowed: []
  forbidden:
  - unrelated_redis_cache
  - unrelated_queue_worker
