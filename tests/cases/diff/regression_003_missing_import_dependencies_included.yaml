name: regression_003_missing_import_dependencies_included
tags:
- lang:python
- change:single_file
- change:new_function
- change:signature
- context:import_chain
- distance:2hop
- difficulty:medium
repo:
  initial_files:
    src/analytics/service.py: |
      from typing import Dict, List, Optional
      from analytics.stats import pearson_correlation_with_pvalue, calculate_ema_value
      from analytics.series import resample_series, align_timestamps
      from analytics.config import load_trend_config, get_quality_window

      class AnalyticsService:
          def __init__(self, data_source, cache_ttl: int = 300):
              self.data_source = data_source
              self.cache_ttl = cache_ttl
              self._cache: Dict[str, List[float]] = {}

          def get_correlation_report(self, metric_a: str, metric_b: str) -> Dict:
              series_a = self.data_source.fetch_series(metric_a)
              series_b = self.data_source.fetch_series(metric_b)
              aligned_a, aligned_b = align_timestamps(series_a, series_b)
              correlation, pvalue = pearson_correlation_with_pvalue(aligned_a, aligned_b)
              return {
                  "metric_a": metric_a,
                  "metric_b": metric_b,
                  "correlation": round(correlation, 4),
                  "p_value": round(pvalue, 6),
                  "sample_size": len(aligned_a),
              }

          def get_smoothed_series(self, metric: str, window: int = 7) -> List[float]:
              raw = self.data_source.fetch_series(metric)
              resampled = resample_series(raw, interval_seconds=3600)
              return calculate_ema_value(resampled, span=window)
    src/analytics/stats.py: |
      from typing import List, Tuple
      import math

      def pearson_correlation_with_pvalue(x: List[float], y: List[float]) -> Tuple[float, float]:
          n = len(x)
          if n < 3:
              return 0.0, 1.0
          mean_x = sum(x) / n
          mean_y = sum(y) / n
          cov = sum((xi - mean_x) * (yi - mean_y) for xi, yi in zip(x, y))
          std_x = math.sqrt(sum((xi - mean_x) ** 2 for xi in x))
          std_y = math.sqrt(sum((yi - mean_y) ** 2 for yi in y))
          if std_x == 0 or std_y == 0:
              return 0.0, 1.0
          r = cov / (std_x * std_y)
          t_stat = r * math.sqrt((n - 2) / (1 - r ** 2 + 1e-12))
          p_value = 2.0 * (1.0 - min(abs(t_stat) / math.sqrt(n), 1.0))
          return r, max(0.0, p_value)

      def calculate_ema_value(values: List[float], span: int = 7) -> List[float]:
          if not values:
              return []
          alpha = 2.0 / (span + 1)
          result = [values[0]]
          for i in range(1, len(values)):
              ema = alpha * values[i] + (1 - alpha) * result[-1]
              result.append(round(ema, 6))
          return result

      def weighted_mean(values: List[float], weights: List[float]) -> float:
          if not values or len(values) != len(weights):
              return 0.0
          total_weight = sum(weights)
          if total_weight == 0:
              return 0.0
          return sum(v * w for v, w in zip(values, weights)) / total_weight
    src/analytics/series.py: |
      from typing import List, Tuple

      def resample_series(values: List[float], interval_seconds: int = 3600) -> List[float]:
          if len(values) < 2:
              return values[:]
          bucket_count = max(1, len(values) // max(1, interval_seconds // 60))
          bucket_size = len(values) / bucket_count
          resampled = []
          for i in range(bucket_count):
              start_idx = int(i * bucket_size)
              end_idx = int((i + 1) * bucket_size)
              chunk = values[start_idx:end_idx]
              if chunk:
                  resampled.append(sum(chunk) / len(chunk))
          return resampled

      def align_timestamps(
          series_a: List[float], series_b: List[float]
      ) -> Tuple[List[float], List[float]]:
          min_len = min(len(series_a), len(series_b))
          return series_a[:min_len], series_b[:min_len]

      def interpolate_gaps(values: List[float], gap_marker: float = -1.0) -> List[float]:
          result = values[:]
          for i in range(len(result)):
              if result[i] == gap_marker:
                  left = next((result[j] for j in range(i - 1, -1, -1) if result[j] != gap_marker), 0.0)
                  right = next((result[j] for j in range(i + 1, len(result)) if result[j] != gap_marker), left)
                  result[i] = (left + right) / 2.0
          return result
    src/analytics/config.py: |
      from typing import Dict, Optional

      def load_trend_config(mode: str) -> Dict[str, int]:
          configs = {
              "short": {"window": 7, "min_samples": 5},
              "medium": {"window": 30, "min_samples": 14},
              "long": {"window": 90, "min_samples": 30},
              "extended": {"window": 365, "min_samples": 60},
          }
          return configs.get(mode, configs["medium"])

      def get_quality_window(mode: str) -> int:
          config = load_trend_config(mode)
          return config["min_samples"]

      def get_aggregation_methods() -> list:
          return ["mean", "median", "sum", "max", "min"]
    src/models/user.py: |
      from dataclasses import dataclass, field
      from typing import List, Optional
      from datetime import datetime

      @dataclass
      class User:
          id: int
          username: str
          email: str
          created_at: datetime = field(default_factory=datetime.utcnow)
          is_active: bool = True

          def is_admin(self) -> bool:
              return self.username in ("admin", "superuser")

          def display_name(self) -> str:
              return f"{self.username} ({self.email})"

      @dataclass
      class UserPreferences:
          user_id: int
          theme: str = "default"
          locale: str = "en"
          notifications_enabled: bool = True
    src/models/session.py: |
      from dataclasses import dataclass
      from datetime import datetime, timedelta

      SESSION_DURATION_HOURS = 24
      SESSION_COOKIE_NAME = "sid"

      @dataclass
      class Session:
          id: str
          user_id: int
          token: str
          created_at: datetime
          expires_at: datetime
          is_revoked: bool = False

          def is_valid(self) -> bool:
              return datetime.utcnow() < self.expires_at and not self.is_revoked

          def remaining_seconds(self) -> float:
              delta = self.expires_at - datetime.utcnow()
              return max(0.0, delta.total_seconds())

      def create_session_token(user_id: int, duration_hours: int = 24) -> Session:
          now = datetime.utcnow()
          return Session(
              id=f"sess_{user_id}_{int(now.timestamp())}",
              user_id=user_id,
              token=f"token_{user_id}",
              created_at=now,
              expires_at=now + timedelta(hours=duration_hours),
          )
    tests/test_utils.py: |
      from datetime import datetime

      class TestDatabaseHelper:
          def __init__(self, db_name: str = "test_db", host: str = "localhost"):
              self.db_name = db_name
              self.host = host

          def setup_tables(self) -> None:
              pass

          def teardown_tables(self) -> None:
              pass

          def insert_fixture(self, table: str, data: dict) -> int:
              return 1

      def make_test_timestamp() -> datetime:
          return datetime(2025, 1, 15, 12, 0, 0)
  changed_files:
    src/analytics/service.py: |
      from typing import Dict, List, Optional
      from analytics.stats import pearson_correlation_with_pvalue, calculate_ema_value
      from analytics.series import resample_series, align_timestamps
      from analytics.config import load_trend_config, get_quality_window

      class AnalyticsService:
          def __init__(self, data_source, cache_ttl: int = 300):
              self.data_source = data_source
              self.cache_ttl = cache_ttl
              self._cache: Dict[str, List[float]] = {}

          def get_correlation_report(self, metric_a: str, metric_b: str) -> Dict:
              series_a = self.data_source.fetch_series(metric_a)
              series_b = self.data_source.fetch_series(metric_b)
              aligned_a, aligned_b = align_timestamps(series_a, series_b)
              correlation, pvalue = pearson_correlation_with_pvalue(aligned_a, aligned_b)
              return {
                  "metric_a": metric_a,
                  "metric_b": metric_b,
                  "correlation": round(correlation, 4),
                  "p_value": round(pvalue, 6),
                  "sample_size": len(aligned_a),
              }

          def get_smoothed_series(self, metric: str, window: int = 7) -> List[float]:
              raw = self.data_source.fetch_series(metric)
              resampled = resample_series(raw, interval_seconds=3600)
              return calculate_ema_value(resampled, span=window)

          def compute_advanced_score(
              self, metric: str, mode: str = "medium"
          ) -> Dict[str, float]:
              raw_series = self.data_source.fetch_series(metric)
              trend_config = load_trend_config(mode)
              window = trend_config["window"]
              quality_window = get_quality_window(mode)
              recent = raw_series[-window:] if len(raw_series) > window else raw_series
              resampled = resample_series(recent, interval_seconds=1800)
              smoothed = calculate_ema_value(resampled, span=quality_window)
              if len(smoothed) < quality_window:
                  return {"score": 0.0, "confidence": 0.0, "trend_direction": 0.0}
              baseline = smoothed[:quality_window]
              current = smoothed[-quality_window:]
              aligned_base, aligned_curr = align_timestamps(baseline, current)
              correlation, p_value = pearson_correlation_with_pvalue(
                  aligned_base, aligned_curr
              )
              trend_direction = (sum(current) / len(current)) - (
                  sum(baseline) / len(baseline)
              )
              confidence = 1.0 - p_value if p_value < 0.05 else 0.0
              score = correlation * confidence * (1.0 if trend_direction > 0 else -1.0)
              return {
                  "score": round(score, 4),
                  "confidence": round(confidence, 4),
                  "trend_direction": round(trend_direction, 4),
                  "correlation": round(correlation, 4),
                  "p_value": round(p_value, 6),
                  "window_size": window,
              }
    src/analytics/stats.py: |
      from typing import List, Tuple
      import math

      def pearson_correlation_with_pvalue(x: List[float], y: List[float]) -> Tuple[float, float]:
          n = len(x)
          if n < 3:
              return 0.0, 1.0
          mean_x = sum(x) / n
          mean_y = sum(y) / n
          cov = sum((xi - mean_x) * (yi - mean_y) for xi, yi in zip(x, y))
          std_x = math.sqrt(sum((xi - mean_x) ** 2 for xi in x))
          std_y = math.sqrt(sum((yi - mean_y) ** 2 for yi in y))
          if std_x == 0 or std_y == 0:
              return 0.0, 1.0
          r = cov / (std_x * std_y)
          t_stat = r * math.sqrt((n - 2) / (1 - r ** 2 + 1e-12))
          p_value = 2.0 * (1.0 - min(abs(t_stat) / math.sqrt(n), 1.0))
          return r, max(0.0, p_value)

      def calculate_ema_value(values: List[float], span: int = 7) -> List[float]:
          if not values:
              return []
          alpha = 2.0 / (span + 1)
          result = [values[0]]
          for i in range(1, len(values)):
              ema = alpha * values[i] + (1 - alpha) * result[-1]
              result.append(round(ema, 6))
          return result

      def weighted_mean(values: List[float], weights: List[float]) -> float:
          if not values or len(values) != len(weights):
              return 0.0
          total_weight = sum(weights)
          if total_weight == 0:
              return 0.0
          return sum(v * w for v, w in zip(values, weights)) / total_weight
    src/analytics/series.py: |
      from typing import List, Tuple

      def resample_series(values: List[float], interval_seconds: int = 3600) -> List[float]:
          if len(values) < 2:
              return values[:]
          bucket_count = max(1, len(values) // max(1, interval_seconds // 60))
          bucket_size = len(values) / bucket_count
          resampled = []
          for i in range(bucket_count):
              start_idx = int(i * bucket_size)
              end_idx = int((i + 1) * bucket_size)
              chunk = values[start_idx:end_idx]
              if chunk:
                  resampled.append(sum(chunk) / len(chunk))
          return resampled

      def align_timestamps(
          series_a: List[float], series_b: List[float]
      ) -> Tuple[List[float], List[float]]:
          min_len = min(len(series_a), len(series_b))
          return series_a[:min_len], series_b[:min_len]

      def interpolate_gaps(values: List[float], gap_marker: float = -1.0) -> List[float]:
          result = values[:]
          for i in range(len(result)):
              if result[i] == gap_marker:
                  left = next((result[j] for j in range(i - 1, -1, -1) if result[j] != gap_marker), 0.0)
                  right = next((result[j] for j in range(i + 1, len(result)) if result[j] != gap_marker), left)
                  result[i] = (left + right) / 2.0
          return result
    src/analytics/config.py: |
      from typing import Dict, Optional

      def load_trend_config(mode: str) -> Dict[str, int]:
          configs = {
              "short": {"window": 7, "min_samples": 5},
              "medium": {"window": 30, "min_samples": 14},
              "long": {"window": 90, "min_samples": 30},
              "extended": {"window": 365, "min_samples": 60},
          }
          return configs.get(mode, configs["medium"])

      def get_quality_window(mode: str) -> int:
          config = load_trend_config(mode)
          return config["min_samples"]

      def get_aggregation_methods() -> list:
          return ["mean", "median", "sum", "max", "min"]
    src/models/user.py: |
      from dataclasses import dataclass, field
      from typing import List, Optional
      from datetime import datetime

      @dataclass
      class User:
          id: int
          username: str
          email: str
          created_at: datetime = field(default_factory=datetime.utcnow)
          is_active: bool = True

          def is_admin(self) -> bool:
              return self.username in ("admin", "superuser")

          def display_name(self) -> str:
              return f"{self.username} ({self.email})"

      @dataclass
      class UserPreferences:
          user_id: int
          theme: str = "default"
          locale: str = "en"
          notifications_enabled: bool = True
    src/models/session.py: |
      from dataclasses import dataclass
      from datetime import datetime, timedelta

      SESSION_DURATION_HOURS = 24
      SESSION_COOKIE_NAME = "sid"

      @dataclass
      class Session:
          id: str
          user_id: int
          token: str
          created_at: datetime
          expires_at: datetime
          is_revoked: bool = False

          def is_valid(self) -> bool:
              return datetime.utcnow() < self.expires_at and not self.is_revoked

          def remaining_seconds(self) -> float:
              delta = self.expires_at - datetime.utcnow()
              return max(0.0, delta.total_seconds())

      def create_session_token(user_id: int, duration_hours: int = 24) -> Session:
          now = datetime.utcnow()
          return Session(
              id=f"sess_{user_id}_{int(now.timestamp())}",
              user_id=user_id,
              token=f"token_{user_id}",
              created_at=now,
              expires_at=now + timedelta(hours=duration_hours),
          )
    tests/test_utils.py: |
      from datetime import datetime

      class TestDatabaseHelper:
          def __init__(self, db_name: str = "test_db", host: str = "localhost"):
              self.db_name = db_name
              self.host = host

          def setup_tables(self) -> None:
              pass

          def teardown_tables(self) -> None:
              pass

          def insert_fixture(self, table: str, data: dict) -> int:
              return 1

      def make_test_timestamp() -> datetime:
          return datetime(2025, 1, 15, 12, 0, 0)
  commit_message: Update files
fragments:
- id: service
  selector:
    path: src/analytics/service.py
    anchor: def compute_advanced_score
- id: service_1
  selector:
    path: src/analytics/service.py
    anchor: pearson_correlation_with_pvalue
- id: service_2
  selector:
    path: src/analytics/service.py
    anchor: resample_series
- id: service_3
  selector:
    path: src/analytics/service.py
    anchor: load_trend_config
- id: stats
  selector:
    path: src/analytics/stats.py
    anchor: def pearson_correlation_with_pvalue
- id: stats_1
  selector:
    path: src/analytics/stats.py
    anchor: def calculate_ema_value
- id: series
  selector:
    path: src/analytics/series.py
    anchor: def resample_series
- id: series_1
  selector:
    path: src/analytics/series.py
    anchor: def align_timestamps
- id: config
  selector:
    path: src/analytics/config.py
    anchor: def load_trend_config
- id: config_1
  selector:
    path: src/analytics/config.py
    anchor: def get_quality_window
- id: no_user
  selector:
    path: src/models/user.py
- id: no_session
  selector:
    path: src/models/session.py
- id: no_test_utils
  selector:
    path: tests/test_utils.py
- id: no_global_0
  selector:
    anchor: is_admin
- id: no_global_1
  selector:
    anchor: display_name
- id: no_global_2
  selector:
    anchor: UserPreferences
- id: no_global_3
  selector:
    anchor: SESSION_DURATION_HOURS
- id: no_global_4
  selector:
    anchor: SESSION_COOKIE_NAME
- id: no_global_5
  selector:
    anchor: is_revoked
- id: no_global_6
  selector:
    anchor: create_session_token
- id: no_global_7
  selector:
    anchor: remaining_seconds
- id: no_global_8
  selector:
    anchor: TestDatabaseHelper
- id: no_global_9
  selector:
    anchor: setup_tables
- id: no_global_10
  selector:
    anchor: teardown_tables
- id: no_global_11
  selector:
    anchor: insert_fixture
- id: no_global_12
  selector:
    anchor: make_test_timestamp
oracle:
  required:
  - service
  - service_1
  - service_2
  - service_3
  - stats
  - stats_1
  - series
  - series_1
  - config
  - config_1
  allowed: []
  forbidden:
  - no_user
  - no_session
  - no_test_utils
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
  - no_global_8
  - no_global_9
  - no_global_10
  - no_global_11
  - no_global_12
