# Scenario: UserActor implements Actor with receive handler for the sealed
# UserMessage protocol, managing user state and sending responses.
#
# Diff: UserActor.scala adds Actor implementation with pattern matching on
# CreateUser, DeleteUser, GetUser messages.
#
# Reviewer questions:
#   Q1: "What messages does the actor handle?" -> Messages.scala (UNCHANGED)
#        defines CreateUser, DeleteUser, GetUser, UserCreated.
#   Q2: "How are responses sent?" -> sender() ! UserCreated / sender() ! users.get
#
# Trap files (plausible but no dependency on UserMessage or UserActor):
#   - OrderActor.scala: another actor for order processing. Same "Actor" pattern
#     but handles different message types entirely.
#   - UserRepository.scala: CRUD operations on users but synchronous, no actor.
#     Shares "user storage" domain but different concurrency model.

initial:
  Messages.scala: |
    sealed trait UserMessage
    case class CreateUser(name: String) extends UserMessage
    case class DeleteUser(id: String) extends UserMessage
    case class GetUser(id: String) extends UserMessage
    case class UserCreated(id: String, name: String)
  UserActor.scala: |
    class UserActor {}
  OrderActor.scala: |
    import akka.actor.{Actor, ActorLogging}

    sealed trait OrderMessage
    case class PlaceOrder(item: String, quantity: Int) extends OrderMessage
    case class CancelOrder(orderId: String) extends OrderMessage

    class OrderActor extends Actor with ActorLogging {
      private var orders: Map[String, String] = Map.empty

      def receive: Receive = {
        case PlaceOrder(item, quantity) =>
          val id = java.util.UUID.randomUUID().toString
          orders = orders + (id -> item)
          log.info(s"Placed order $id: $quantity x $item")

        case CancelOrder(orderId) =>
          orders = orders - orderId
      }
    }
  UserRepository.scala: |
    class UserRepository {
      private var users: Map[String, String] = Map.empty

      def create(name: String): String = {
        val id = java.util.UUID.randomUUID().toString
        users = users + (id -> name)
        id
      }

      def find(id: String): Option[String] = users.get(id)

      def delete(id: String): Boolean = {
        val existed = users.contains(id)
        users = users - id
        existed
      }

      def count: Int = users.size
    }

changed:
  Messages.scala: |
    sealed trait UserMessage
    case class CreateUser(name: String) extends UserMessage
    case class DeleteUser(id: String) extends UserMessage
    case class GetUser(id: String) extends UserMessage
    case class UserCreated(id: String, name: String)
  UserActor.scala: |
    import akka.actor.{Actor, ActorLogging, Props}

    class UserActor extends Actor with ActorLogging {
      private var users: Map[String, String] = Map.empty

      def receive: Receive = {
        case CreateUser(name) =>
          val id = java.util.UUID.randomUUID().toString
          users = users + (id -> name)
          log.info(s"Created user $id: $name")
          sender() ! UserCreated(id, name)

        case DeleteUser(id) =>
          users = users - id
          log.info(s"Deleted user $id")

        case GetUser(id) =>
          sender() ! users.get(id)
      }
    }

    object UserActor {
      def props: Props = Props[UserActor]()
    }
  OrderActor.scala: |
    import akka.actor.{Actor, ActorLogging}

    sealed trait OrderMessage
    case class PlaceOrder(item: String, quantity: Int) extends OrderMessage
    case class CancelOrder(orderId: String) extends OrderMessage

    class OrderActor extends Actor with ActorLogging {
      private var orders: Map[String, String] = Map.empty

      def receive: Receive = {
        case PlaceOrder(item, quantity) =>
          val id = java.util.UUID.randomUUID().toString
          orders = orders + (id -> item)
          log.info(s"Placed order $id: $quantity x $item")

        case CancelOrder(orderId) =>
          orders = orders - orderId
      }
    }
  UserRepository.scala: |
    class UserRepository {
      private var users: Map[String, String] = Map.empty

      def create(name: String): String = {
        val id = java.util.UUID.randomUUID().toString
        users = users + (id -> name)
        id
      }

      def find(id: String): Option[String] = users.get(id)

      def delete(id: String): Boolean = {
        val existed = users.contains(id)
        users = users - id
        existed
      }

      def count: Int = users.size
    }

must_include_files:
  # Changed: implements Actor with message handling
  - UserActor.scala
  # Unchanged but essential: defines the message protocol
  - Messages.scala
must_include_content_from:
  UserActor.scala:
    - "import akka.actor.{Actor, ActorLogging, Props}"
    - "case CreateUser(name) =>"
  Messages.scala:
    - "sealed trait UserMessage"
    - "case class CreateUser(name: String) extends UserMessage"
must_not_include:
  # OrderActor.scala: another actor but different message types
  - OrderActor
  - PlaceOrder
  - CancelOrder
  - OrderMessage
  # UserRepository.scala: sync user CRUD, no actor pattern
  - UserRepository
  - "def create(name: String): String = {"
must_not_include_files:
  - OrderActor.scala
  - UserRepository.scala
max_fragments: 6
max_files: 3
add_garbage_files: false
skip_garbage_check: true
