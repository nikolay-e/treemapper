# Scenario: Formatter imports StringExtensions implicit class and uses its
# extension methods (shout, whisper, repeat, truncate, isBlank).
#
# Diff: src/services/Formatter.scala adds import and new methods that call
# implicit extension methods on String.
#
# Reviewer questions:
#   Q1: "What extension methods are available?"
#       -> src/extensions/StringExtensions.scala (UNCHANGED) shows RichString
#          with shout, whisper, repeat, truncate, isBlank. Reviewer verifies
#          the methods exist and match Formatter's usage.
#   Q2: "How does the import bring them in scope?"
#       -> import extensions.StringExtensions._ brings the implicit class
#          into scope, enabling .shout etc. on String literals.
#
# Not needed:
#   - src/extensions/CollectionExtensions.scala: another implicit class for
#     List ops. Same extensions package, same implicit class pattern.
#   - src/extensions/NumberExtensions.scala: implicit class for Int/Double.
#     Same package, same pattern, amplifies the sibling trap.
#   - src/services/Validator.scala: validates strings using standard library.
#     Same "string manipulation" domain but no StringExtensions dependency.
#   - src/services/Sanitizer.scala: another service that ALSO imports
#     StringExtensions._. HARDEST TRAP: same import, calls same .truncate
#     and .isBlank methods. Sibling consumer of the same implicit class.
#   - src/services/FormatterTest.scala: tests for Formatter, exercises
#     formatLoud, formatQuiet, etc.

max_enrichment: 8.4
initial:
  src/extensions/StringExtensions.scala: |
    package extensions

    object StringExtensions {
      implicit class RichString(val s: String) extends AnyVal {
        def shout: String = s.toUpperCase + "!"
        def whisper: String = s.toLowerCase + "..."
        def repeat(n: Int): String = s * n
        def truncate(maxLen: Int): String =
          if (s.length <= maxLen) s else s.take(maxLen) + "..."
        def isBlank: Boolean = s.trim.isEmpty
      }
    }
  src/services/Formatter.scala: |
    package services

    class Formatter {
      def formatTitle(text: String): String = text.trim
    }
  src/extensions/CollectionExtensions.scala: |
    package extensions

    object CollectionExtensions {
      implicit class RichList[T](val list: List[T]) extends AnyVal {
        def secondOption: Option[T] = list.drop(1).headOption
        def takeEvery(n: Int): List[T] = list.zipWithIndex.collect {
          case (elem, i) if i % n == 0 => elem
        }
        def distinctBy[K](f: T => K): List[T] = {
          var seen = Set.empty[K]
          list.filter { elem =>
            val key = f(elem)
            if (seen.contains(key)) false
            else { seen += key; true }
          }
        }
      }
    }
  src/extensions/NumberExtensions.scala: |
    package extensions

    object NumberExtensions {
      implicit class RichInt(val n: Int) extends AnyVal {
        def isEven: Boolean = n % 2 == 0
        def isOdd: Boolean = n % 2 != 0
        def squared: Int = n * n
        def factorial: BigInt = (1 to n).foldLeft(BigInt(1))(_ * _)
      }

      implicit class RichDouble(val d: Double) extends AnyVal {
        def roundTo(places: Int): Double = {
          val factor = math.pow(10, places)
          (d * factor).round / factor
        }
      }
    }
  src/services/Validator.scala: |
    package services

    class Validator {
      def isValidEmail(email: String): Boolean =
        email.contains("@") && email.contains(".")

      def isValidLength(text: String, min: Int, max: Int): Boolean =
        text.length >= min && text.length <= max

      def sanitize(text: String): String =
        text.trim.replaceAll("[<>]", "")
    }
  src/services/Sanitizer.scala: |
    package services

    import extensions.StringExtensions._

    class Sanitizer {
      def clean(text: String): String = {
        if (text.isBlank) ""
        else text.truncate(500).trim
      }

      def normalizeWhitespace(text: String): String = {
        text.replaceAll("\\s+", " ").trim
      }
    }
  src/services/FormatterTest.scala: |
    package services

    class FormatterTest {
      def testFormatLoud(): Unit = {
        val f = new Formatter
        assert(f.formatLoud("hello") == "HELLO!")
      }

      def testFormatQuiet(): Unit = {
        val f = new Formatter
        assert(f.formatQuiet("HELLO") == "hello...")
      }

      def testFormatSummaryEmpty(): Unit = {
        val f = new Formatter
        assert(f.formatSummary("  ", 100) == "(empty)")
      }
    }

changed:
  src/services/Formatter.scala: |
    package services

    import extensions.StringExtensions._

    class Formatter {
      def formatTitle(text: String): String = text.trim

      def formatLoud(text: String): String = text.shout

      def formatQuiet(text: String): String = text.whisper

      def formatRepeated(text: String, times: Int): String =
        text.repeat(times)

      def formatSummary(text: String, maxLen: Int): String = {
        if (text.isBlank) "(empty)"
        else text.truncate(maxLen)
      }
    }
must_include_files:
  # Q1+Q2: StringExtensions defines the implicit class methods
  - src/extensions/StringExtensions.scala
must_include_content_from:
  src/extensions/StringExtensions.scala:
    - "implicit class RichString"
    - "def shout: String"
    - "def whisper: String"
    - "def truncate(maxLen: Int)"
    - "def isBlank: Boolean"
must_not_include:
  # CollectionExtensions.scala: another implicit class, same package
  - RichList
  - secondOption
  - takeEvery
  - distinctBy
  # NumberExtensions.scala: another implicit class in same package
  - RichInt
  - RichDouble
  - "def squared"
  - "def isEven"
  # Validator.scala: string operations but standard library
  - "class Validator"
  - isValidEmail
  - sanitize
  # Sanitizer.scala: ALSO imports StringExtensions._ and calls isBlank/truncate
  - "class Sanitizer"
  - "def clean"
  - normalizeWhitespace
  # FormatterTest.scala: tests for the changed class
  - "class FormatterTest"
  - testFormatLoud
must_not_include_files:
  - src/extensions/CollectionExtensions.scala
  - src/extensions/NumberExtensions.scala
  - src/services/Validator.scala
  - src/services/Sanitizer.scala
  - src/services/FormatterTest.scala
max_fragments: 5
max_files: 3
add_garbage_files: false
skip_garbage_check: true
