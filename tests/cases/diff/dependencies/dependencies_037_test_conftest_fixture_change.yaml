name: dependencies_037_test_conftest_fixture_change
repo:
  initial_files:
    src/database.py: |
      class Database:
          def __init__(self, connection_string: str):
              self.connection_string = connection_string
              self.connected = False

          def connect(self):
              self.connected = True

          def disconnect(self):
              self.connected = False

          def query(self, sql: str) -> list:
              if not self.connected:
                  raise RuntimeError("Not connected")
              return []
    tests/conftest.py: |
      import pytest
      from src.database import Database

      @pytest.fixture
      def db():
          database = Database("test://localhost")
          database.connect()
          yield database
          database.disconnect()
    tests/test_queries.py: |
      def test_query_returns_list(db):
          result = db.query("SELECT * FROM users")
          assert isinstance(result, list)

      def test_db_is_connected(db):
          assert db.connected is True
    tests/integration/conftest.py: |
      import pytest
      from src.database import Database
      GARBAGE_DEP037_INTEG_CONFTEST_A = "integration_conftest"
      GARBAGE_DEP037_INTEG_SETUP_B = True

      @pytest.fixture
      def db():
          GARBAGE_DEP037_INTEG_DB_C = "integration_db_fixture"
          database = Database("integration://localhost:5432")
          database.connect()
          yield database
          database.disconnect()
    tests/test_migrations.py: |
      GARBAGE_DEP037_MIGTEST_D = "migration_test_module"
      GARBAGE_DEP037_MIGTEST_INIT_E = True

      def test_migration_runs(db):
          GARBAGE_DEP037_MIGTEST_RUN_F = "running"
          result = db.query("SELECT version FROM schema_migrations")
          assert isinstance(result, list)
    src/cache_store.py: |
      GARBAGE_DEP037_CACHE_G = "cache_store_module"
      GARBAGE_DEP037_CACHE_INIT_H = True

      class CacheStore:
          def __init__(self, connection_string: str):
              self.connection_string = connection_string
              self.connected = False

          def connect(self):
              GARBAGE_DEP037_CACHE_CONN_I = "connecting"
              self.connected = True

          def disconnect(self):
              self.connected = False

          def query(self, key: str) -> list:
              return []
  changed_files:
    src/database.py: |
      class Database:
          def __init__(self, connection_string: str):
              self.connection_string = connection_string
              self.connected = False

          def connect(self):
              self.connected = True

          def disconnect(self):
              self.connected = False

          def query(self, sql: str) -> list:
              if not self.connected:
                  raise RuntimeError("Not connected")
              return []
    tests/conftest.py: |
      import pytest
      from src.database import Database

      @pytest.fixture
      def db():
          database = Database("test://localhost")
          database.connect()
          yield database
          database.disconnect()

      @pytest.fixture
      def db_with_data(db):
          db.query("INSERT INTO users VALUES (1, 'test')")
          return db

      @pytest.fixture
      def transaction_db(db):
          db.query("BEGIN TRANSACTION")
          yield db
          db.query("ROLLBACK")
    tests/test_queries.py: |
      def test_query_returns_list(db):
          result = db.query("SELECT * FROM users")
          assert isinstance(result, list)

      def test_db_is_connected(db):
          assert db.connected is True
    tests/integration/conftest.py: |
      import pytest
      from src.database import Database
      GARBAGE_DEP037_INTEG_CONFTEST_A = "integration_conftest"
      GARBAGE_DEP037_INTEG_SETUP_B = True

      @pytest.fixture
      def db():
          GARBAGE_DEP037_INTEG_DB_C = "integration_db_fixture"
          database = Database("integration://localhost:5432")
          database.connect()
          yield database
          database.disconnect()
    tests/test_migrations.py: |
      GARBAGE_DEP037_MIGTEST_D = "migration_test_module"
      GARBAGE_DEP037_MIGTEST_INIT_E = True

      def test_migration_runs(db):
          GARBAGE_DEP037_MIGTEST_RUN_F = "running"
          result = db.query("SELECT version FROM schema_migrations")
          assert isinstance(result, list)
    src/cache_store.py: |
      GARBAGE_DEP037_CACHE_G = "cache_store_module"
      GARBAGE_DEP037_CACHE_INIT_H = True

      class CacheStore:
          def __init__(self, connection_string: str):
              self.connection_string = connection_string
              self.connected = False

          def connect(self):
              GARBAGE_DEP037_CACHE_CONN_I = "connecting"
              self.connected = True

          def disconnect(self):
              self.connected = False

          def query(self, key: str) -> list:
              return []
  commit_message: Update files
fragments:
- id: conftest
  selector:
    path: tests/conftest.py
    anchor: 'def db_with_data(db):'
- id: conftest_1
  selector:
    path: tests/conftest.py
    anchor: 'def transaction_db(db):'
- id: conftest_2
  selector:
    path: tests/conftest.py
    anchor: INSERT INTO users VALUES
- id: no_conftest
  selector:
    path: tests/integration/conftest.py
- id: no_test_migrations
  selector:
    path: tests/test_migrations.py
- id: no_cache_store
  selector:
    path: src/cache_store.py
- id: no_global_0
  selector:
    anchor: GARBAGE_DEP037_INTEG_CONFTEST_A
- id: no_global_1
  selector:
    anchor: GARBAGE_DEP037_INTEG_SETUP_B
- id: no_global_2
  selector:
    anchor: GARBAGE_DEP037_INTEG_DB_C
- id: no_global_3
  selector:
    anchor: GARBAGE_DEP037_MIGTEST_D
- id: no_global_4
  selector:
    anchor: GARBAGE_DEP037_MIGTEST_INIT_E
- id: no_global_5
  selector:
    anchor: GARBAGE_DEP037_MIGTEST_RUN_F
- id: no_global_6
  selector:
    anchor: GARBAGE_DEP037_CACHE_G
- id: no_global_7
  selector:
    anchor: GARBAGE_DEP037_CACHE_INIT_H
- id: no_global_8
  selector:
    anchor: GARBAGE_DEP037_CACHE_CONN_I
oracle:
  required:
  - conftest
  - conftest_1
  - conftest_2
  allowed: []
  forbidden:
  - no_conftest
  - no_test_migrations
  - no_cache_store
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
  - no_global_8
