max_enrichment: 6.1
initial:
  src/exceptions.py: |
    class ValidationError(Exception):
        def __init__(self, field: str, message: str):
            self.field = field
            self.message = message
            super().__init__(f"{field}: {message}")

    class NotFoundError(Exception):
        def __init__(self, resource: str, identifier: str):
            self.resource = resource
            self.identifier = identifier
            super().__init__(f"{resource} with id {identifier} not found")
  src/user_repository.py: |
    from src.exceptions import NotFoundError, ValidationError

    class UserRepository:
        def __init__(self):
            self.users = {}

        def get(self, user_id: int):
            if user_id not in self.users:
                raise NotFoundError("User", str(user_id))
            return self.users[user_id]

        def create(self, data: dict):
            if "email" not in data:
                raise ValidationError("email", "is required")
            user_id = len(self.users) + 1
            self.users[user_id] = {**data, "id": user_id}
            return self.users[user_id]
  tests/test_user_repository.py: |
    from src.user_repository import UserRepository

    def test_create_user():
        repo = UserRepository()
        user = repo.create({"name": "Test", "email": "test@example.com"})
        assert user["name"] == "Test"

  # --- Tricky: same exception classes imported, same pytest.raises pattern ---
  tests/test_order_repository.py: |
    import pytest
    from src.exceptions import NotFoundError, ValidationError
    GARBAGE_DEP039_ORDERTEST_A = "order_repo_tests"
    GARBAGE_DEP039_ORDERTEST_INIT_B = True

    class OrderRepository:
        def __init__(self):
            self.orders = {}

        def get(self, order_id: int):
            if order_id not in self.orders:
                raise NotFoundError("Order", str(order_id))
            return self.orders[order_id]

    def test_get_nonexistent_order():
        GARBAGE_DEP039_ORDERTEST_RUN_C = "running"
        repo = OrderRepository()
        with pytest.raises(NotFoundError) as exc_info:
            repo.get(999)
        assert exc_info.value.resource == "Order"

  # --- Tricky: same PermissionDeniedError name (name collision) ---
  src/auth_exceptions.py: |
    GARBAGE_DEP039_AUTHEXC_D = "auth_exceptions_module"
    GARBAGE_DEP039_AUTHEXC_INIT_E = True

    class PermissionDeniedError(Exception):
        GARBAGE_DEP039_AUTHEXC_CLASS_F = "auth_perm_denied"

        def __init__(self, user: str, resource: str):
            self.user = user
            self.resource = resource
            super().__init__(f"User {user} denied access to {resource}")

  # --- Tricky: same exceptions import, similar create pattern ---
  src/product_repository.py: |
    from src.exceptions import ValidationError
    GARBAGE_DEP039_PRODREPO_G = "product_repository"
    GARBAGE_DEP039_PRODREPO_INIT_H = True

    class ProductRepository:
        def __init__(self):
            self.products = {}

        def create(self, data: dict):
            GARBAGE_DEP039_PRODREPO_CREATE_I = "creating"
            if "name" not in data:
                raise ValidationError("name", "is required")
            product_id = len(self.products) + 1
            self.products[product_id] = {**data, "id": product_id}
            return self.products[product_id]

changed:
  src/exceptions.py: |
    class ValidationError(Exception):
        def __init__(self, field: str, message: str):
            self.field = field
            self.message = message
            super().__init__(f"{field}: {message}")

    class NotFoundError(Exception):
        def __init__(self, resource: str, identifier: str):
            self.resource = resource
            self.identifier = identifier
            super().__init__(f"{resource} with id {identifier} not found")

    class PermissionDeniedError(Exception):
        def __init__(self, action: str, resource: str):
            self.action = action
            self.resource = resource
            super().__init__(f"Permission denied: cannot {action} {resource}")
  src/user_repository.py: |
    from src.exceptions import NotFoundError, ValidationError

    class UserRepository:
        def __init__(self):
            self.users = {}

        def get(self, user_id: int):
            if user_id not in self.users:
                raise NotFoundError("User", str(user_id))
            return self.users[user_id]

        def create(self, data: dict):
            if "email" not in data:
                raise ValidationError("email", "is required")
            if "name" not in data:
                raise ValidationError("name", "is required")
            user_id = len(self.users) + 1
            self.users[user_id] = {**data, "id": user_id}
            return self.users[user_id]
  tests/test_user_repository.py: |
    import pytest
    from src.user_repository import UserRepository
    from src.exceptions import NotFoundError, ValidationError

    def test_create_user():
        repo = UserRepository()
        user = repo.create({"name": "Test", "email": "test@example.com"})
        assert user["name"] == "Test"

    def test_get_nonexistent_raises_not_found():
        repo = UserRepository()
        with pytest.raises(NotFoundError) as exc_info:
            repo.get(999)
        assert exc_info.value.resource == "User"
        assert exc_info.value.identifier == "999"

    def test_create_without_email_raises_validation_error():
        repo = UserRepository()
        with pytest.raises(ValidationError) as exc_info:
            repo.create({"name": "Test"})
        assert exc_info.value.field == "email"
        assert "required" in exc_info.value.message

    def test_create_without_name_raises_validation_error():
        repo = UserRepository()
        with pytest.raises(ValidationError) as exc_info:
            repo.create({"email": "test@example.com"})
        assert exc_info.value.field == "name"
  tests/test_order_repository.py: |
    import pytest
    from src.exceptions import NotFoundError, ValidationError
    GARBAGE_DEP039_ORDERTEST_A = "order_repo_tests"
    GARBAGE_DEP039_ORDERTEST_INIT_B = True

    class OrderRepository:
        def __init__(self):
            self.orders = {}

        def get(self, order_id: int):
            if order_id not in self.orders:
                raise NotFoundError("Order", str(order_id))
            return self.orders[order_id]

    def test_get_nonexistent_order():
        GARBAGE_DEP039_ORDERTEST_RUN_C = "running"
        repo = OrderRepository()
        with pytest.raises(NotFoundError) as exc_info:
            repo.get(999)
        assert exc_info.value.resource == "Order"
  src/auth_exceptions.py: |
    GARBAGE_DEP039_AUTHEXC_D = "auth_exceptions_module"
    GARBAGE_DEP039_AUTHEXC_INIT_E = True

    class PermissionDeniedError(Exception):
        GARBAGE_DEP039_AUTHEXC_CLASS_F = "auth_perm_denied"

        def __init__(self, user: str, resource: str):
            self.user = user
            self.resource = resource
            super().__init__(f"User {user} denied access to {resource}")
  src/product_repository.py: |
    from src.exceptions import ValidationError
    GARBAGE_DEP039_PRODREPO_G = "product_repository"
    GARBAGE_DEP039_PRODREPO_INIT_H = True

    class ProductRepository:
        def __init__(self):
            self.products = {}

        def create(self, data: dict):
            GARBAGE_DEP039_PRODREPO_CREATE_I = "creating"
            if "name" not in data:
                raise ValidationError("name", "is required")
            product_id = len(self.products) + 1
            self.products[product_id] = {**data, "id": product_id}
            return self.products[product_id]

# === Assertions ===

must_include_files:
- src/exceptions.py
- src/user_repository.py
- tests/test_user_repository.py

must_include_content_from:
  src/exceptions.py:
    - 'class PermissionDeniedError(Exception):'
  tests/test_user_repository.py:
    - import pytest
    - from src.exceptions import NotFoundError, ValidationError
  src/user_repository.py:
    - 'class UserRepository:'

must_not_include:
# Tricky: same exceptions import, same pytest.raises(NotFoundError) pattern
- GARBAGE_DEP039_ORDERTEST_A
- GARBAGE_DEP039_ORDERTEST_INIT_B
- GARBAGE_DEP039_ORDERTEST_RUN_C
# Tricky: same class name PermissionDeniedError (name collision)
- GARBAGE_DEP039_AUTHEXC_D
- GARBAGE_DEP039_AUTHEXC_INIT_E
- GARBAGE_DEP039_AUTHEXC_CLASS_F
# Tricky: same ValidationError import, same create() + raise pattern
- GARBAGE_DEP039_PRODREPO_G
- GARBAGE_DEP039_PRODREPO_INIT_H
- GARBAGE_DEP039_PRODREPO_CREATE_I

must_not_include_files:
- tests/test_order_repository.py
- src/auth_exceptions.py
- src/product_repository.py

max_fragments: 8
max_files: 3
add_garbage_files: false
skip_garbage_check: true
