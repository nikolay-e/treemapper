name: helm_031_template_default
tags:
- lang:python
- change:single_file
- change:config
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    values.yaml: |
      # timeout not set
    templates/deployment.yaml: |
      apiVersion: apps/v1
      kind: Deployment
      spec:
        template:
          spec:
            containers:
            - name: app
              env:
              - name: TIMEOUT
                value: "30"
    junk.py: |
      garbage_marker_12345 = 1
      unused_marker_67890 = 2
  changed_files:
    values.yaml: |
      # timeout not set
    templates/deployment.yaml: |
      apiVersion: apps/v1
      kind: Deployment
      spec:
        template:
          spec:
            containers:
            - name: app
              env:
              - name: TIMEOUT
                value: {{ .Values.timeout | default 30 | quote }}
    junk.py: |
      garbage_marker_12345 = 1
      unused_marker_67890 = 2
  commit_message: Update files
fragments:
- id: deployment_file
  selector:
    path: templates/deployment.yaml
- id: req_global_0
  selector:
    anchor: 'value: {{ .Values.timeout | default 30 | quote }}'
oracle:
  required:
  - deployment_file
  - req_global_0
  allowed: []
  forbidden: []
