name: helm_023_template_service
tags:
- lang:markdown
- lang:yaml
- change:single_file
- change:config
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    chart/Chart.yaml: |
      apiVersion: v2
      name: svc-app
      version: 1.0.0
    chart/values.yaml: |
      service:
        type: ClusterIP
        port: 80
    chart/templates/service.yaml: |
      apiVersion: v1
      kind: Service
      metadata:
        name: myapp-service
      spec:
        type: ClusterIP
        ports:
          - port: 80
    chart/templates/gatekeeper-constraint.yaml: |
      apiVersion: constraints.gatekeeper.sh/v1beta1
      metadata:
      spec:
        match:
          kinds:
    chart/docs/service-mesh.md: |
      # Service Mesh Configuration
      ## Traffic Routing
  changed_files:
    chart/Chart.yaml: |
      apiVersion: v2
      name: svc-app
      version: 1.0.0
    chart/values.yaml: |
      service:
        type: ClusterIP
        port: 80
    chart/templates/service.yaml: |
      apiVersion: v1
      kind: Service
      metadata:
        name: {{ .Release.Name }}-service
      spec:
        type: {{ .Values.service.type }}
        ports:
          - port: {{ .Values.service.port }}
    chart/templates/gatekeeper-constraint.yaml: |
      apiVersion: constraints.gatekeeper.sh/v1beta1
      metadata:
      spec:
        match:
          kinds:
    chart/docs/service-mesh.md: |
      # Service Mesh Configuration
      ## Traffic Routing
  commit_message: Update files
fragments:
- id: service_file
  selector:
    path: chart/templates/service.yaml
- id: req_global_0
  selector:
    anchor: 'name: {{ .Release.Name }}-service'
oracle:
  required:
  - service_file
  - req_global_0
  allowed: []
  forbidden: []
