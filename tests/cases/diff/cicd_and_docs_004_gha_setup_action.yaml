name: cicd_and_docs_004_gha_setup_action
tags:
- lang:typescript
- change:single_file
- change:config
- context:same_file
- distance:0hop
- difficulty:easy
repo:
  initial_files:
    package.json: |
      {
        "name": "myapp",
        "version": "2.0.0",
        "engines": { "node": ">=18" },
        "scripts": {
          "build": "tsc",
          "test": "jest",
          "lint": "eslint src/"
        },
        "dependencies": {
          "express": "^4.18.0",
          "zod": "^3.22.0"
        },
        "devDependencies": {
          "typescript": "^5.0.0",
          "jest": "^29.0.0"
        }
      }
    .github/workflows/ci.yml: |
      name: CI
      on: [push, pull_request]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm install
            - run: npm run build
    src/app.ts: |
      import express from 'express';
      import { userRouter } from './routes/user';

      const app = express();
      app.use(express.json());
      app.use('/api/users', userRouter);

      export default app;
    garbage_cache.ts: |
      const GARBAGE_CICD_DOCS_004_CACHE_MARKER_A = "redis";
      const GARBAGE_CICD_DOCS_004_TTL_MARKER_B = 3600;

      class GarbageCacheService {
        static GARBAGE_CICD_DOCS_004_STRATEGY_MARKER_C = "lru";
        invalidate(): void {}
      }
    garbage_queue.ts: |
      const GARBAGE_CICD_DOCS_004_QUEUE_MARKER_D = "rabbitmq";
      const GARBAGE_CICD_DOCS_004_PRIORITY_MARKER_E = 10;

      class GarbageQueueManager {
        static GARBAGE_CICD_DOCS_004_RETRY_MARKER_F = 3;
        enqueue(): void {}
      }
  changed_files:
    package.json: |
      {
        "name": "myapp",
        "version": "2.0.0",
        "engines": { "node": ">=18" },
        "scripts": {
          "build": "tsc",
          "test": "jest",
          "lint": "eslint src/"
        },
        "dependencies": {
          "express": "^4.18.0",
          "zod": "^3.22.0"
        },
        "devDependencies": {
          "typescript": "^5.0.0",
          "jest": "^29.0.0"
        }
      }
    .github/workflows/ci.yml: |
      name: CI
      on: [push, pull_request]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                node-version: 18
                cache: npm
            - run: npm install
            - run: npm run build
    src/app.ts: |
      import express from 'express';
      import { userRouter } from './routes/user';

      const app = express();
      app.use(express.json());
      app.use('/api/users', userRouter);

      export default app;
    garbage_cache.ts: |
      const GARBAGE_CICD_DOCS_004_CACHE_MARKER_A = "redis";
      const GARBAGE_CICD_DOCS_004_TTL_MARKER_B = 3600;

      class GarbageCacheService {
        static GARBAGE_CICD_DOCS_004_STRATEGY_MARKER_C = "lru";
        invalidate(): void {}
      }
    garbage_queue.ts: |
      const GARBAGE_CICD_DOCS_004_QUEUE_MARKER_D = "rabbitmq";
      const GARBAGE_CICD_DOCS_004_PRIORITY_MARKER_E = 10;

      class GarbageQueueManager {
        static GARBAGE_CICD_DOCS_004_RETRY_MARKER_F = 3;
        enqueue(): void {}
      }
  commit_message: Update files
fragments:
- id: ci_file
  selector:
    path: .github/workflows/ci.yml
- id: req_global_0
  selector:
    anchor: '- uses: actions/setup-node@v4'
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_004_CACHE_MARKER_A
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_004_PRIORITY_MARKER_E
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_004_QUEUE_MARKER_D
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_004_RETRY_MARKER_F
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_004_STRATEGY_MARKER_C
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_004_TTL_MARKER_B
- id: no_global_6
  selector:
    anchor: garbage_cache.ts
- id: no_global_7
  selector:
    anchor: garbage_queue.ts
oracle:
  required:
  - ci_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
