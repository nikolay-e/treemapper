User.swift: |
  struct User {
      var name: String
      var email: String
  }
  // garbage_marker_12345
UserEditor.swift: |
  // Initial
  import SwiftUI
  struct UserEditor: View {
      var body: some View { Text("") }
  }
