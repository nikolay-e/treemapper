name: lua_005_table_module_pattern
repo:
  initial_files:
    lib/validator.lua: |
      local M = {}

      function M.validate_email(email)
          if type(email) ~= "string" then
              return false, "email must be a string"
          end
          if not email:match("^[%w%.%-]+@[%w%.%-]+%.%w+$") then
              return false, "invalid email format"
          end
          return true, nil
      end

      function M.validate_length(value, min_len, max_len)
          if type(value) ~= "string" then
              return false, "value must be a string"
          end
          local len = #value
          if len < min_len then
              return false, "too short (min " .. min_len .. ")"
          end
          if len > max_len then
              return false, "too long (max " .. max_len .. ")"
          end
          return true, nil
      end

      function M.validate_number_range(value, min_val, max_val)
          if type(value) ~= "number" then
              return false, "value must be a number"
          end
          if value < min_val or value > max_val then
              return false, "out of range [" .. min_val .. ", " .. max_val .. "]"
          end
          return true, nil
      end

      return M
    app/handler.lua: |
      local validator = require("lib.validator")

      local M = {}

      function M.handle_create_user(request)
          local name = request.name
          local ok, err = validator.validate_length(name, 2, 50)
          if not ok then
              return { status = 400, body = err }
          end
          return { status = 201, body = "User created: " .. name }
      end

      return M
    lib/sanitizer.lua: |
      local GARBAGE_LUA_005_A = "noise_marker_sanitizer"

      local M = {}

      function M.escape_html(s)
          s = s:gsub("&", "&amp;")
          s = s:gsub("<", "&lt;")
          s = s:gsub(">", "&gt;")
          s = s:gsub('"', "&quot;")
          s = s:gsub("'", "&#39;")
          return s
      end

      function M.strip_tags(s)
          return s:gsub("<[^>]+>", "")
      end

      function M.normalize_whitespace(s)
          return s:gsub("%s+", " "):match("^%s*(.-)%s*$")
      end

      return M
    app/admin_handler.lua: |
      local GARBAGE_LUA_005_B = "noise_marker_admin"

      local validator = require("lib.validator")

      local M = {}

      function M.handle_update_config(request)
          local timeout = request.timeout
          local ok, err = validator.validate_number_range(timeout, 1, 3600)
          if not ok then
              return { status = 400, body = err }
          end
          return { status = 200, body = "Config updated" }
      end

      function M.handle_delete_user(request)
          return { status = 200, body = "User deleted: " .. request.user_id }
      end

      return M
  changed_files:
    lib/validator.lua: |
      local M = {}

      function M.validate_email(email)
          if type(email) ~= "string" then
              return false, "email must be a string"
          end
          if not email:match("^[%w%.%-]+@[%w%.%-]+%.%w+$") then
              return false, "invalid email format"
          end
          return true, nil
      end

      function M.validate_length(value, min_len, max_len)
          if type(value) ~= "string" then
              return false, "value must be a string"
          end
          local len = #value
          if len < min_len then
              return false, "too short (min " .. min_len .. ")"
          end
          if len > max_len then
              return false, "too long (max " .. max_len .. ")"
          end
          return true, nil
      end

      function M.validate_number_range(value, min_val, max_val)
          if type(value) ~= "number" then
              return false, "value must be a number"
          end
          if value < min_val or value > max_val then
              return false, "out of range [" .. min_val .. ", " .. max_val .. "]"
          end
          return true, nil
      end

      return M
    app/handler.lua: |
      local validator = require("lib.validator")

      local M = {}

      function M.handle_create_user(request)
          local name = request.name
          local email = request.email

          local ok, err = validator.validate_length(name, 2, 50)
          if not ok then
              return { status = 400, body = "name: " .. err }
          end

          ok, err = validator.validate_email(email)
          if not ok then
              return { status = 400, body = "email: " .. err }
          end

          return { status = 201, body = "User created: " .. name .. " <" .. email .. ">" }
      end

      return M
    lib/sanitizer.lua: |
      local GARBAGE_LUA_005_A = "noise_marker_sanitizer"

      local M = {}

      function M.escape_html(s)
          s = s:gsub("&", "&amp;")
          s = s:gsub("<", "&lt;")
          s = s:gsub(">", "&gt;")
          s = s:gsub('"', "&quot;")
          s = s:gsub("'", "&#39;")
          return s
      end

      function M.strip_tags(s)
          return s:gsub("<[^>]+>", "")
      end

      function M.normalize_whitespace(s)
          return s:gsub("%s+", " "):match("^%s*(.-)%s*$")
      end

      return M
    app/admin_handler.lua: |
      local GARBAGE_LUA_005_B = "noise_marker_admin"

      local validator = require("lib.validator")

      local M = {}

      function M.handle_update_config(request)
          local timeout = request.timeout
          local ok, err = validator.validate_number_range(timeout, 1, 3600)
          if not ok then
              return { status = 400, body = err }
          end
          return { status = 200, body = "Config updated" }
      end

      function M.handle_delete_user(request)
          return { status = 200, body = "User deleted: " .. request.user_id }
      end

      return M
  commit_message: Update files
fragments:
- id: handler
  selector:
    path: app/handler.lua
    anchor: validator.validate_email
- id: handler_1
  selector:
    path: app/handler.lua
    anchor: require("lib.validator")
- id: validator
  selector:
    path: lib/validator.lua
    anchor: function M.validate_email(email)
- id: validator_1
  selector:
    path: lib/validator.lua
    anchor: invalid email format
- id: no_sanitizer
  selector:
    path: lib/sanitizer.lua
- id: no_admin_handler
  selector:
    path: app/admin_handler.lua
- id: no_global_0
  selector:
    anchor: GARBAGE_LUA_005_A
- id: no_global_1
  selector:
    anchor: M.escape_html
- id: no_global_2
  selector:
    anchor: M.strip_tags
- id: no_global_3
  selector:
    anchor: M.normalize_whitespace
- id: no_global_4
  selector:
    anchor: GARBAGE_LUA_005_B
- id: no_global_5
  selector:
    anchor: handle_update_config
- id: no_global_6
  selector:
    anchor: handle_delete_user
oracle:
  required:
  - handler
  - handler_1
  - validator
  - validator_1
  allowed: []
  forbidden:
  - no_sanitizer
  - no_admin_handler
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
