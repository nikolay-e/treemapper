locals.tf: |
  locals {
    common_tags = {
      Project     = "myapp"
      Owner       = "team"
      Environment = "prod"
    }
  }
main.tf: |
  resource "aws_instance" "web" {
    ami           = "ami-12345678"
    instance_type = "t3.micro"
    tags          = local.common_tags
  }
