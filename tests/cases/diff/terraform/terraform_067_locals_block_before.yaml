locals.tf: |
  locals {
    common_tags = {
      Project = "myapp"
      Owner   = "team"
    }
  }
main.tf: |
  resource "aws_instance" "web" {
    ami           = "ami-12345678"
    instance_type = "t3.micro"
    tags          = local.common_tags
  }
