name: terraform_488_provider_version
initial:
  versions.tf: |
    terraform {
      required_providers {
        aws = {
          source  = "hashicorp/aws"
          version = "~> 4.0"
        }
      }
    }
changed:
  versions.tf: |
    terraform {
      required_providers {
        aws = {
          source  = "hashicorp/aws"
          version = "~> 5.0"
        }
        random = {
          source  = "hashicorp/random"
          version = "~> 3.5"
        }
        null = {
          source  = "hashicorp/null"
          version = "~> 3.2"
        }
      }
    }
assertions:
  must_include:
  - versions.tf
options:
  commit_message: Update provider versions
