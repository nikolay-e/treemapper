name: fragments_010_async_with_decorator
repo:
  initial_files:
    async_decorated.py: |
      import asyncio
      from functools import wraps

      def async_decorator(func):
          @wraps(func)
          async def wrapper(*args, **kwargs):
              return await func(*args, **kwargs)
          return wrapper

      def sync_decorator_041():

      async def unrelated_async_041():

      class UnrelatedDecoratedClass:

          @staticmethod
          async def static_async():

      @async_decorator
      async def decorated_async():
          await other()

      @async_decorator
      async def another_decorated_041():

      class AnotherDecClass:
          @async_decorator
          async def decorated_method(self):
    garbage_async_dec_041.py: |
      async def garbage_async_decorated():
          return "GARBAGE_FRAG_garbageasyncdec041"

      class GarbageDecClass041:
          VALUE = "GARBAGE_FRAG_garbagedecclass041"

      GARBAGE_DEC_CONST = "GARBAGE_FRAG_garbagedecconst041"
  changed_files:
    async_decorated.py: |
      import asyncio
      from functools import wraps

      def async_decorator(func):
          @wraps(func)
          async def wrapper(*args, **kwargs):
              return await func(*args, **kwargs)
          return wrapper

      def sync_decorator_041():

      async def unrelated_async_041():

      class UnrelatedDecoratedClass:

          @staticmethod
          async def static_async():

      @async_decorator
      async def decorated_async():
          await other()
          return True

      @async_decorator
      async def another_decorated_041():

      class AnotherDecClass:
          @async_decorator
          async def decorated_method(self):
    garbage_async_dec_041.py: |
      async def garbage_async_decorated():
          return "GARBAGE_FRAG_garbageasyncdec041"

      class GarbageDecClass041:
          VALUE = "GARBAGE_FRAG_garbagedecclass041"

      GARBAGE_DEC_CONST = "GARBAGE_FRAG_garbagedecconst041"
  commit_message: Update files
fragments:
- id: async_decorated_file
  selector:
    path: async_decorated.py
- id: req_global_0
  selector:
    anchor: return True
- id: no_global_0
  selector:
    anchor: GARBAGE_DEC_CONST
- id: no_global_1
  selector:
    anchor: garbage_async_dec_041.py
oracle:
  required:
  - async_decorated_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
