name: cicd_and_docs_069_sql_view_definition
repo:
  initial_files:
    sql/views/user_orders_view.sql: |
      CREATE OR REPLACE VIEW user_orders_summary AS
      SELECT
          u.id AS user_id,
          u.name AS user_name,
          COUNT(o.id) AS order_count,
          SUM(o.total) AS total_spent
      FROM users u
      LEFT JOIN orders o ON u.id = o.user_id
      GROUP BY u.id, u.name;
    src/reports/user_report.py: |
      from typing import Dict, List, Optional
      from dataclasses import dataclass
      from decimal import Decimal

      @dataclass
      class UserOrdersSummary:
          user_id: int
          user_name: str
          order_count: int
          total_spent: Decimal

      class UserReport:
          def __init__(self, db):
              self.db = db

          def get_summary(self, user_id: int) -> Optional[UserOrdersSummary]:
              result = self.db.query(
                  "SELECT * FROM user_orders_summary WHERE user_id = %s",
                  [user_id]
              )
              if result:
                  return UserOrdersSummary(**result[0])
              return None

          def get_all_summaries(self) -> List[UserOrdersSummary]:
              results = self.db.query("SELECT * FROM user_orders_summary")
              return [UserOrdersSummary(**r) for r in results]
    garbage_view_manager.ts: |
      const GARBAGE_CICD_DOCS_069_VIEW_MARKER_A = "materialized";
      const GARBAGE_CICD_DOCS_069_REFRESH_MARKER_B = "concurrent";

      class GarbageViewManager {
        static GARBAGE_CICD_DOCS_069_CACHE_MARKER_C = 3600;
        manage(): void {}
      }
    garbage_view_optimizer.ts: |
      const GARBAGE_CICD_DOCS_069_OPTIMIZE_MARKER_D = "analyze";
      const GARBAGE_CICD_DOCS_069_INDEX_MARKER_E = "btree";

      class GarbageViewOptimizer {
        static GARBAGE_CICD_DOCS_069_VACUUM_MARKER_F = false;
        optimize(): void {}
      }
  changed_files:
    sql/views/user_orders_view.sql: |
      CREATE OR REPLACE VIEW user_orders_summary AS
      SELECT
          u.id AS user_id,
          u.name AS user_name,
          u.email AS user_email,
          COUNT(o.id) AS order_count,
          COALESCE(SUM(o.total), 0) AS total_spent,
          COALESCE(AVG(o.total), 0) AS avg_order_value,
          MIN(o.created_at) AS first_order_date,
          MAX(o.created_at) AS last_order_date,
          CASE
              WHEN COUNT(o.id) >= 10 THEN 'gold'
              WHEN COUNT(o.id) >= 5 THEN 'silver'
              ELSE 'bronze'
          END AS customer_tier
      FROM users u
      LEFT JOIN orders o ON u.id = o.user_id AND o.status != 'cancelled'
      GROUP BY u.id, u.name, u.email;

      -- Create index on the underlying table for view performance
      CREATE INDEX IF NOT EXISTS idx_orders_user_status
          ON orders(user_id, status);
    src/reports/user_report.py: |
      from typing import Dict, List, Optional
      from dataclasses import dataclass
      from decimal import Decimal

      @dataclass
      class UserOrdersSummary:
          user_id: int
          user_name: str
          order_count: int
          total_spent: Decimal

      class UserReport:
          def __init__(self, db):
              self.db = db

          def get_summary(self, user_id: int) -> Optional[UserOrdersSummary]:
              result = self.db.query(
                  "SELECT * FROM user_orders_summary WHERE user_id = %s",
                  [user_id]
              )
              if result:
                  return UserOrdersSummary(**result[0])
              return None

          def get_all_summaries(self) -> List[UserOrdersSummary]:
              results = self.db.query("SELECT * FROM user_orders_summary")
              return [UserOrdersSummary(**r) for r in results]
    garbage_view_manager.ts: |
      const GARBAGE_CICD_DOCS_069_VIEW_MARKER_A = "materialized";
      const GARBAGE_CICD_DOCS_069_REFRESH_MARKER_B = "concurrent";

      class GarbageViewManager {
        static GARBAGE_CICD_DOCS_069_CACHE_MARKER_C = 3600;
        manage(): void {}
      }
    garbage_view_optimizer.ts: |
      const GARBAGE_CICD_DOCS_069_OPTIMIZE_MARKER_D = "analyze";
      const GARBAGE_CICD_DOCS_069_INDEX_MARKER_E = "btree";

      class GarbageViewOptimizer {
        static GARBAGE_CICD_DOCS_069_VACUUM_MARKER_F = false;
        optimize(): void {}
      }
  commit_message: Update files
fragments:
- id: user_orders_view_file
  selector:
    path: sql/views/user_orders_view.sql
- id: req_global_0
  selector:
    anchor: u.email AS user_email,
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_069_CACHE_MARKER_C
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_069_INDEX_MARKER_E
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_069_OPTIMIZE_MARKER_D
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_069_REFRESH_MARKER_B
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_069_VACUUM_MARKER_F
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_069_VIEW_MARKER_A
- id: no_global_6
  selector:
    anchor: garbage_view_manager.ts
- id: no_global_7
  selector:
    anchor: garbage_view_optimizer.ts
oracle:
  required:
  - user_orders_view_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
