initial:
  module.py: |
    def func():
        if should_recurse():
            return func()
        return base_case()

    def should_recurse():
        return False

    def base_case():
        return 0

    class RecursiveProcessor:
        def process(self, data, depth=0):
            if depth > 10:
                return self.finalize(data)
            transformed = self.transform(data)
            return self.process(transformed, depth + 1)

        def transform(self, data):
            return data * 2

        def finalize(self, data):
            return {"result": data}
  recursive_utils.py: |
    def factorial(n):
        if n <= 1:
            return 1
        return n * factorial(n - 1)

    def fibonacci(n):
        if n <= 1:
            return n
        return fibonacci(n - 1) + fibonacci(n - 2)

    def gcd(a, b):
        if b == 0:
            return a
        return gcd(b, a % b)

    def binary_search(arr, target, low, high):
        if low > high:
            return -1
        mid = (low + high) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            return binary_search(arr, target, mid + 1, high)
        else:
            return binary_search(arr, target, low, mid - 1)
  tree_traversal.py: |
    class TreeNode:
        def __init__(self, value):
            self.value = value
            self.left = None
            self.right = None

    def inorder(node, result=None):
        if result is None:
            result = []
        if node:
            inorder(node.left, result)
            result.append(node.value)
            inorder(node.right, result)
        return result

    def preorder(node, result=None):
        if result is None:
            result = []
        if node:
            result.append(node.value)
            preorder(node.left, result)
            preorder(node.right, result)
        return result

    def postorder(node, result=None):
        if result is None:
            result = []
        if node:
            postorder(node.left, result)
            postorder(node.right, result)
            result.append(node.value)
        return result
  garbage/recursive_garbage_GARBAGE_GRAPH_hij001.py: |
    def ackermann_GARBAGE_GRAPH_hij001(m, n):
        if m == 0:
            return n + 1
        elif n == 0:
            return ackermann_GARBAGE_GRAPH_hij001(m - 1, 1)
        else:
            return ackermann_GARBAGE_GRAPH_hij001(m - 1, ackermann_GARBAGE_GRAPH_hij001(m, n - 1))

    def tower_of_hanoi_GARBAGE_GRAPH_hij001(n, source, target, auxiliary):
        if n == 1:
            return [(source, target)]
        moves = []
        moves.extend(tower_of_hanoi_GARBAGE_GRAPH_hij001(n - 1, source, auxiliary, target))
        moves.append((source, target))
        moves.extend(tower_of_hanoi_GARBAGE_GRAPH_hij001(n - 1, auxiliary, target, source))
        return moves
  garbage/mutual_recursion_GARBAGE_GRAPH_hij002.py: |
    def is_even_GARBAGE_GRAPH_hij002(n):
        if n == 0:
            return True
        return is_odd_GARBAGE_GRAPH_hij002(n - 1)

    def is_odd_GARBAGE_GRAPH_hij002(n):
        if n == 0:
            return False
        return is_even_GARBAGE_GRAPH_hij002(n - 1)
  garbage/nested_recursion_GARBAGE_GRAPH_hij003.py: |
    def nested_recurse_GARBAGE_GRAPH_hij003(n):
        if n <= 0:
            return 1
        return nested_recurse_GARBAGE_GRAPH_hij003(nested_recurse_GARBAGE_GRAPH_hij003(n - 1) - 1)
  # --- Tricky garbage: memoization pattern with cache but no reference to module.py ---
  garbage/memo_cache_GARBAGE_GRAPH_012.py: |
    GARBAGE_GRAPH_012_MEMOIZE_MODULE_A = "memoize_cache_v2"
    GARBAGE_GRAPH_012_MEMOIZE_FLAG_B = True

    def memoized_compute_GARBAGE_GRAPH_012(n, cache=None):
        if cache is None:
            cache = {}
        if n in cache:
            return cache[n]
        result = n * 2
        cache[n] = result
        return result

    def cached_fibonacci_GARBAGE_GRAPH_012(n):
        return memoized_compute_GARBAGE_GRAPH_012(n)
  # --- Tricky garbage: recursion helpers with similar structure ---
  garbage/recurse_helpers_GARBAGE_GRAPH_012.py: |
    GARBAGE_GRAPH_012_RECURSE_HELPER_C = "recursion_helper_module"
    GARBAGE_GRAPH_012_RECURSE_DEPTH_D = 100

    def should_stop_GARBAGE_GRAPH_012():
        return False

    def base_value_GARBAGE_GRAPH_012():
        return 0

    def memoized_lookup_GARBAGE_GRAPH_012(n, cache=None):
        if cache is None:
            cache = {}
        return cache.get(n, n)
changed:
  module.py: |
    def func():
        if should_recurse():
            return func()
        return base_case()

    def should_recurse():
        return False

    def base_case():
        return 0

    def memoized_func(n, cache=None):
        if cache is None:
            cache = {}
        if n in cache:
            return cache[n]
        result = func()
        cache[n] = result
        return result

    class RecursiveProcessor:
        def process(self, data, depth=0):
            if depth > 10:
                return self.finalize(data)
            transformed = self.transform(data)
            return self.process(transformed, depth + 1)

        def transform(self, data):
            return data * 2

        def finalize(self, data):
            return {"result": data}
  recursive_utils.py: |
    def factorial(n):
        if n <= 1:
            return 1
        return n * factorial(n - 1)

    def fibonacci(n):
        if n <= 1:
            return n
        return fibonacci(n - 1) + fibonacci(n - 2)

    def gcd(a, b):
        if b == 0:
            return a
        return gcd(b, a % b)

    def binary_search(arr, target, low, high):
        if low > high:
            return -1
        mid = (low + high) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            return binary_search(arr, target, mid + 1, high)
        else:
            return binary_search(arr, target, low, mid - 1)
  tree_traversal.py: |
    class TreeNode:
        def __init__(self, value):
            self.value = value
            self.left = None
            self.right = None

    def inorder(node, result=None):
        if result is None:
            result = []
        if node:
            inorder(node.left, result)
            result.append(node.value)
            inorder(node.right, result)
        return result

    def preorder(node, result=None):
        if result is None:
            result = []
        if node:
            result.append(node.value)
            preorder(node.left, result)
            preorder(node.right, result)
        return result

    def postorder(node, result=None):
        if result is None:
            result = []
        if node:
            postorder(node.left, result)
            postorder(node.right, result)
            result.append(node.value)
        return result
  garbage/recursive_garbage_GARBAGE_GRAPH_hij001.py: |
    def ackermann_GARBAGE_GRAPH_hij001(m, n):
        if m == 0:
            return n + 1
        elif n == 0:
            return ackermann_GARBAGE_GRAPH_hij001(m - 1, 1)
        else:
            return ackermann_GARBAGE_GRAPH_hij001(m - 1, ackermann_GARBAGE_GRAPH_hij001(m, n - 1))

    def tower_of_hanoi_GARBAGE_GRAPH_hij001(n, source, target, auxiliary):
        if n == 1:
            return [(source, target)]
        moves = []
        moves.extend(tower_of_hanoi_GARBAGE_GRAPH_hij001(n - 1, source, auxiliary, target))
        moves.append((source, target))
        moves.extend(tower_of_hanoi_GARBAGE_GRAPH_hij001(n - 1, auxiliary, target, source))
        return moves
  garbage/mutual_recursion_GARBAGE_GRAPH_hij002.py: |
    def is_even_GARBAGE_GRAPH_hij002(n):
        if n == 0:
            return True
        return is_odd_GARBAGE_GRAPH_hij002(n - 1)

    def is_odd_GARBAGE_GRAPH_hij002(n):
        if n == 0:
            return False
        return is_even_GARBAGE_GRAPH_hij002(n - 1)
  garbage/nested_recursion_GARBAGE_GRAPH_hij003.py: |
    def nested_recurse_GARBAGE_GRAPH_hij003(n):
        if n <= 0:
            return 1
        return nested_recurse_GARBAGE_GRAPH_hij003(nested_recurse_GARBAGE_GRAPH_hij003(n - 1) - 1)
  garbage/memo_cache_GARBAGE_GRAPH_012.py: |
    GARBAGE_GRAPH_012_MEMOIZE_MODULE_A = "memoize_cache_v2"
    GARBAGE_GRAPH_012_MEMOIZE_FLAG_B = True

    def memoized_compute_GARBAGE_GRAPH_012(n, cache=None):
        if cache is None:
            cache = {}
        if n in cache:
            return cache[n]
        result = n * 2
        cache[n] = result
        return result

    def cached_fibonacci_GARBAGE_GRAPH_012(n):
        return memoized_compute_GARBAGE_GRAPH_012(n)
  garbage/recurse_helpers_GARBAGE_GRAPH_012.py: |
    GARBAGE_GRAPH_012_RECURSE_HELPER_C = "recursion_helper_module"
    GARBAGE_GRAPH_012_RECURSE_DEPTH_D = 100

    def should_stop_GARBAGE_GRAPH_012():
        return False

    def base_value_GARBAGE_GRAPH_012():
        return 0

    def memoized_lookup_GARBAGE_GRAPH_012(n, cache=None):
        if cache is None:
            cache = {}
        return cache.get(n, n)

# === Assertions ===

must_include_files:
- module.py

must_include_content_from:
  module.py:
    - 'def memoized_func(n, cache=None):'
    - result = func()
    - cache[n] = result

must_not_include:
# Plain garbage markers
- mutual_recursion_GARBAGE_GRAPH_hij002.py
- nested_recursion_GARBAGE_GRAPH_hij003.py
- recursive_garbage_GARBAGE_GRAPH_hij001.py
# Tricky: memoization pattern with cache
- GARBAGE_GRAPH_012_MEMOIZE_MODULE_A
- GARBAGE_GRAPH_012_MEMOIZE_FLAG_B
# Tricky: recursion helpers with similar structure
- GARBAGE_GRAPH_012_RECURSE_HELPER_C
- GARBAGE_GRAPH_012_RECURSE_DEPTH_D

must_not_include_files:
- garbage/recursive_garbage_GARBAGE_GRAPH_hij001.py
- garbage/mutual_recursion_GARBAGE_GRAPH_hij002.py
- garbage/nested_recursion_GARBAGE_GRAPH_hij003.py
- garbage/memo_cache_GARBAGE_GRAPH_012.py
- garbage/recurse_helpers_GARBAGE_GRAPH_012.py

max_fragments: 6
max_files: 3
add_garbage_files: false
skip_garbage_check: true
