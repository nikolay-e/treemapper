name: go_018_function_type
repo:
  initial_files:
    internal/http/types.go: |
      package http

      type Request struct {
          Path    string
          Method  string
          Headers map[string]string
          Body    []byte
      }

      type Response struct {
          Status  int
          Headers map[string]string
          Body    []byte
      }

      type Handler func(req Request) Response
      type Middleware func(Handler) Handler
    internal/http/router.go: |
      package http

      type Router struct {
          handlers map[string]Handler
      }

      func NewRouter() *Router {
          return &Router{
              handlers: make(map[string]Handler),
          }
      }

      func (r *Router) Handle(path string, handler Handler) {
          r.handlers[path] = handler
      }
    internal/http/cors.go: |
      package http

      type CORSConfig struct {
          AllowedOrigins []string
          AllowedMethods []string
          AllowedHeaders []string
          MaxAge         int
      }

      func DefaultCORSConfig() *CORSConfig {
          return &CORSConfig{
              AllowedOrigins: []string{"*"},
              AllowedMethods: []string{"GET", "POST", "PUT", "DELETE"},
              AllowedHeaders: []string{"Content-Type", "Authorization"},
              MaxAge:         86400,
          }
      }
  changed_files:
    internal/http/types.go: |
      package http

      type Request struct {
          Path    string
          Method  string
          Headers map[string]string
          Body    []byte
      }

      type Response struct {
          Status  int
          Headers map[string]string
          Body    []byte
      }

      type Handler func(req Request) Response
      type Middleware func(Handler) Handler
    internal/http/router.go: |
      package http

      import (
          "log"
          "time"
      )

      type Router struct {
          handlers    map[string]Handler
          middlewares []Middleware
          logger      *log.Logger
      }

      func NewRouter(logger *log.Logger) *Router {
          return &Router{
              handlers: make(map[string]Handler),
              logger:   logger,
          }
      }

      func (r *Router) Use(m Middleware) {
          r.middlewares = append(r.middlewares, m)
      }

      func (r *Router) Handle(path string, handler Handler) {
          wrapped := r.applyMiddlewares(handler)
          r.handlers[path] = wrapped
      }

      func (r *Router) applyMiddlewares(h Handler) Handler {
          for i := len(r.middlewares) - 1; i >= 0; i-- {
              h = r.middlewares[i](h)
          }
          return h
      }

      func (r *Router) ServeHTTP(req Request) Response {
          handler, ok := r.handlers[req.Path]
          if !ok {
              return Response{Status: 404, Body: []byte("Not Found")}
          }
          return handler(req)
      }

      func LoggingMiddleware(logger *log.Logger) Middleware {
          return func(next Handler) Handler {
              return func(req Request) Response {
                  start := time.Now()
                  resp := next(req)
                  logger.Printf("%s %s - %d (%v)", req.Method, req.Path, resp.Status, time.Since(start))
                  return resp
              }
          }
      }

      func RecoveryMiddleware(logger *log.Logger) Middleware {
          return func(next Handler) Handler {
              return func(req Request) (resp Response) {
                  defer func() {
                      if r := recover(); r != nil {
                          logger.Printf("Panic recovered: %v", r)
                          resp = Response{Status: 500, Body: []byte("Internal Server Error")}
                      }
                  }()
                  return next(req)
              }
          }
      }

      func AuthMiddleware(validateToken func(string) bool) Middleware {
          return func(next Handler) Handler {
              return func(req Request) Response {
                  token := req.Headers["Authorization"]
                  if token == "" || !validateToken(token) {
                      return Response{Status: 401, Body: []byte("Unauthorized")}
                  }
                  return next(req)
              }
          }
      }
    internal/http/cors.go: |
      package http

      type CORSConfig struct {
          AllowedOrigins []string
          AllowedMethods []string
          AllowedHeaders []string
          MaxAge         int
      }

      func DefaultCORSConfig() *CORSConfig {
          return &CORSConfig{
              AllowedOrigins: []string{"*"},
              AllowedMethods: []string{"GET", "POST", "PUT", "DELETE"},
              AllowedHeaders: []string{"Content-Type", "Authorization"},
              MaxAge:         86400,
          }
      }
  commit_message: Update files
fragments:
- id: router_file
  selector:
    path: internal/http/router.go
- id: router_uses_middleware_type
  selector:
    anchor: func (r *Router) Use(m Middleware) {
- id: router_uses_handler_type
  selector:
    anchor: func (r *Router) applyMiddlewares(h Handler) Handler {
- id: types_handler_definition
  selector:
    path: internal/http/types.go
    anchor: type Handler func(req Request) Response
- id: types_middleware_definition
  selector:
    anchor: type Middleware func(Handler) Handler
- id: no_cors_config
  selector:
    path: internal/http/cors.go
    anchor: type CORSConfig struct {
- id: no_cors_default
  selector:
    anchor: func DefaultCORSConfig() *CORSConfig {
oracle:
  required:
  - router_file
  - router_uses_middleware_type
  - router_uses_handler_type
  - types_handler_definition
  - types_middleware_definition
  allowed: []
  forbidden:
  - no_cors_config
  - no_cors_default
