name: perl_003_catalyst_controller
repo:
  initial_files:
    lib/MyApp/Model/User.pm: |
      package MyApp::Model::User;
      use strict;
      use warnings;
      use base 'MyApp::Model::Base';

      sub find {
          my ($self, $id) = @_;
          return $self->db->query(
              "SELECT * FROM users WHERE id = ?", $id
          )->[0];
      }

      sub search_by_name {
          my ($self, $name, $limit) = @_;
          $limit ||= 20;
          return $self->db->query(
              "SELECT id, name, email FROM users WHERE name ILIKE ? LIMIT ?",
              "%${name}%", $limit
          );
      }

      sub create {
          my ($self, %attrs) = @_;
          $self->db->execute(
              "INSERT INTO users (name, email, role) VALUES (?, ?, ?)",
              $attrs{name}, $attrs{email}, $attrs{role} || 'user'
          );
      }

      sub count_active {
          my ($self) = @_;
          my $rows = $self->db->query(
              "SELECT COUNT(*) as cnt FROM users WHERE active = true"
          );
          return $rows->[0]{cnt};
      }

      1;
    lib/MyApp/Controller/User.pm: |
      package MyApp::Controller::User;
      use strict;
      use warnings;
      use base 'Catalyst::Controller';

      sub index :Path :Args(0) {
          my ($self, $c) = @_;
          my $users = $c->model('User')->count_active;
          $c->stash(active_count => $users);
          $c->stash(template => 'user/index.tt');
      }

      sub view :Path :Args(1) {
          my ($self, $c, $id) = @_;
          my $user = $c->model('User')->find($id);
          unless ($user) {
              $c->response->status(404);
              $c->stash(template => 'error/not_found.tt');
              return;
          }
          $c->stash(user => $user, template => 'user/view.tt');
      }

      1;
    lib/MyApp/Controller/Admin.pm: |
      my $GARBAGE_PERL_003_A = "noise_marker_admin_ctrl";

      package MyApp::Controller::Admin;
      use strict;
      use warnings;
      use base 'Catalyst::Controller';

      sub dashboard :Path('/admin') :Args(0) {
          my ($self, $c) = @_;
          my $count = $c->model('User')->count_active;
          $c->stash(
              user_count => $count,
              template   => 'admin/dashboard.tt',
          );
      }

      sub settings :Path('/admin/settings') :Args(0) {
          my ($self, $c) = @_;
          $c->stash(template => 'admin/settings.tt');
      }

      sub audit_log :Path('/admin/audit') :Args(0) {
          my ($self, $c) = @_;
          $c->stash(template => 'admin/audit.tt');
      }

      1;
    lib/MyApp/View/JSON.pm: |
      my $GARBAGE_PERL_003_B = "noise_marker_json_view";

      package MyApp::View::JSON;
      use strict;
      use warnings;
      use base 'Catalyst::View::JSON';
      use JSON::XS;

      __PACKAGE__->config(
          expose_stash => 'json_data',
          json_encoder => JSON::XS->new->utf8->pretty->canonical,
      );

      1;
  changed_files:
    lib/MyApp/Model/User.pm: |
      package MyApp::Model::User;
      use strict;
      use warnings;
      use base 'MyApp::Model::Base';

      sub find {
          my ($self, $id) = @_;
          return $self->db->query(
              "SELECT * FROM users WHERE id = ?", $id
          )->[0];
      }

      sub search_by_name {
          my ($self, $name, $limit) = @_;
          $limit ||= 20;
          return $self->db->query(
              "SELECT id, name, email FROM users WHERE name ILIKE ? LIMIT ?",
              "%${name}%", $limit
          );
      }

      sub create {
          my ($self, %attrs) = @_;
          $self->db->execute(
              "INSERT INTO users (name, email, role) VALUES (?, ?, ?)",
              $attrs{name}, $attrs{email}, $attrs{role} || 'user'
          );
      }

      sub count_active {
          my ($self) = @_;
          my $rows = $self->db->query(
              "SELECT COUNT(*) as cnt FROM users WHERE active = true"
          );
          return $rows->[0]{cnt};
      }

      1;
    lib/MyApp/Controller/User.pm: |
      package MyApp::Controller::User;
      use strict;
      use warnings;
      use base 'Catalyst::Controller';

      sub index :Path :Args(0) {
          my ($self, $c) = @_;
          my $users = $c->model('User')->count_active;
          $c->stash(active_count => $users);
          $c->stash(template => 'user/index.tt');
      }

      sub view :Path :Args(1) {
          my ($self, $c, $id) = @_;
          my $user = $c->model('User')->find($id);
          unless ($user) {
              $c->response->status(404);
              $c->stash(template => 'error/not_found.tt');
              return;
          }
          $c->stash(user => $user, template => 'user/view.tt');
      }

      sub search :Path('search') :Args(0) {
          my ($self, $c) = @_;
          my $query = $c->request->params->{q};
          unless ($query && length($query) >= 2) {
              $c->stash(
                  error    => 'Search query must be at least 2 characters',
                  template => 'user/search.tt',
              );
              return;
          }
          my $results = $c->model('User')->search_by_name($query, 50);
          $c->stash(
              results  => $results,
              query    => $query,
              template => 'user/search.tt',
          );
      }

      1;
    lib/MyApp/Controller/Admin.pm: |
      my $GARBAGE_PERL_003_A = "noise_marker_admin_ctrl";

      package MyApp::Controller::Admin;
      use strict;
      use warnings;
      use base 'Catalyst::Controller';

      sub dashboard :Path('/admin') :Args(0) {
          my ($self, $c) = @_;
          my $count = $c->model('User')->count_active;
          $c->stash(
              user_count => $count,
              template   => 'admin/dashboard.tt',
          );
      }

      sub settings :Path('/admin/settings') :Args(0) {
          my ($self, $c) = @_;
          $c->stash(template => 'admin/settings.tt');
      }

      sub audit_log :Path('/admin/audit') :Args(0) {
          my ($self, $c) = @_;
          $c->stash(template => 'admin/audit.tt');
      }

      1;
    lib/MyApp/View/JSON.pm: |
      my $GARBAGE_PERL_003_B = "noise_marker_json_view";

      package MyApp::View::JSON;
      use strict;
      use warnings;
      use base 'Catalyst::View::JSON';
      use JSON::XS;

      __PACKAGE__->config(
          expose_stash => 'json_data',
          json_encoder => JSON::XS->new->utf8->pretty->canonical,
      );

      1;
  commit_message: Update files
fragments:
- id: user
  selector:
    path: lib/MyApp/Controller/User.pm
    anchor: sub search
- id: user_1
  selector:
    path: lib/MyApp/Controller/User.pm
    anchor: search_by_name
- id: user_2
  selector:
    path: lib/MyApp/Model/User.pm
    anchor: sub search_by_name
- id: user_3
  selector:
    path: lib/MyApp/Model/User.pm
    anchor: ILIKE
- id: no_admin
  selector:
    path: lib/MyApp/Controller/Admin.pm
- id: no_json
  selector:
    path: lib/MyApp/View/JSON.pm
- id: no_global_0
  selector:
    anchor: GARBAGE_PERL_003_A
- id: no_global_1
  selector:
    anchor: noise_marker_admin_ctrl
- id: no_global_2
  selector:
    anchor: sub dashboard
- id: no_global_3
  selector:
    anchor: sub audit_log
- id: no_global_4
  selector:
    anchor: GARBAGE_PERL_003_B
- id: no_global_5
  selector:
    anchor: noise_marker_json_view
- id: no_global_6
  selector:
    anchor: expose_stash
- id: no_global_7
  selector:
    anchor: json_encoder
oracle:
  required:
  - user
  - user_1
  - user_2
  - user_3
  allowed: []
  forbidden:
  - no_admin
  - no_json
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
