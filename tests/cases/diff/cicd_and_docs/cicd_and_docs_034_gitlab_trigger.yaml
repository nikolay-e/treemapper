name: cicd_and_docs_034_gitlab_trigger
repo:
  initial_files:
    .gitlab-ci.yml: |
      stages:
        - build
        - deploy

      build:
        stage: build
        script:
          - npm ci
          - npm run build

      deploy:
        stage: deploy
        script:
          - ./deploy.sh
    deploy.sh: |
      #!/bin/bash
      set -e
      echo "Running local deploy..."
      rsync -avz dist/ server:/app/
    package.json: |
      {
        "name": "gitlab-trigger-app",
        "version": "1.0.0",
        "scripts": {
          "build": "esbuild src/index.ts --bundle --outdir=dist"
        }
      }
    garbage_pipeline_orchestrator.ts: |
      const GARBAGE_CICD_DOCS_034_ORCHESTRATOR_MARKER_A = "dag";
      const GARBAGE_CICD_DOCS_034_QUEUE_MARKER_B = "rabbitmq";

      class GarbagePipelineOrchestrator {
        static GARBAGE_CICD_DOCS_034_ASYNC_MARKER_C = true;
        orchestrate(): void {}
      }
    garbage_pipeline_monitor.ts: |
      const GARBAGE_CICD_DOCS_034_MONITOR_MARKER_D = "prometheus";
      const GARBAGE_CICD_DOCS_034_ALERT_MARKER_E = "slack";

      class GarbagePipelineMonitor {
        static GARBAGE_CICD_DOCS_034_INTERVAL_MARKER_F = 30;
        monitor(): void {}
      }
  changed_files:
    .gitlab-ci.yml: |
      stages:
        - build
        - deploy

      build:
        stage: build
        script:
          - npm ci
          - npm run build

      deploy:
        stage: deploy
        trigger:
          project: team/deploy
          branch: main
          strategy: depend
    deploy.sh: |
      #!/bin/bash
      set -e
      echo "Running local deploy..."
      rsync -avz dist/ server:/app/
    package.json: |
      {
        "name": "gitlab-trigger-app",
        "version": "1.0.0",
        "scripts": {
          "build": "esbuild src/index.ts --bundle --outdir=dist"
        }
      }
    garbage_pipeline_orchestrator.ts: |
      const GARBAGE_CICD_DOCS_034_ORCHESTRATOR_MARKER_A = "dag";
      const GARBAGE_CICD_DOCS_034_QUEUE_MARKER_B = "rabbitmq";

      class GarbagePipelineOrchestrator {
        static GARBAGE_CICD_DOCS_034_ASYNC_MARKER_C = true;
        orchestrate(): void {}
      }
    garbage_pipeline_monitor.ts: |
      const GARBAGE_CICD_DOCS_034_MONITOR_MARKER_D = "prometheus";
      const GARBAGE_CICD_DOCS_034_ALERT_MARKER_E = "slack";

      class GarbagePipelineMonitor {
        static GARBAGE_CICD_DOCS_034_INTERVAL_MARKER_F = 30;
        monitor(): void {}
      }
  commit_message: Update files
fragments:
- id: gitlab_ci_file
  selector:
    path: .gitlab-ci.yml
- id: req_global_0
  selector:
    anchor: 'project: team/deploy'
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_034_ALERT_MARKER_E
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_034_ASYNC_MARKER_C
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_034_INTERVAL_MARKER_F
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_034_MONITOR_MARKER_D
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_034_ORCHESTRATOR_MARKER_A
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_034_QUEUE_MARKER_B
- id: no_global_6
  selector:
    anchor: garbage_pipeline_monitor.ts
- id: no_global_7
  selector:
    anchor: garbage_pipeline_orchestrator.ts
oracle:
  required:
  - gitlab_ci_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
