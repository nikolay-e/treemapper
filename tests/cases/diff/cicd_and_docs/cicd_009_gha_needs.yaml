name: cicd_009_gha_needs
initial:
  .github/workflows/ci.yml: |
    name: CI
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - run: npm run build
      test:
        runs-on: ubuntu-latest
        steps:
          - run: npm test
      deploy:
        runs-on: ubuntu-latest
        steps:
          - run: ./deploy.sh
changed:
  .github/workflows/ci.yml: |
    name: CI
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - run: npm run build
      test:
        runs-on: ubuntu-latest
        steps:
          - run: npm test
      deploy:
        runs-on: ubuntu-latest
        needs: [build, test]
        steps:
          - run: ./deploy.sh
assertions:
  must_include:
  - ci.yml
  must_not_include:
  - garbage_marker_12345
  - unused_marker_67890
