name: cicd_and_docs_047_architecture_module_mention
repo:
  initial_files:
    docs/architecture.md: |
      # Architecture

      ## Overview

      The system consists of several modules:

      - `auth` - Authentication
      - `api` - REST API endpoints
    src/auth/login.py: |
      def login(username, password):
          return {"token": "abc123"}
    src/api/routes.py: |
      from auth.login import login

      def setup_routes(app):
          app.post("/login", login)
    src/api/__init__.py: |
      from .routes import setup_routes
    garbage_arch_analyzer.ts: |
      const GARBAGE_CICD_DOCS_047_ANALYZER_MARKER_A = "madge";
      const GARBAGE_CICD_DOCS_047_FORMAT_MARKER_B = "svg";

      class GarbageArchAnalyzer {
        static GARBAGE_CICD_DOCS_047_CIRCULAR_MARKER_C = true;
        analyze(): void {}
      }
    garbage_arch_validator.ts: |
      const GARBAGE_CICD_DOCS_047_VALIDATOR_MARKER_D = "dependency-cruiser";
      const GARBAGE_CICD_DOCS_047_RULES_MARKER_E = ".dependency-cruiser.js";

      class GarbageArchValidator {
        static GARBAGE_CICD_DOCS_047_STRICT_MARKER_F = false;
        validate(): boolean { return true; }
      }
  changed_files:
    docs/architecture.md: |
      # Architecture

      ## Overview

      The system consists of several modules:

      - `auth` - Authentication and authorization
      - `api` - REST API endpoints
      - `cache` - Redis caching layer
      - `events` - Event publishing and handling

      ## Module Dependencies

      ```
      api -> auth -> cache
      api -> events
      ```

      ## Data Flow

      1. Request hits `api` module
      2. `auth` validates token
      3. `cache` checks for cached response
      4. `events` publishes audit event
    src/auth/login.py: |
      def login(username, password):
          return {"token": "abc123"}
    src/api/routes.py: |
      from auth.login import login

      def setup_routes(app):
          app.post("/login", login)
    src/api/__init__.py: |
      from .routes import setup_routes
    src/cache/__init__.py: |
      import redis

      client = redis.Redis()

      def get_cached(key):
          return client.get(key)

      def set_cached(key, value, ttl=3600):
          client.setex(key, ttl, value)
    src/events/__init__.py: |
      from typing import Callable

      handlers = {}

      def publish(event_name, data):
          for handler in handlers.get(event_name, []):
              handler(data)

      def subscribe(event_name, handler: Callable):
          handlers.setdefault(event_name, []).append(handler)
    garbage_arch_analyzer.ts: |
      const GARBAGE_CICD_DOCS_047_ANALYZER_MARKER_A = "madge";
      const GARBAGE_CICD_DOCS_047_FORMAT_MARKER_B = "svg";

      class GarbageArchAnalyzer {
        static GARBAGE_CICD_DOCS_047_CIRCULAR_MARKER_C = true;
        analyze(): void {}
      }
    garbage_arch_validator.ts: |
      const GARBAGE_CICD_DOCS_047_VALIDATOR_MARKER_D = "dependency-cruiser";
      const GARBAGE_CICD_DOCS_047_RULES_MARKER_E = ".dependency-cruiser.js";

      class GarbageArchValidator {
        static GARBAGE_CICD_DOCS_047_STRICT_MARKER_F = false;
        validate(): boolean { return true; }
      }
  commit_message: Update files
fragments:
- id: architecture_file
  selector:
    path: docs/architecture.md
- id: init_file
  selector:
    path: src/cache/__init__.py
- id: init_file_1
  selector:
    path: src/events/__init__.py
- id: req_global_0
  selector:
    anchor: '- `auth` - Authentication and authorization'
- id: req_global_1
  selector:
    anchor: api -> auth -> cache
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_047_ANALYZER_MARKER_A
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_047_CIRCULAR_MARKER_C
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_047_FORMAT_MARKER_B
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_047_RULES_MARKER_E
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_047_STRICT_MARKER_F
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_047_VALIDATOR_MARKER_D
- id: no_global_6
  selector:
    anchor: garbage_arch_analyzer.ts
- id: no_global_7
  selector:
    anchor: garbage_arch_validator.ts
oracle:
  required:
  - architecture_file
  - init_file
  - init_file_1
  - req_global_0
  - req_global_1
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
