name: cicd_022_gitlab_image
initial:
  .gitlab-ci.yml: |
    build:
      script:
        - npm run build
changed:
  .gitlab-ci.yml: |
    image: node:18-alpine

    build:
      script:
        - npm ci
        - npm run build
assertions:
  must_include:
  - .gitlab-ci.yml
  must_not_include:
  - garbage_marker_12345
  - unused_marker_67890
