name: algorithm_012_merge_multiple_scattered_changes
tags:
- lang:python
- change:single_file
- change:new_function
- context:same_file
- distance:0hop
- difficulty:easy
repo:
  initial_files:
    src/core/application.py: |
      import os
      import logging
      from typing import Dict, Any, Optional

      APP_NAME = "MyApplication"
      VERSION = "1.0.0"
      DEBUG = False

      logger = logging.getLogger(__name__)

      def initialize_config() -> Dict[str, Any]:
          return {
              "name": APP_NAME,
              "version": VERSION,
              "debug": DEBUG,
          }

      def setup_logging(level: str = "INFO") -> None:
          logging.basicConfig(level=getattr(logging, level))
          logger.info(f"Logging initialized at {level} level")

      def validate_environment() -> bool:
          required = ["DATABASE_URL", "SECRET_KEY"]
          missing = [var for var in required if not os.getenv(var)]
          if missing:
              logger.error(f"Missing environment variables: {missing}")
              return False
          return True

      class ApplicationContext:
          def __init__(self, config: Dict[str, Any]):
              self.config = config
              self.initialized = False

          def start(self) -> None:
              if not self.initialized:
                  setup_logging()
                  self.initialized = True

          def get_setting(self, key: str) -> Optional[Any]:
              return self.config.get(key)
    src/core/worker_application.py: |
      import os
      import logging
      from typing import Dict, Any, Optional, List
      GARBAGE_ALGO_012_WORKER_APP_G = "worker"
      GARBAGE_ALGO_012_WORKER_VERSION_H = "1.0.0"

      APP_NAME = "WorkerApplication"
      VERSION = "1.0.0"
      DEBUG = os.getenv("WORKER_DEBUG", "false").lower() == "true"

      logger = logging.getLogger(__name__)

      def initialize_config() -> Dict[str, Any]:
          return {
              "name": APP_NAME,
              "version": VERSION,
              "debug": DEBUG,
              "environment": os.getenv("ENVIRONMENT", "development"),
          }

      class ApplicationContext:
          def __init__(self, config: Dict[str, Any]):
              self.config = config
              self.initialized = False
              self.plugins: List[str] = []

          def start(self) -> None:
              if not self.initialized:
                  self.initialized = True
                  logger.info(f"Application {APP_NAME} v{VERSION} started")

          def get_setting(self, key: str, default: Any = None) -> Optional[Any]:
              return self.config.get(key, default)
    src/core/scheduler.py: |
      import os
      import logging
      from typing import Dict, Any, Optional
      GARBAGE_ALGO_012_SCHEDULER_MARKER_I = "scheduler"
      GARBAGE_ALGO_012_CRON_MARKER_J = True

      APP_NAME = "SchedulerService"
      VERSION = "2.0.0"
      logger = logging.getLogger(__name__)

      def setup_logging(level: str = "INFO") -> None:
          logging.basicConfig(
              level=getattr(logging, level),
              format="%(asctime)s - %(name)s - %(levelname)s - %(message)s"
          )
          logger.info(f"Logging initialized at {level} level")

      def validate_environment() -> bool:
          required = ["SCHEDULER_URL", "API_KEY"]
          missing = [var for var in required if not os.getenv(var)]
          if missing:
              logger.error(f"Missing environment variables: {missing}")
              return False
          return True
    src/core/middleware.py: |
      import logging
      from typing import Dict, Any, Optional, List
      GARBAGE_ALGO_012_MIDDLEWARE_MARKER_K = "middleware"
      GARBAGE_ALGO_012_CHAIN_MARKER_L = True

      logger = logging.getLogger(__name__)

      class MiddlewareContext:
          def __init__(self, config: Dict[str, Any]):
              self.config = config
              self.initialized = False
              self.plugins: List[str] = []

          def start(self) -> None:
              if not self.initialized:
                  self.initialized = True

          def get_setting(self, key: str, default: Any = None) -> Optional[Any]:
              return self.config.get(key, default)
    garbage_plugins.py: |
      GARBAGE_ALGO_012_PLUGIN_MARKER_A = "plugin"
      GARBAGE_ALGO_012_LOADER_MARKER_B = True

      class PluginLoader:
          GARBAGE_ALGO_012_REGISTRY_MARKER_C = {}

          def load(self, name: str) -> None:
              pass
    garbage_extensions.py: |
      GARBAGE_ALGO_012_EXT_MARKER_D = "extension"
      GARBAGE_ALGO_012_HOOK_MARKER_E = []

      class ExtensionManager:
          GARBAGE_ALGO_012_ACTIVE_MARKER_F = True

          def register(self, ext: str) -> None:
              pass
  changed_files:
    src/core/application.py: |
      import os
      import logging
      from typing import Dict, Any, Optional, List

      APP_NAME = "MyApplication"
      VERSION = "2.0.0"
      DEBUG = os.getenv("DEBUG", "false").lower() == "true"

      logger = logging.getLogger(__name__)

      def initialize_config() -> Dict[str, Any]:
          return {
              "name": APP_NAME,
              "version": VERSION,
              "debug": DEBUG,
              "environment": os.getenv("ENVIRONMENT", "development"),
          }

      def setup_logging(level: str = "INFO") -> None:
          logging.basicConfig(
              level=getattr(logging, level),
              format="%(asctime)s - %(name)s - %(levelname)s - %(message)s"
          )
          logger.info(f"Logging initialized at {level} level")

      def validate_environment() -> bool:
          required = ["DATABASE_URL", "SECRET_KEY"]
          missing = [var for var in required if not os.getenv(var)]
          if missing:
              logger.error(f"Missing environment variables: {missing}")
              return False
          return True

      class ApplicationContext:
          def __init__(self, config: Dict[str, Any]):
              self.config = config
              self.initialized = False
              self.plugins: List[str] = []

          def start(self) -> None:
              if not self.initialized:
                  setup_logging(self.config.get("log_level", "INFO"))
                  self.initialized = True
                  logger.info(f"Application {APP_NAME} v{VERSION} started")

          def get_setting(self, key: str, default: Any = None) -> Optional[Any]:
              return self.config.get(key, default)
    src/core/worker_application.py: |
      import os
      import logging
      from typing import Dict, Any, Optional, List
      GARBAGE_ALGO_012_WORKER_APP_G = "worker"
      GARBAGE_ALGO_012_WORKER_VERSION_H = "1.0.0"

      APP_NAME = "WorkerApplication"
      VERSION = "1.0.0"
      DEBUG = os.getenv("WORKER_DEBUG", "false").lower() == "true"

      logger = logging.getLogger(__name__)

      def initialize_config() -> Dict[str, Any]:
          return {
              "name": APP_NAME,
              "version": VERSION,
              "debug": DEBUG,
              "environment": os.getenv("ENVIRONMENT", "development"),
          }

      class ApplicationContext:
          def __init__(self, config: Dict[str, Any]):
              self.config = config
              self.initialized = False
              self.plugins: List[str] = []

          def start(self) -> None:
              if not self.initialized:
                  self.initialized = True
                  logger.info(f"Application {APP_NAME} v{VERSION} started")

          def get_setting(self, key: str, default: Any = None) -> Optional[Any]:
              return self.config.get(key, default)
    src/core/scheduler.py: |
      import os
      import logging
      from typing import Dict, Any, Optional
      GARBAGE_ALGO_012_SCHEDULER_MARKER_I = "scheduler"
      GARBAGE_ALGO_012_CRON_MARKER_J = True

      APP_NAME = "SchedulerService"
      VERSION = "2.0.0"
      logger = logging.getLogger(__name__)

      def setup_logging(level: str = "INFO") -> None:
          logging.basicConfig(
              level=getattr(logging, level),
              format="%(asctime)s - %(name)s - %(levelname)s - %(message)s"
          )
          logger.info(f"Logging initialized at {level} level")

      def validate_environment() -> bool:
          required = ["SCHEDULER_URL", "API_KEY"]
          missing = [var for var in required if not os.getenv(var)]
          if missing:
              logger.error(f"Missing environment variables: {missing}")
              return False
          return True
    src/core/middleware.py: |
      import logging
      from typing import Dict, Any, Optional, List
      GARBAGE_ALGO_012_MIDDLEWARE_MARKER_K = "middleware"
      GARBAGE_ALGO_012_CHAIN_MARKER_L = True

      logger = logging.getLogger(__name__)

      class MiddlewareContext:
          def __init__(self, config: Dict[str, Any]):
              self.config = config
              self.initialized = False
              self.plugins: List[str] = []

          def start(self) -> None:
              if not self.initialized:
                  self.initialized = True

          def get_setting(self, key: str, default: Any = None) -> Optional[Any]:
              return self.config.get(key, default)
    garbage_plugins.py: |
      GARBAGE_ALGO_012_PLUGIN_MARKER_A = "plugin"
      GARBAGE_ALGO_012_LOADER_MARKER_B = True

      class PluginLoader:
          GARBAGE_ALGO_012_REGISTRY_MARKER_C = {}

          def load(self, name: str) -> None:
              pass
    garbage_extensions.py: |
      GARBAGE_ALGO_012_EXT_MARKER_D = "extension"
      GARBAGE_ALGO_012_HOOK_MARKER_E = []

      class ExtensionManager:
          GARBAGE_ALGO_012_ACTIVE_MARKER_F = True

          def register(self, ext: str) -> None:
              pass
  commit_message: Update files
fragments:
- id: application
  selector:
    path: src/core/application.py
    anchor: from typing import Dict, Any, Optional, List
- id: application_1
  selector:
    path: src/core/application.py
    anchor: '"environment": os.getenv("ENVIRONMENT", "development"),'
- id: application_2
  selector:
    path: src/core/application.py
    anchor: VERSION = "2.0.0"
- id: application_3
  selector:
    path: src/core/application.py
    anchor: 'self.plugins: List[str] = []'
- id: application_4
  selector:
    path: src/core/application.py
    anchor: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
- id: no_garbage_plugins
  selector:
    path: garbage_plugins.py
- id: no_garbage_extensions
  selector:
    path: garbage_extensions.py
- id: no_worker_application
  selector:
    path: src/core/worker_application.py
- id: no_scheduler
  selector:
    path: src/core/scheduler.py
- id: no_middleware
  selector:
    path: src/core/middleware.py
- id: no_global_0
  selector:
    anchor: GARBAGE_ALGO_012_PLUGIN_MARKER_A
- id: no_global_1
  selector:
    anchor: GARBAGE_ALGO_012_LOADER_MARKER_B
- id: no_global_2
  selector:
    anchor: GARBAGE_ALGO_012_REGISTRY_MARKER_C
- id: no_global_3
  selector:
    anchor: GARBAGE_ALGO_012_EXT_MARKER_D
- id: no_global_4
  selector:
    anchor: GARBAGE_ALGO_012_HOOK_MARKER_E
- id: no_global_5
  selector:
    anchor: GARBAGE_ALGO_012_ACTIVE_MARKER_F
- id: no_global_6
  selector:
    anchor: GARBAGE_ALGO_012_WORKER_APP_G
- id: no_global_7
  selector:
    anchor: GARBAGE_ALGO_012_WORKER_VERSION_H
- id: no_global_8
  selector:
    anchor: GARBAGE_ALGO_012_SCHEDULER_MARKER_I
- id: no_global_9
  selector:
    anchor: GARBAGE_ALGO_012_CRON_MARKER_J
- id: no_global_10
  selector:
    anchor: GARBAGE_ALGO_012_MIDDLEWARE_MARKER_K
- id: no_global_11
  selector:
    anchor: GARBAGE_ALGO_012_CHAIN_MARKER_L
oracle:
  required:
  - application
  - application_1
  - application_2
  - application_3
  - application_4
  allowed: []
  forbidden:
  - no_garbage_plugins
  - no_garbage_extensions
  - no_worker_application
  - no_scheduler
  - no_middleware
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
  - no_global_8
  - no_global_9
  - no_global_10
  - no_global_11
