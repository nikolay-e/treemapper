chart/Chart.yaml: |
  apiVersion: v2
  name: svc-app
  version: 1.0.0
chart/values.yaml: |
  service:
    type: ClusterIP
    port: 80
chart/templates/service.yaml: |
  apiVersion: v1
  kind: Service
  metadata:
    name: myapp-service
  spec:
    type: ClusterIP
    ports:
      - port: 80
chart/templates/gatekeeper-constraint.yaml: |
  apiVersion: constraints.gatekeeper.sh/v1beta1
  kind: GARBAGE_HELM_023_CONSTRAINT_KIND_A
  metadata:
    name: GARBAGE_HELM_023_CONSTRAINT_NAME_B
  spec:
    match:
      kinds:
        - apiGroups: [GARBAGE_HELM_023_API_GROUP_C]
chart/docs/service-mesh.md: |
  # Service Mesh Configuration
  GARBAGE_HELM_023_MESH_DOC_D
  ## Traffic Routing
  GARBAGE_HELM_023_ROUTING_DOC_E
