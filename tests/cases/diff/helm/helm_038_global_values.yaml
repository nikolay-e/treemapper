name: helm_038_global_values
repo:
  initial_files:
    values.yaml: |
      image:
        repository: myapp
    unrelated.txt: |
      garbage_marker_12345
      unused_marker_67890
  changed_files:
    values.yaml: |
      global:
        imageRegistry: registry.example.com
        imagePullSecrets:
          - name: registry-secret

      image:
        repository: myapp
    unrelated.txt: |
      garbage_marker_12345
      unused_marker_67890
  commit_message: Update files
fragments:
- id: values_file
  selector:
    path: values.yaml
- id: req_global_0
  selector:
    anchor: 'imageRegistry: registry.example.com'
- id: no_global_0
  selector:
    anchor: unrelated.txt
oracle:
  required:
  - values_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
