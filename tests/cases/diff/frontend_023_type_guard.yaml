name: frontend_023_type_guard
tags:
- lang:typescript
- change:single_file
- context:import_chain
- distance:2hop
- difficulty:medium
repo:
  initial_files:
    src/entities.ts: |
      export interface User {
          type: 'user';
          id: string;
          name: string;
          email: string;
      }

      export interface Admin extends User {
          type: 'admin';
          permissions: string[];
      }

      export interface Guest {
          type: 'guest';
          sessionId: string;
      }
    src/guards.ts: |
      console.log('initial');
    src/validators/input.ts: |
      export function isValidEmail(input: unknown): input is string {
          return typeof input === 'string' && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(input);
      }

      export function isPositiveNumber(input: unknown): input is number {
          return typeof input === 'number' && input > 0;
      }
    src/types/animal.ts: |
      export interface Dog {
          kind: 'dog';
          name: string;
          breed: string;
      }

      export interface Cat {
          kind: 'cat';
          name: string;
          indoor: boolean;
      }

      export type Animal = Dog | Cat;

      export function isDog(animal: Animal): animal is Dog {
          return animal.kind === 'dog';
      }
  changed_files:
    src/entities.ts: |
      export interface User {
          type: 'user';
          id: string;
          name: string;
          email: string;
      }

      export interface Admin extends User {
          type: 'admin';
          permissions: string[];
      }

      export interface Guest {
          type: 'guest';
          sessionId: string;
      }
    src/guards.ts: |
      import { User, Admin, Guest } from './entities';

      export type Person = User | Admin | Guest;

      export function isUser(obj: unknown): obj is User {
          return typeof obj === 'object' && obj !== null &&
              'type' in obj && (obj as User).type === 'user';
      }

      export function isAdmin(person: Person): person is Admin {
          return person.type === 'admin';
      }

      export function isGuest(person: Person): person is Guest {
          return person.type === 'guest';
      }

      export function hasEmail(person: Person): person is User | Admin {
          return 'email' in person;
      }
    src/validators/input.ts: |
      export function isValidEmail(input: unknown): input is string {
          return typeof input === 'string' && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(input);
      }

      export function isPositiveNumber(input: unknown): input is number {
          return typeof input === 'number' && input > 0;
      }
    src/types/animal.ts: |
      export interface Dog {
          kind: 'dog';
          name: string;
          breed: string;
      }

      export interface Cat {
          kind: 'cat';
          name: string;
          indoor: boolean;
      }

      export type Animal = Dog | Cat;

      export function isDog(animal: Animal): animal is Dog {
          return animal.kind === 'dog';
      }
  commit_message: Update files
fragments:
- id: entities
  selector:
    path: src/entities.ts
    anchor: interface User {
- id: entities_1
  selector:
    path: src/entities.ts
    anchor: interface Admin extends User {
- id: entities_2
  selector:
    path: src/entities.ts
    anchor: interface Guest {
- id: no_input
  selector:
    path: src/validators/input.ts
- id: no_animal
  selector:
    path: src/types/animal.ts
- id: no_global_0
  selector:
    anchor: isValidEmail
- id: no_global_1
  selector:
    anchor: isPositiveNumber
- id: no_global_2
  selector:
    anchor: Animal
- id: no_global_3
  selector:
    anchor: isDog
oracle:
  required:
  - entities
  - entities_1
  - entities_2
  allowed: []
  forbidden:
  - no_input
  - no_animal
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
