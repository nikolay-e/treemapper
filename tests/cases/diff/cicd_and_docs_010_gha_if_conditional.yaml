name: cicd_and_docs_010_gha_if_conditional
repo:
  initial_files:
    .github/workflows/ci.yml: |
      name: CI
      on: [push]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm run build
        deploy:
          runs-on: ubuntu-latest
          needs: build
          steps:
            - uses: actions/checkout@v4
            - run: ./deploy.sh
    scripts/deploy.sh: |
      #!/bin/bash
      set -e
      echo "Deploying to production..."
      aws s3 sync ./dist s3://prod-bucket/
    src/config/environments.ts: |
      export type Environment = 'development' | 'staging' | 'production';

      export function getEnvironment(): Environment {
        return (process.env.NODE_ENV as Environment) || 'development';
      }

      export function isProduction(): boolean {
        return getEnvironment() === 'production';
      }
    garbage_branch.ts: |
      const GARBAGE_CICD_DOCS_010_BRANCH_MARKER_A = "feature";
      const GARBAGE_CICD_DOCS_010_PREFIX_MARKER_B = "refs/heads";

      class GarbageBranchManager {
        static GARBAGE_CICD_DOCS_010_PROTECTED_MARKER_C = true;
        getBranch(): string { return ""; }
      }
    garbage_merge.ts: |
      const GARBAGE_CICD_DOCS_010_MERGE_MARKER_D = "squash";
      const GARBAGE_CICD_DOCS_010_STRATEGY_MARKER_E = "recursive";

      class GarbageMergeHandler {
        static GARBAGE_CICD_DOCS_010_CONFLICT_MARKER_F = "abort";
        merge(): void {}
      }
  changed_files:
    .github/workflows/ci.yml: |
      name: CI
      on: [push]
      jobs:
        build:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm run build
        deploy:
          runs-on: ubuntu-latest
          needs: build
          if: github.ref == 'refs/heads/main'
          steps:
            - uses: actions/checkout@v4
            - run: ./deploy.sh
    scripts/deploy.sh: |
      #!/bin/bash
      set -e
      echo "Deploying to production..."
      aws s3 sync ./dist s3://prod-bucket/
    src/config/environments.ts: |
      export type Environment = 'development' | 'staging' | 'production';

      export function getEnvironment(): Environment {
        return (process.env.NODE_ENV as Environment) || 'development';
      }

      export function isProduction(): boolean {
        return getEnvironment() === 'production';
      }
    garbage_branch.ts: |
      const GARBAGE_CICD_DOCS_010_BRANCH_MARKER_A = "feature";
      const GARBAGE_CICD_DOCS_010_PREFIX_MARKER_B = "refs/heads";

      class GarbageBranchManager {
        static GARBAGE_CICD_DOCS_010_PROTECTED_MARKER_C = true;
        getBranch(): string { return ""; }
      }
    garbage_merge.ts: |
      const GARBAGE_CICD_DOCS_010_MERGE_MARKER_D = "squash";
      const GARBAGE_CICD_DOCS_010_STRATEGY_MARKER_E = "recursive";

      class GarbageMergeHandler {
        static GARBAGE_CICD_DOCS_010_CONFLICT_MARKER_F = "abort";
        merge(): void {}
      }
  commit_message: Update files
fragments:
- id: ci_file
  selector:
    path: .github/workflows/ci.yml
- id: req_global_0
  selector:
    anchor: 'if: github.ref == ''refs/heads/main'''
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_010_BRANCH_MARKER_A
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_010_CONFLICT_MARKER_F
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_010_MERGE_MARKER_D
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_010_PREFIX_MARKER_B
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_010_PROTECTED_MARKER_C
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_010_STRATEGY_MARKER_E
- id: no_global_6
  selector:
    anchor: garbage_branch.ts
- id: no_global_7
  selector:
    anchor: garbage_merge.ts
oracle:
  required:
  - ci_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
