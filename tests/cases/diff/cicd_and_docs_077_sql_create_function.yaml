name: cicd_and_docs_077_sql_create_function
tags:
- lang:python
- lang:sql
- change:single_file
- context:cross_module
- distance:2hop
- difficulty:medium
repo:
  initial_files:
    sql/functions/format_currency.sql: |
      CREATE OR REPLACE FUNCTION format_currency(amount DECIMAL)
      RETURNS TEXT AS $$
      BEGIN
          RETURN '$' || TO_CHAR(amount, 'FM999,999,990.00');
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    sql/functions/format_date.sql: |
      CREATE OR REPLACE FUNCTION format_date(ts TIMESTAMP, fmt TEXT DEFAULT 'YYYY-MM-DD')
      RETURNS TEXT AS $$
      BEGIN
          RETURN TO_CHAR(ts, fmt);
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    sql/queries/order_report.sql: |
      SELECT
          id,
          user_id,
          format_currency(total) AS formatted_total,
          created_at
      FROM orders
      ORDER BY created_at DESC;
    src/models/formatted_order.py: |
      from dataclasses import dataclass
      from datetime import datetime

      @dataclass
      class FormattedOrder:
          id: int
          user_id: int
          formatted_total: str
          created_at: datetime
    src/repositories/order_report_repository.py: |
      from typing import List
      from models.formatted_order import FormattedOrder

      class OrderReportRepository:
          def __init__(self, db):
              self.db = db

          def get_order_report(self) -> List[FormattedOrder]:
              results = self.db.execute_file('sql/queries/order_report.sql')
              return [FormattedOrder(**r) for r in results]
  changed_files:
    sql/functions/format_currency.sql: |
      CREATE OR REPLACE FUNCTION format_currency(
          amount DECIMAL,
          currency_code TEXT DEFAULT 'USD',
          show_decimals BOOLEAN DEFAULT TRUE
      )
      RETURNS TEXT AS $$
      DECLARE
          symbol TEXT;
          formatted TEXT;
      BEGIN
          symbol := CASE currency_code
              WHEN 'USD' THEN '$'
              WHEN 'EUR' THEN '€'
              WHEN 'GBP' THEN '£'
              WHEN 'JPY' THEN '¥'
              WHEN 'CHF' THEN 'CHF '
              WHEN 'CAD' THEN 'C$'
              WHEN 'AUD' THEN 'A$'
              ELSE currency_code || ' '
          END;

          IF show_decimals THEN
              formatted := TO_CHAR(amount, 'FM999,999,990.00');
          ELSE
              formatted := TO_CHAR(ROUND(amount), 'FM999,999,990');
          END IF;

          IF amount < 0 THEN
              RETURN '(' || symbol || REPLACE(formatted, '-', '') || ')';
          ELSE
              RETURN symbol || formatted;
          END IF;
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;

      CREATE OR REPLACE FUNCTION format_currency_locale(
          amount DECIMAL,
          locale TEXT DEFAULT 'en_US'
      )
      RETURNS TEXT AS $$
      BEGIN
          RETURN CASE locale
              WHEN 'en_US' THEN format_currency(amount, 'USD')
              WHEN 'en_GB' THEN format_currency(amount, 'GBP')
              WHEN 'de_DE' THEN format_currency(amount, 'EUR')
              WHEN 'ja_JP' THEN format_currency(amount, 'JPY', FALSE)
              ELSE format_currency(amount, 'USD')
          END;
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    sql/functions/format_date.sql: |
      CREATE OR REPLACE FUNCTION format_date(ts TIMESTAMP, fmt TEXT DEFAULT 'YYYY-MM-DD')
      RETURNS TEXT AS $$
      BEGIN
          RETURN TO_CHAR(ts, fmt);
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    sql/queries/order_report.sql: |
      SELECT
          id,
          user_id,
          format_currency(total) AS formatted_total,
          created_at
      FROM orders
      ORDER BY created_at DESC;
    src/models/formatted_order.py: |
      from dataclasses import dataclass
      from datetime import datetime

      @dataclass
      class FormattedOrder:
          id: int
          user_id: int
          formatted_total: str
          created_at: datetime
    src/repositories/order_report_repository.py: |
      from typing import List
      from models.formatted_order import FormattedOrder

      class OrderReportRepository:
          def __init__(self, db):
              self.db = db

          def get_order_report(self) -> List[FormattedOrder]:
              results = self.db.execute_file('sql/queries/order_report.sql')
              return [FormattedOrder(**r) for r in results]
  commit_message: "db: extend format_currency with multi-currency and locale support"
fragments:
- id: function_changed
  selector:
    path: sql/functions/format_currency.sql
- id: new_signature
  selector:
    anchor: currency_code TEXT DEFAULT 'USD'
- id: caller_old_signature
  selector:
    path: sql/queries/order_report.sql
    anchor: format_currency(total) AS formatted_total
- id: report_repository
  selector:
    path: src/repositories/order_report_repository.py
    anchor: execute_file('sql/queries/order_report.sql')
- id: no_format_date_function
  selector:
    path: sql/functions/format_date.sql
    anchor: format_date
- id: no_formatted_order_model
  selector:
    path: src/models/formatted_order.py
    anchor: FormattedOrder
oracle:
  required:
  - function_changed
  - new_signature
  - caller_old_signature
  allowed:
  - report_repository
  forbidden:
  - no_format_date_function
  - no_formatted_order_model
