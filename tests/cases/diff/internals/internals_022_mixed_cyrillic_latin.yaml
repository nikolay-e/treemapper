name: internals_022_mixed_cyrillic_latin
initial:
  mixed_script.py: |
    def placeholder():
        pass
changed:
  mixed_script.py: |
    # This has mixed а (cyrillic) and a (latin)
    def process(а, a):
        return а + a
assertions:
  must_include:
  - process
options:
  add_garbage: true
