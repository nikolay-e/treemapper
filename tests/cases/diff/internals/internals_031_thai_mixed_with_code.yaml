name: internals_031_thai_mixed_with_code
initial:
  thai_mixed.py: |
    def placeholder():
        pass
changed:
  thai_mixed.py: |
    # ฟังก์ชันคำนวณ
    def compute(x):  # คำนวณค่า
        # ส่งคืนผลลัพธ์
        return x * 2
assertions:
  must_include:
  - compute
options:
  add_garbage: true
