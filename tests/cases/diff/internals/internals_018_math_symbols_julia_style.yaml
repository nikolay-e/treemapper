name: internals_018_math_symbols_julia_style
initial:
  math_symbols.jl: |
    x = 1
changed:
  math_symbols.jl: |
    ∑ = sum
    ∏ = prod
    α = 0.5
    β = 0.3
    result = calculate(α, β)
assertions:
  must_include:
  - calculate
  - result
options:
  add_garbage: true
