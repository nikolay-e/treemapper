name: internals_020_greek_letters_scientific_code
initial:
  greek_science.py: |
    def placeholder():
        pass
changed:
  greek_science.py: |
    def calculate_Δ(θ, φ):
        import math
        return θ * math.cos(φ)

    def compute_σ(μ, data):
        return sum((x - μ)**2 for x in data)
assertions:
  must_include:
  - calculate_Δ
  - compute_σ
options:
  add_garbage: true
