name: csharp_004_task_run
repo:
  initial_files:
    Services/ComputeService.cs: |
      using System;

      namespace MyApp.Services;

      public class ComputeService
      {
          public int HeavyComputation(int input)
          {
              int result = 0;
              for (int i = 0; i < input; i++)
              {
                  result += (i * i) % 997;
              }
              return result;
          }

          public double ComputeAverage(int[] values)
          {
              if (values.Length == 0) return 0.0;
              long sum = 0;
              foreach (var v in values) sum += v;
              return (double)sum / values.Length;
          }
      }
    Program.cs: |
      using System;

      class Program
      {
          static void Main()
          {
              Console.WriteLine("Application ready");
          }
      }
    Unrelated/NotificationSender.cs: |
      namespace MyApp.Unrelated;

      public class NotificationSender
      {
          private const string GARBAGE_CS_004_NOTIFICATION_A = "notification_marker";

          public void Send(string message)
          {
              _ = GARBAGE_CS_004_NOTIFICATION_A + message;
          }
      }
    Unrelated/RateLimiter.cs: |
      namespace MyApp.Unrelated;

      public class RateLimiter
      {
          public const string GARBAGE_CS_004_RATE_LIMITER_B = "rate_limit_marker";

          public bool IsAllowed(string key)
          {
              return key != GARBAGE_CS_004_RATE_LIMITER_B;
          }
      }
  changed_files:
    Services/ComputeService.cs: |
      using System;

      namespace MyApp.Services;

      public class ComputeService
      {
          public int HeavyComputation(int input)
          {
              int result = 0;
              for (int i = 0; i < input; i++)
              {
                  result += (i * i) % 997;
              }
              return result;
          }

          public double ComputeAverage(int[] values)
          {
              if (values.Length == 0) return 0.0;
              long sum = 0;
              foreach (var v in values) sum += v;
              return (double)sum / values.Length;
          }
      }
    Program.cs: |
      using System;
      using System.Threading.Tasks;
      using MyApp.Services;

      class Program
      {
          static async Task Main()
          {
              var service = new ComputeService();
              var result = await Task.Run(() => service.HeavyComputation(1000000));
              var avg = await Task.Run(() => service.ComputeAverage(new[] { 1, 2, 3, 4, 5 }));
              Console.WriteLine($"Result: {result}, Average: {avg}");
          }
      }
    Unrelated/NotificationSender.cs: |
      namespace MyApp.Unrelated;

      public class NotificationSender
      {
          private const string GARBAGE_CS_004_NOTIFICATION_A = "notification_marker";

          public void Send(string message)
          {
              _ = GARBAGE_CS_004_NOTIFICATION_A + message;
          }
      }
    Unrelated/RateLimiter.cs: |
      namespace MyApp.Unrelated;

      public class RateLimiter
      {
          public const string GARBAGE_CS_004_RATE_LIMITER_B = "rate_limit_marker";

          public bool IsAllowed(string key)
          {
              return key != GARBAGE_CS_004_RATE_LIMITER_B;
          }
      }
  commit_message: Update files
fragments:
- id: program_file
  selector:
    path: Program.cs
- id: req_global_0
  selector:
    anchor: using System.Threading.Tasks;
- id: req_global_1
  selector:
    anchor: static async Task Main()
- id: no_global_0
  selector:
    anchor: GARBAGE_CS_004_NOTIFICATION_A
- id: no_global_1
  selector:
    anchor: GARBAGE_CS_004_RATE_LIMITER_B
- id: no_global_2
  selector:
    anchor: NotificationSender.cs
- id: no_global_3
  selector:
    anchor: RateLimiter.cs
oracle:
  required:
  - program_file
  - req_global_0
  - req_global_1
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
