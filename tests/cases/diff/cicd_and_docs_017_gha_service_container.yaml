name: cicd_and_docs_017_gha_service_container
tags:
- lang:javascript
- change:single_file
- change:config
- context:same_file
- distance:0hop
- difficulty:easy
repo:
  initial_files:
    tests/db.test.js: |
      const { Pool } = require('pg');

      describe('Database', () => {
        let pool;

        beforeAll(() => {
          pool = new Pool({ connectionString: process.env.DATABASE_URL });
        });

        after all(async () => {
          await pool.end();
        });

        test('connects to database', async () => {
          const result = await pool.query('SELECT 1 as value');
          expect(result.rows[0].value).toBe(1);
        });

        test('creates table', async () => {
          await pool.query('CREATE TABLE IF NOT EXISTS test_table (id SERIAL PRIMARY KEY)');
        });
      });
    .github/workflows/test.yml: |
      name: Test
      on: [push, pull_request]
      jobs:
        test:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - run: npm ci
            - run: npm test
    package.json: |
      {
        "name": "database-tests-app",
        "version": "1.0.0",
        "scripts": {
          "test": "jest"
        },
        "dependencies": {
          "pg": "^8.11.0"
        }
      }
    garbage_docker.ts: |
      const GARBAGE_CICD_DOCS_017_DOCKER_MARKER_A = "compose";
      const GARBAGE_CICD_DOCS_017_NETWORK_MARKER_B = "bridge";

      class GarbageDockerService {
        static GARBAGE_CICD_DOCS_017_VOLUME_MARKER_C = "/data";
        start(): void {}
      }
    garbage_container.ts: |
      const GARBAGE_CICD_DOCS_017_CONTAINER_MARKER_D = "alpine";
      const GARBAGE_CICD_DOCS_017_ENTRYPOINT_MARKER_E = "/bin/sh";

      class GarbageContainerManager {
        static GARBAGE_CICD_DOCS_017_MEMORY_MARKER_F = "512m";
        run(): void {}
      }
  changed_files:
    tests/db.test.js: |
      const { Pool } = require('pg');

      describe('Database', () => {
        let pool;

        beforeAll(() => {
          pool = new Pool({ connectionString: process.env.DATABASE_URL });
        });

        after all(async () => {
          await pool.end();
        });

        test('connects to database', async () => {
          const result = await pool.query('SELECT 1 as value');
          expect(result.rows[0].value).toBe(1);
        });

        test('creates table', async () => {
          await pool.query('CREATE TABLE IF NOT EXISTS test_table (id SERIAL PRIMARY KEY)');
        });
      });
    .github/workflows/test.yml: |
      name: Test
      on: [push, pull_request]
      jobs:
        test:
          runs-on: ubuntu-latest
          services:
            postgres:
              image: postgres:15
              env:
                POSTGRES_PASSWORD: postgres
              ports:
                - 5432:5432
              options: >-
                --health-cmd pg_isready
                --health-interval 10s
                --health-timeout 5s
                --health-retries 5
          steps:
            - uses: actions/checkout@v4
            - run: npm ci
            - run: npm test
              env:
                DATABASE_URL: ${{ secrets.DATABASE_URL }}
    package.json: |
      {
        "name": "database-tests-app",
        "version": "1.0.0",
        "scripts": {
          "test": "jest"
        },
        "dependencies": {
          "pg": "^8.11.0"
        }
      }
    garbage_docker.ts: |
      const GARBAGE_CICD_DOCS_017_DOCKER_MARKER_A = "compose";
      const GARBAGE_CICD_DOCS_017_NETWORK_MARKER_B = "bridge";

      class GarbageDockerService {
        static GARBAGE_CICD_DOCS_017_VOLUME_MARKER_C = "/data";
        start(): void {}
      }
    garbage_container.ts: |
      const GARBAGE_CICD_DOCS_017_CONTAINER_MARKER_D = "alpine";
      const GARBAGE_CICD_DOCS_017_ENTRYPOINT_MARKER_E = "/bin/sh";

      class GarbageContainerManager {
        static GARBAGE_CICD_DOCS_017_MEMORY_MARKER_F = "512m";
        run(): void {}
      }
  commit_message: Update files
fragments:
- id: test_file
  selector:
    path: .github/workflows/test.yml
- id: req_global_0
  selector:
    anchor: 'image: postgres:15'
- id: no_global_0
  selector:
    anchor: GARBAGE_CICD_DOCS_017_CONTAINER_MARKER_D
- id: no_global_1
  selector:
    anchor: GARBAGE_CICD_DOCS_017_DOCKER_MARKER_A
- id: no_global_2
  selector:
    anchor: GARBAGE_CICD_DOCS_017_ENTRYPOINT_MARKER_E
- id: no_global_3
  selector:
    anchor: GARBAGE_CICD_DOCS_017_MEMORY_MARKER_F
- id: no_global_4
  selector:
    anchor: GARBAGE_CICD_DOCS_017_NETWORK_MARKER_B
- id: no_global_5
  selector:
    anchor: GARBAGE_CICD_DOCS_017_VOLUME_MARKER_C
- id: no_global_6
  selector:
    anchor: garbage_container.ts
- id: no_global_7
  selector:
    anchor: garbage_docker.ts
oracle:
  required:
  - test_file
  - req_global_0
  allowed: []
  forbidden:
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
  - no_global_6
  - no_global_7
