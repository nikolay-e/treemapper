name: go_030_constructor_pattern
xfail:
  category: go-cross-file-discovery
repo:
  initial_files:
    pkg/config/config.go: |
      package config

      import "time"

      type Config struct {
          Host         string
          Port         int
          ReadTimeout  time.Duration
          WriteTimeout time.Duration
          MaxConns     int
      }

      func DefaultConfig() *Config {
          return &Config{
              Host:         "localhost",
              Port:         8080,
              ReadTimeout:  30 * time.Second,
              WriteTimeout: 30 * time.Second,
              MaxConns:     100,
          }
      }

      func (c *Config) Address() string {
          return c.Host + ":" + string(rune(c.Port))
      }
    pkg/server/server.go: |
      package server

      import "myapp/pkg/config"

      type Server struct {
          config config.Config
      }
    pkg/server/logger.go: |
      package server

      type Logger interface {
          Info(msg string, args ...interface{})
          Error(msg string, args ...interface{})
          Debug(msg string, args ...interface{})
      }

      type noopLogger struct{}

      func (l *noopLogger) Info(msg string, args ...interface{})  {}
      func (l *noopLogger) Error(msg string, args ...interface{}) {}
      func (l *noopLogger) Debug(msg string, args ...interface{}) {}

      func NewNoopLogger() Logger {
          return &noopLogger{}
      }
    pkg/server/routes.go: |
      package server

      import "net/http"

      func RegisterHealthRoutes(mux *http.ServeMux) {
          mux.HandleFunc("/healthz", func(w http.ResponseWriter, r *http.Request) {
              w.WriteHeader(http.StatusOK)
              w.Write([]byte("ok"))
          })
          mux.HandleFunc("/readyz", func(w http.ResponseWriter, r *http.Request) {
              w.WriteHeader(http.StatusOK)
              w.Write([]byte("ready"))
          })
      }
  changed_files:
    pkg/config/config.go: |
      package config

      import "time"

      type Config struct {
          Host         string
          Port         int
          ReadTimeout  time.Duration
          WriteTimeout time.Duration
          MaxConns     int
      }

      func DefaultConfig() *Config {
          return &Config{
              Host:         "localhost",
              Port:         8080,
              ReadTimeout:  30 * time.Second,
              WriteTimeout: 30 * time.Second,
              MaxConns:     100,
          }
      }

      func (c *Config) Address() string {
          return c.Host + ":" + string(rune(c.Port))
      }
    pkg/server/server.go: |
      package server

      import (
          "fmt"
          "myapp/pkg/config"
          "net/http"
      )

      type Server struct {
          config *config.Config
          logger Logger
          mux    *http.ServeMux
      }

      func NewServer(cfg *config.Config, logger Logger) (*Server, error) {
          if cfg == nil {
              return nil, fmt.Errorf("config is required")
          }
          if cfg.Port <= 0 || cfg.Port > 65535 {
              return nil, fmt.Errorf("invalid port: %d", cfg.Port)
          }
          if logger == nil {
              logger = NewNoopLogger()
          }
          return &Server{
              config: cfg,
              logger: logger,
              mux:    http.NewServeMux(),
          }, nil
      }

      func (s *Server) Start() error {
          addr := s.config.Address()
          s.logger.Info("starting server", "addr", addr)
          srv := &http.Server{
              Addr:         addr,
              Handler:      s.mux,
              ReadTimeout:  s.config.ReadTimeout,
              WriteTimeout: s.config.WriteTimeout,
          }
          return srv.ListenAndServe()
      }

      func (s *Server) Shutdown() error {
          s.logger.Info("shutting down server")
          return nil
      }
    pkg/server/logger.go: |
      package server

      type Logger interface {
          Info(msg string, args ...interface{})
          Error(msg string, args ...interface{})
          Debug(msg string, args ...interface{})
      }

      type noopLogger struct{}

      func (l *noopLogger) Info(msg string, args ...interface{})  {}
      func (l *noopLogger) Error(msg string, args ...interface{}) {}
      func (l *noopLogger) Debug(msg string, args ...interface{}) {}

      func NewNoopLogger() Logger {
          return &noopLogger{}
      }
    pkg/server/routes.go: |
      package server

      import "net/http"

      func RegisterHealthRoutes(mux *http.ServeMux) {
          mux.HandleFunc("/healthz", func(w http.ResponseWriter, r *http.Request) {
              w.WriteHeader(http.StatusOK)
              w.Write([]byte("ok"))
          })
          mux.HandleFunc("/readyz", func(w http.ResponseWriter, r *http.Request) {
              w.WriteHeader(http.StatusOK)
              w.Write([]byte("ready"))
          })
      }
  commit_message: Update files
fragments:
- id: server_file
  selector:
    path: pkg/server/server.go
- id: server_imports_config
  selector:
    anchor: '"myapp/pkg/config"'
- id: server_uses_config_pointer
  selector:
    anchor: config *config.Config
- id: config_struct
  selector:
    path: pkg/config/config.go
    anchor: type Config struct {
- id: config_address_method
  selector:
    anchor: func (c *Config) Address() string {
- id: logger_interface
  selector:
    path: pkg/server/logger.go
    anchor: type Logger interface {
- id: logger_noop_fallback
  selector:
    anchor: func NewNoopLogger() Logger {
- id: no_routes
  selector:
    path: pkg/server/routes.go
    anchor: func RegisterHealthRoutes(mux *http.ServeMux) {
oracle:
  required:
  - server_file
  - server_imports_config
  - server_uses_config_pointer
  - config_struct
  - config_address_method
  - logger_interface
  - logger_noop_fallback
  allowed: []
  forbidden:
  - no_routes
