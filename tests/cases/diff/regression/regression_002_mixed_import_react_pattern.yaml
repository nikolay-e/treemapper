name: regression_002_mixed_import_react_pattern
repo:
  initial_files:
    src/components/UserProfile.tsx: |
      import React, { useState, useEffect } from 'react';
      import { fetchUser } from '../services/userService';
      import { formatDate } from '../utils/dateUtils';

      interface UserProfileProps {
        userId: string;
      }

      export function UserProfile({ userId }: UserProfileProps) {
        const [user, setUser] = useState(null);
        const [loading, setLoading] = useState(true);

        useEffect(() => {
          fetchUser(userId).then(data => {
            setUser(data);
            setLoading(false);
          });
        }, [userId]);

        if (loading) return <div>Loading...</div>;

        return (
          <div className="profile">
            <h1>{user?.name}</h1>
            <p>Joined: {formatDate(user?.createdAt)}</p>
          </div>
        );
      }
    src/services/userService.ts: |
      const API_URL = '/api/users';

      export async function fetchUser(userId: string) {
        const response = await fetch(`${API_URL}/${userId}`);
        if (!response.ok) throw new Error('User not found');
        return response.json();
      }

      export async function updateUser(userId: string, data: Record<string, unknown>) {
        const response = await fetch(`${API_URL}/${userId}`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });
        return response.json();
      }
    src/utils/dateUtils.ts: |
      export function formatDate(dateStr: string | null): string {
        if (!dateStr) return 'Unknown';
        const date = new Date(dateStr);
        return date.toLocaleDateString('en-US', {
          year: 'numeric',
          month: 'long',
          day: 'numeric',
        });
      }

      export function isRecent(dateStr: string, days: number = 7): boolean {
        const date = new Date(dateStr);
        const threshold = new Date();
        threshold.setDate(threshold.getDate() - days);
        return date > threshold;
      }
  changed_files:
    src/components/UserProfile.tsx: |
      import React, { useState, useEffect, useCallback } from 'react';
      import { fetchUser, updateUser } from '../services/userService';
      import { formatDate } from '../utils/dateUtils';

      interface UserProfileProps {
        userId: string;
      }

      export function UserProfile({ userId }: UserProfileProps) {
        const [user, setUser] = useState(null);
        const [loading, setLoading] = useState(true);
        const [editing, setEditing] = useState(false);

        useEffect(() => {
          fetchUser(userId).then(data => {
            setUser(data);
            setLoading(false);
          });
        }, [userId]);

        const handleSave = useCallback(async (data: Record<string, unknown>) => {
          const updated = await updateUser(userId, data);
          setUser(updated);
          setEditing(false);
        }, [userId]);

        if (loading) return <div>Loading...</div>;

        return (
          <div className="profile">
            <h1>{user?.name}</h1>
            <p>Joined: {formatDate(user?.createdAt)}</p>
            <button onClick={() => setEditing(!editing)}>
              {editing ? 'Cancel' : 'Edit'}
            </button>
          </div>
        );
      }
    src/services/userService.ts: |
      const API_URL = '/api/users';

      export async function fetchUser(userId: string) {
        const response = await fetch(`${API_URL}/${userId}`);
        if (!response.ok) throw new Error('User not found');
        return response.json();
      }

      export async function updateUser(userId: string, data: Record<string, unknown>) {
        const response = await fetch(`${API_URL}/${userId}`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });
        return response.json();
      }
    src/utils/dateUtils.ts: |
      export function formatDate(dateStr: string | null): string {
        if (!dateStr) return 'Unknown';
        const date = new Date(dateStr);
        return date.toLocaleDateString('en-US', {
          year: 'numeric',
          month: 'long',
          day: 'numeric',
        });
      }

      export function isRecent(dateStr: string, days: number = 7): boolean {
        const date = new Date(dateStr);
        const threshold = new Date();
        threshold.setDate(threshold.getDate() - days);
        return date > threshold;
      }
  commit_message: Update files
fixtures:
  auto_garbage: true
fragments:
- id: userprofile
  selector:
    path: src/components/UserProfile.tsx
    anchor: export function UserProfile
- id: userprofile_1
  selector:
    path: src/components/UserProfile.tsx
    anchor: const handleSave = useCallback
- id: userprofile_2
  selector:
    path: src/components/UserProfile.tsx
    anchor: updateUser(userId, data)
- id: userservice
  selector:
    path: src/services/userService.ts
    anchor: export async function fetchUser
- id: userservice_1
  selector:
    path: src/services/userService.ts
    anchor: export async function updateUser
- id: no_global_0
  selector:
    anchor: GARBAGE_REG_002_A
oracle:
  required:
  - userprofile
  - userprofile_1
  - userprofile_2
  - userservice
  - userservice_1
  allowed: []
  forbidden:
  - no_global_0
