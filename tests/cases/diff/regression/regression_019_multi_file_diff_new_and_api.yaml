# Test: Multi-file API diff — routes add pagination, auth adds validation, new pagination module
#
# Scenario: src/api/routes.py adds pagination import and POST /users endpoint.
# src/api/auth.py adds validate_token() and uses it in require_auth.
# A new file src/api/pagination.py provides the paginate() function.
# src/api/middleware.py is UNCHANGED.
#
# Reviewer questions:
#   Q1: "How does the new pagination work?"
#       -> src/api/pagination.py (NEW) slices list by page/per_page, returns
#          items with metadata.
#   Q2: "What token validation was added?"
#       -> src/api/auth.py adds validate_token() checking Bearer prefix + length.
#
# Trap files (plausible but unrelated):
#   - src/api/middleware.py: request logging middleware — same api/ package but
#     unchanged and not imported by the changed files.
#   - tests/conftest.py: test fixtures — same project but testing infrastructure,
#     not runtime code.
#   - src/api/serializers.py: response serialization — same api/ package but
#     not imported by routes or auth.

initial:
  src/api/routes.py: |
    from flask import Flask, jsonify, request
    from src.api.auth import require_auth

    app = Flask(__name__)

    @app.route("/api/users", methods=["GET"])
    @require_auth
    def list_users():
        return jsonify({"users": []})

    @app.route("/api/users/<int:user_id>", methods=["GET"])
    @require_auth
    def get_user(user_id):
        return jsonify({"user": {"id": user_id}})
  src/api/auth.py: |
    from functools import wraps
    from flask import request, abort

    def require_auth(f):
        @wraps(f)
        def decorated(*args, **kwargs):
            token = request.headers.get("Authorization")
            if not token:
                abort(401)
            return f(*args, **kwargs)
        return decorated
  src/api/middleware.py: |
    import time
    import logging

    def request_logger(app):
        @app.before_request
        def log_request():
            logging.info("Request: %s %s", request.method, request.path)

        @app.after_request
        def log_response(response):
            logging.info("Response: %s", response.status_code)
            return response
  tests/conftest.py: |
    import pytest
    from flask import Flask

    @pytest.fixture
    def app():
        app = Flask(__name__)
        app.config["TESTING"] = True
        return app

    @pytest.fixture
    def client(app):
        return app.test_client()

    @pytest.fixture
    def auth_headers():
        return {"Authorization": "Bearer test-token-12345"}

    @pytest.fixture
    def sample_user():
        return {"name": "Test User", "email": "test@example.com"}
  src/api/serializers.py: |
    from typing import Dict, List, Any
    from datetime import datetime

    def serialize_user(user: Dict) -> Dict:
        return {
            "id": user["id"],
            "name": user["name"],
            "email": user["email"],
            "created_at": user.get("created_at", datetime.utcnow()).isoformat(),
        }

    def serialize_users(users: List[Dict]) -> List[Dict]:
        return [serialize_user(u) for u in users]

    def serialize_error(message: str, code: int) -> Dict:
        return {"error": {"message": message, "code": code}}
changed:
  src/api/routes.py: |
    from flask import Flask, jsonify, request
    from src.api.auth import require_auth
    from src.api.pagination import paginate

    app = Flask(__name__)

    @app.route("/api/users", methods=["GET"])
    @require_auth
    def list_users():
        page = request.args.get("page", 1, type=int)
        per_page = request.args.get("per_page", 20, type=int)
        users = get_all_users()
        return jsonify(paginate(users, page, per_page))

    @app.route("/api/users/<int:user_id>", methods=["GET"])
    @require_auth
    def get_user(user_id):
        return jsonify({"user": {"id": user_id}})

    @app.route("/api/users", methods=["POST"])
    @require_auth
    def create_user():
        data = request.get_json()
        return jsonify({"user": data}), 201
  src/api/auth.py: |
    from functools import wraps
    from flask import request, abort

    def require_auth(f):
        @wraps(f)
        def decorated(*args, **kwargs):
            token = request.headers.get("Authorization")
            if not token:
                abort(401)
            if not validate_token(token):
                abort(403)
            return f(*args, **kwargs)
        return decorated

    def validate_token(token: str) -> bool:
        return token.startswith("Bearer ") and len(token) > 10
  src/api/pagination.py: |
    from typing import Any

    def paginate(items: list[Any], page: int, per_page: int) -> dict:
        start = (page - 1) * per_page
        end = start + per_page
        page_items = items[start:end]
        return {
            "items": page_items,
            "page": page,
            "per_page": per_page,
            "total": len(items),
            "pages": (len(items) + per_page - 1) // per_page,
        }
  src/api/middleware.py: |
    import time
    import logging

    def request_logger(app):
        @app.before_request
        def log_request():
            logging.info("Request: %s %s", request.method, request.path)

        @app.after_request
        def log_response(response):
            logging.info("Response: %s", response.status_code)
            return response
  tests/conftest.py: |
    import pytest
    from flask import Flask

    @pytest.fixture
    def app():
        app = Flask(__name__)
        app.config["TESTING"] = True
        return app

    @pytest.fixture
    def client(app):
        return app.test_client()

    @pytest.fixture
    def auth_headers():
        return {"Authorization": "Bearer test-token-12345"}

    @pytest.fixture
    def sample_user():
        return {"name": "Test User", "email": "test@example.com"}
  src/api/serializers.py: |
    from typing import Dict, List, Any
    from datetime import datetime

    def serialize_user(user: Dict) -> Dict:
        return {
            "id": user["id"],
            "name": user["name"],
            "email": user["email"],
            "created_at": user.get("created_at", datetime.utcnow()).isoformat(),
        }

    def serialize_users(users: List[Dict]) -> List[Dict]:
        return [serialize_user(u) for u in users]

    def serialize_error(message: str, code: int) -> Dict:
        return {"error": {"message": message, "code": code}}
must_include_files:
  - src/api/routes.py
  - src/api/auth.py
  # Q1: pagination.py is the new file that routes.py imports
  - src/api/pagination.py
must_include_content_from:
  src/api/routes.py:
    - "from src.api.pagination import paginate"
    - "def create_user"
  src/api/auth.py:
    - "def validate_token"
  src/api/pagination.py:
    - "def paginate"
must_not_include:
  # src/api/middleware.py: request logging — unchanged, not imported by changed files
  - request_logger
  - log_request
  - log_response
  # tests/conftest.py: test fixtures — testing infrastructure, not runtime
  - auth_headers
  - sample_user
  # src/api/serializers.py: response serialization — same package but not imported
  - serialize_user
  - serialize_error
must_not_include_files:
  - src/api/middleware.py
  - tests/conftest.py
  - src/api/serializers.py
max_fragments: 6
max_files: 4
add_garbage_files: false
skip_garbage_check: true
