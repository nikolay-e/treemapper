must_include_files:
  - main.cpp
must_include_content:
  - |
    typedef void (*Callback)(int);

    void execute(Callback callback, int value) {
        callback(value);
    }
  - |
    Callback handler = onSuccess;
    execute(handler, 200);
must_not_include:
  - GARBAGE_JVM_002_SIGNAL_MARKER_A
  - GARBAGE_JVM_002_TRAP_MARKER_B
  - GARBAGE_JVM_002_LOOP_MARKER_C
  - GARBAGE_JVM_002_POLL_MARKER_D
  - GarbageSignalHandler
  - GarbageEventLoop
  - garbage_signal_handler.cpp
  - garbage_event_loop.cpp
