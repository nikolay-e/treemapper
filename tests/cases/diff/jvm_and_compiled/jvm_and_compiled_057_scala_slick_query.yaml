# Scenario: UserRepository is implemented with Slick queries that use
#   the Tables.users TableQuery for filtering, searching, and counting.
#
# Diff shows:
#   - class UserRepository {}  (empty)
#   + import Tables._, findAdults filtering _.age >= 18,
#     findActive filtering _.active === true, findByName, countByAge
#
# Reviewer questions:
#   Q1: "What columns does the users table have — do age/active/name exist?"
#       -> Needs Tables.scala: Users table with id, name, age, active columns
#          and User case class mapping. The filters access these columns.
#
#   Q2: "Is Users a TableQuery — does Tables.users exist?"
#       -> Same file: object Tables { val users = TableQuery[Users] }.
#
# Not needed for comprehension:
#   - OrderRepository.scala: IDENTICAL Slick repository pattern — same
#     db.run, filter, result API. HARDEST TRAP: same Slick query DSL,
#     different table.
#
#   - ProductRepository.scala: Another Slick repository with same methods.
#     Same pattern. Different entity.
#
#   - DatabaseMigration.scala: Schema migration runner. Database-adjacent
#     vocabulary. No dependency from UserRepository.
#
#   - SlickProfile.scala: Configures Slick database profile. Same Slick
#     import path. Infrastructure, not query logic.
#
#   - UserService.scala: Calls UserRepository methods. Consumer of the
#     changed code. But service logic doesn't help review query correctness.

initial:
  Tables.scala: |
    import slick.jdbc.PostgresProfile.api._

    case class User(id: Long, name: String, age: Int, active: Boolean)

    class Users(tag: Tag) extends Table[User](tag, "users") {
      def id = column[Long]("id", O.PrimaryKey, O.AutoInc)
      def name = column[String]("name")
      def age = column[Int]("age")
      def active = column[Boolean]("active")
      def * = (id, name, age, active).mapTo[User]
    }

    object Tables {
      val users = TableQuery[Users]
    }
  UserRepository.scala: |
    class UserRepository {}
  repositories/OrderRepository.scala: |
    import slick.jdbc.PostgresProfile.api._
    import scala.concurrent.Future

    case class Order(id: Long, userId: Long, total: Double, status: String)

    class Orders(tag: Tag) extends Table[Order](tag, "orders") {
      def id = column[Long]("id", O.PrimaryKey, O.AutoInc)
      def userId = column[Long]("user_id")
      def total = column[Double]("total")
      def status = column[String]("status")
      def * = (id, userId, total, status).mapTo[Order]
    }

    class OrderRepository(db: Database) {
      val orders = TableQuery[Orders]

      def findByUser(userId: Long): Future[Seq[Order]] = {
        db.run(orders.filter(_.userId === userId).result)
      }

      def findPending(): Future[Seq[Order]] = {
        db.run(orders.filter(_.status === "pending").result)
      }

      def totalByUser(userId: Long): Future[Option[Double]] = {
        db.run(orders.filter(_.userId === userId).map(_.total).sum.result)
      }
    }
  repositories/ProductRepository.scala: |
    import slick.jdbc.PostgresProfile.api._
    import scala.concurrent.Future

    case class Product(id: Long, name: String, price: Double, available: Boolean)

    class Products(tag: Tag) extends Table[Product](tag, "products") {
      def id = column[Long]("id", O.PrimaryKey, O.AutoInc)
      def name = column[String]("name")
      def price = column[Double]("price")
      def available = column[Boolean]("available")
      def * = (id, name, price, available).mapTo[Product]
    }

    class ProductRepository(db: Database) {
      val products = TableQuery[Products]

      def findAvailable(): Future[Seq[Product]] = {
        db.run(products.filter(_.available === true).result)
      }

      def findByName(name: String): Future[Option[Product]] = {
        db.run(products.filter(_.name === name).result.headOption)
      }
    }
  migration/DatabaseMigration.scala: |
    object DatabaseMigration {
      def migrate(version: Int): Unit = {
        println(s"Migrating to version $version")
      }

      def rollback(version: Int): Unit = {
        println(s"Rolling back to version $version")
      }

      def currentVersion(): Int = 1

      def pendingMigrations(): List[Int] = List(2, 3, 4)
    }
  services/UserService.scala: |
    import scala.concurrent.{Future, ExecutionContext}
    import scala.concurrent.ExecutionContext.Implicits.global

    class UserService(repo: UserRepository) {
      def getAdultUsers(): Future[Seq[User]] = {
        repo.findAdults()
      }

      def searchByName(name: String): Future[Option[User]] = {
        repo.findByName(name)
      }
    }

changed:
  Tables.scala: |
    import slick.jdbc.PostgresProfile.api._

    case class User(id: Long, name: String, age: Int, active: Boolean)

    class Users(tag: Tag) extends Table[User](tag, "users") {
      def id = column[Long]("id", O.PrimaryKey, O.AutoInc)
      def name = column[String]("name")
      def age = column[Int]("age")
      def active = column[Boolean]("active")
      def * = (id, name, age, active).mapTo[User]
    }

    object Tables {
      val users = TableQuery[Users]
    }
  UserRepository.scala: |
    import slick.jdbc.PostgresProfile.api._
    import scala.concurrent.Future

    class UserRepository(db: Database) {
      import Tables._

      def findAdults(): Future[Seq[User]] = {
        db.run(users.filter(_.age >= 18).result)
      }

      def findActive(): Future[Seq[User]] = {
        db.run(users.filter(_.active === true).result)
      }

      def findByName(name: String): Future[Option[User]] = {
        db.run(users.filter(_.name === name).result.headOption)
      }

      def countByAge(minAge: Int): Future[Int] = {
        db.run(users.filter(_.age >= minAge).length.result)
      }
    }
  repositories/OrderRepository.scala: |
    import slick.jdbc.PostgresProfile.api._
    import scala.concurrent.Future

    case class Order(id: Long, userId: Long, total: Double, status: String)

    class Orders(tag: Tag) extends Table[Order](tag, "orders") {
      def id = column[Long]("id", O.PrimaryKey, O.AutoInc)
      def userId = column[Long]("user_id")
      def total = column[Double]("total")
      def status = column[String]("status")
      def * = (id, userId, total, status).mapTo[Order]
    }

    class OrderRepository(db: Database) {
      val orders = TableQuery[Orders]

      def findByUser(userId: Long): Future[Seq[Order]] = {
        db.run(orders.filter(_.userId === userId).result)
      }

      def findPending(): Future[Seq[Order]] = {
        db.run(orders.filter(_.status === "pending").result)
      }

      def totalByUser(userId: Long): Future[Option[Double]] = {
        db.run(orders.filter(_.userId === userId).map(_.total).sum.result)
      }
    }
  repositories/ProductRepository.scala: |
    import slick.jdbc.PostgresProfile.api._
    import scala.concurrent.Future

    case class Product(id: Long, name: String, price: Double, available: Boolean)

    class Products(tag: Tag) extends Table[Product](tag, "products") {
      def id = column[Long]("id", O.PrimaryKey, O.AutoInc)
      def name = column[String]("name")
      def price = column[Double]("price")
      def available = column[Boolean]("available")
      def * = (id, name, price, available).mapTo[Product]
    }

    class ProductRepository(db: Database) {
      val products = TableQuery[Products]

      def findAvailable(): Future[Seq[Product]] = {
        db.run(products.filter(_.available === true).result)
      }

      def findByName(name: String): Future[Option[Product]] = {
        db.run(products.filter(_.name === name).result.headOption)
      }
    }
  migration/DatabaseMigration.scala: |
    object DatabaseMigration {
      def migrate(version: Int): Unit = {
        println(s"Migrating to version $version")
      }

      def rollback(version: Int): Unit = {
        println(s"Rolling back to version $version")
      }

      def currentVersion(): Int = 1

      def pendingMigrations(): List[Int] = List(2, 3, 4)
    }
  services/UserService.scala: |
    import scala.concurrent.{Future, ExecutionContext}
    import scala.concurrent.ExecutionContext.Implicits.global

    class UserService(repo: UserRepository) {
      def getAdultUsers(): Future[Seq[User]] = {
        repo.findAdults()
      }

      def searchByName(name: String): Future[Option[User]] = {
        repo.findByName(name)
      }
    }

must_include_files:
  - UserRepository.scala
  - Tables.scala

must_include_content_from:
  UserRepository.scala:
    - "import Tables._"
    - "users.filter(_.age >= 18)"
    - "users.filter(_.active === true)"
    - "users.filter(_.name === name)"
    - "users.filter(_.age >= minAge).length.result"
  Tables.scala:
    - "case class User(id: Long, name: String, age: Int, active: Boolean)"
    - "val users = TableQuery[Users]"
    - 'def age = column[Int]("age")'
    - 'def active = column[Boolean]("active")'

must_not_include:
  # OrderRepository.scala: same Slick pattern, different table
  - OrderRepository
  - findByUser
  - findPending
  - totalByUser
  - "orders"
  # ProductRepository.scala: another Slick repo
  - ProductRepository
  - findAvailable
  - Products
  # DatabaseMigration.scala: migration utility, independent
  - DatabaseMigration
  - migrate
  - rollback
  - pendingMigrations
  # UserService.scala: consumer, doesn't help review queries
  - getAdultUsers
  - searchByName

must_not_include_files:
  - repositories/OrderRepository.scala
  - repositories/ProductRepository.scala
  - migration/DatabaseMigration.scala
  - services/UserService.scala

max_fragments: 6
max_files: 3
add_garbage_files: false
skip_garbage_check: true
