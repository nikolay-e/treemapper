initial:
  src/main/java/com/example/BaseEntity.java: |
    package com.example;

    import javax.persistence.*;
    import java.time.LocalDateTime;

    @MappedSuperclass
    public abstract class BaseEntity {
        @Id
        @GeneratedValue(strategy = GenerationType.IDENTITY)
        private Long id;

        @Column(name = "created_at")
        private LocalDateTime createdAt;

        public Long getId() { return id; }
        public LocalDateTime getCreatedAt() { return createdAt; }
    }
  src/main/java/com/example/Product.java: |
    package com.example;

    import javax.persistence.*;

    @Entity
    public class Product extends BaseEntity {
        private String name;
        private Double price;
    }
  GarbageAdapter.java: |
    public class GarbageAdapter {
        public static final String GARBAGE_JVM_030_ADAPTER_MARKER_A = "marker";
        public static final String GARBAGE_JVM_030_BRIDGE_MARKER_B = "marker";
        public void noop() {}
    }
  GarbageFacade.java: |
    public class GarbageFacade {
        public static final String GARBAGE_JVM_030_FACADE_MARKER_C = "marker";
        public static final String GARBAGE_JVM_030_WRAPPER_MARKER_D = "marker";
        public void noop() {}
    }
changed:
  src/main/java/com/example/BaseEntity.java: |
    package com.example;

    import javax.persistence.*;
    import java.time.LocalDateTime;

    @MappedSuperclass
    public abstract class BaseEntity {
        @Id
        @GeneratedValue(strategy = GenerationType.IDENTITY)
        private Long id;

        @Column(name = "created_at")
        private LocalDateTime createdAt;

        @Column(name = "updated_at")
        private LocalDateTime updatedAt;

        @PreUpdate
        protected void onUpdate() {
            updatedAt = LocalDateTime.now();
        }

        public Long getId() { return id; }
        public LocalDateTime getCreatedAt() { return createdAt; }
        public LocalDateTime getUpdatedAt() { return updatedAt; }
    }
  src/main/java/com/example/Product.java: |
    package com.example;

    import javax.persistence.*;

    @Entity
    public class Product extends BaseEntity {
        private String name;
        private Double price;
    }
  GarbageAdapter.java: |
    public class GarbageAdapter {
        public static final String GARBAGE_JVM_030_ADAPTER_MARKER_A = "marker";
        public static final String GARBAGE_JVM_030_BRIDGE_MARKER_B = "marker";
        public void noop() {}
    }
  GarbageFacade.java: |
    public class GarbageFacade {
        public static final String GARBAGE_JVM_030_FACADE_MARKER_C = "marker";
        public static final String GARBAGE_JVM_030_WRAPPER_MARKER_D = "marker";
        public void noop() {}
    }
must_include_files:
- src/main/java/com/example/BaseEntity.java
must_include_content:
- '@Column(name = "updated_at")'
must_not_include:
- GARBAGE_JVM_030_ADAPTER_MARKER_A
- GARBAGE_JVM_030_BRIDGE_MARKER_B
- GARBAGE_JVM_030_FACADE_MARKER_C
- GARBAGE_JVM_030_WRAPPER_MARKER_D
- GarbageAdapter.java
- GarbageFacade.java
add_garbage_files: false
skip_garbage_check: true
