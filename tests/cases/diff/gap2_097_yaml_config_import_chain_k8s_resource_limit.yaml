name: gap2_097_yaml_config_import_chain_k8s_resource_limit
tags:
- change:config
- change:single_file
- context:import_chain
- difficulty:trivial
- distance:0hop
- lang:yaml
repo:
  initial_files:
    k8s/deployment.yaml: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: worker
      spec:
        replicas: 2
        template:
          spec:
            containers:
            - name: worker
              image: worker:latest
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
    k8s/service.yaml: |
      apiVersion: v1
      kind: Service
      metadata:
        name: worker
      spec:
        selector:
          app: worker
        ports:
        - port: 8080
          targetPort: 8080
    k8s/networkpolicy.yaml: |
      apiVersion: networking.k8s.io/v1
      kind: NetworkPolicy
      metadata:
        name: TRAP_GAP2_097_NETPOL_A
      spec:
        podSelector:
          matchLabels:
            role: TRAP_GAP2_097_NETPOL_B
  changed_files:
    k8s/deployment.yaml: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: worker
      spec:
        replicas: 2
        template:
          spec:
            containers:
            - name: worker
              image: worker:latest
              resources:
                requests:
                  cpu: 250m
                  memory: 256Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
  commit_message: Update files
fragments:
- id: cpu_request
  selector:
    path: k8s/deployment.yaml
    anchor: "cpu: 250m"
- id: memory_limit
  selector:
    path: k8s/deployment.yaml
    anchor: "memory: 512Mi"
- id: no_netpol
  selector:
    path: k8s/networkpolicy.yaml
- id: no_global_0
  selector:
    anchor: TRAP_GAP2_097_NETPOL_A
- id: no_global_1
  selector:
    anchor: TRAP_GAP2_097_NETPOL_B
oracle:
  required:
  - cpu_request
  - memory_limit
  forbidden:
  - no_netpol
  - no_global_0
  - no_global_1
