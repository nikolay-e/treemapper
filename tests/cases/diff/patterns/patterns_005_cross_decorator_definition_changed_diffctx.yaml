must_include_files:
  - decorators.py
must_include_content:
  - |
    def cache(ttl=60):
        def decorator(func):
            cache_store = {}
            cache_times = {}
must_not_include:
  - GARBAGE_PAT_005_PROFILER_A
  - GARBAGE_PAT_005_SAMPLE_RATE_B
  - GARBAGE_PAT_005_TRACER_A
  - GARBAGE_PAT_005_SPAN_NAME_B
  - GarbageProfiler
  - GarbageTracer
  - garbage_profiler.py
  - garbage_tracer.py
