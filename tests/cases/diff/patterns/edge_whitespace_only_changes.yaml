name: edge_whitespace_only_changes
initial:
  test.py: |
    def hello():
        x = 1
        return x
changed:
  test.py: |
    def hello():
            x = 1
            return x
assertions:
  must_include:
  - test.py
