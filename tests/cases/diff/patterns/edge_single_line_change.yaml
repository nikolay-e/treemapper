name: edge_single_line_change
initial:
  test.py: |
    def calculate():
        return x + 1
changed:
  test.py: |
    def calculate():
        return x + 2
assertions:
  must_include:
  - calculate
