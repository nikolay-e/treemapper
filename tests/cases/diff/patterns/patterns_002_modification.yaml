# Scenario: conftest.py fixture for UserService refactored to add teardown and admin_user fixture.
#
# Diff: tests/conftest.py changes user_service fixture (yield + clear) and adds admin_user fixture.
#
# Reviewer questions:
#   Q1: "Does the teardown (service.users.clear()) affect existing tests?"
#       -> tests/conftest.py (CHANGED) shows the fixture now yields and clears.
#          tests/test_user_service.py (UNCHANGED) uses user_service and sample_user.
#   Q2: "What is admin_user used for?"
#       -> The new admin_user fixture creates a user with is_admin=True.
#
# Not needed:
#   - utils/password_hasher.py: hashes and verifies passwords with bcrypt-like logic.
#     Shares "user" vocabulary but no import or fixture dependency on UserService.
#   - utils/session_store.py: in-memory session token storage.
#     Related to authentication but has no dependency on conftest or UserService.

initial:
  src/user_service.py: |
    from typing import Optional

    class User:
        def __init__(self, id: int, name: str, email: str):
            self.id = id
            self.name = name
            self.email = email

    class UserService:
        def __init__(self):
            self.users = {}

        def create_user(self, name: str, email: str) -> User:
            user_id = len(self.users) + 1
            user = User(user_id, name, email)
            self.users[user_id] = user
            return user

        def get_user(self, user_id: int) -> Optional[User]:
            return self.users.get(user_id)

        def list_users(self) -> list[User]:
            return list(self.users.values())
  tests/conftest.py: |
    import pytest
    from src.user_service import UserService

    @pytest.fixture
    def user_service():
        return UserService()

    @pytest.fixture
    def sample_user(user_service):
        return user_service.create_user("Test", "test@example.com")
  tests/test_user_service.py: |
    def test_create_user(user_service):
        user = user_service.create_user("John", "john@example.com")
        assert user.name == "John"

    def test_get_user(user_service, sample_user):
        retrieved = user_service.get_user(sample_user.id)
        assert retrieved.name == "Test"
  utils/password_hasher.py: |
    import hashlib
    import os

    HASH_ALGORITHM = "sha256"
    SALT_LENGTH = 16

    class PasswordHasher:
        def hash_password(self, password: str) -> str:
            salt = os.urandom(SALT_LENGTH).hex()
            hashed = hashlib.sha256(f"{salt}{password}".encode()).hexdigest()
            return f"{salt}:{hashed}"

        def verify_password(self, password: str, stored_hash: str) -> bool:
            salt, expected = stored_hash.split(":")
            actual = hashlib.sha256(f"{salt}{password}".encode()).hexdigest()
            return actual == expected
  utils/session_store.py: |
    import time

    SESSION_TTL = 3600
    MAX_SESSIONS_PER_USER = 5

    class SessionStore:
        def __init__(self):
            self._sessions: dict[str, dict] = {}

        def create_session(self, user_id: str, token: str) -> None:
            self._sessions[token] = {
                "user_id": user_id,
                "created_at": time.time(),
            }

        def get_session(self, token: str) -> dict | None:
            session = self._sessions.get(token)
            if session and time.time() - session["created_at"] < SESSION_TTL:
                return session
            return None
changed:
  src/user_service.py: |
    from typing import Optional

    class User:
        def __init__(self, id: int, name: str, email: str):
            self.id = id
            self.name = name
            self.email = email

    class UserService:
        def __init__(self):
            self.users = {}

        def create_user(self, name: str, email: str) -> User:
            user_id = len(self.users) + 1
            user = User(user_id, name, email)
            self.users[user_id] = user
            return user

        def get_user(self, user_id: int) -> Optional[User]:
            return self.users.get(user_id)

        def list_users(self) -> list[User]:
            return list(self.users.values())
  tests/conftest.py: |
    import pytest
    from src.user_service import UserService

    @pytest.fixture
    def user_service():
        service = UserService()
        yield service
        service.users.clear()

    @pytest.fixture
    def sample_user(user_service):
        return user_service.create_user("Test", "test@example.com")

    @pytest.fixture
    def admin_user(user_service):
        user = user_service.create_user("Admin", "admin@example.com")
        user.is_admin = True
        return user
  tests/test_user_service.py: |
    def test_create_user(user_service):
        user = user_service.create_user("John", "john@example.com")
        assert user.name == "John"

    def test_get_user(user_service, sample_user):
        retrieved = user_service.get_user(sample_user.id)
        assert retrieved.name == "Test"
  utils/password_hasher.py: |
    import hashlib
    import os

    HASH_ALGORITHM = "sha256"
    SALT_LENGTH = 16

    class PasswordHasher:
        def hash_password(self, password: str) -> str:
            salt = os.urandom(SALT_LENGTH).hex()
            hashed = hashlib.sha256(f"{salt}{password}".encode()).hexdigest()
            return f"{salt}:{hashed}"

        def verify_password(self, password: str, stored_hash: str) -> bool:
            salt, expected = stored_hash.split(":")
            actual = hashlib.sha256(f"{salt}{password}".encode()).hexdigest()
            return actual == expected
  utils/session_store.py: |
    import time

    SESSION_TTL = 3600
    MAX_SESSIONS_PER_USER = 5

    class SessionStore:
        def __init__(self):
            self._sessions: dict[str, dict] = {}

        def create_session(self, user_id: str, token: str) -> None:
            self._sessions[token] = {
                "user_id": user_id,
                "created_at": time.time(),
            }

        def get_session(self, token: str) -> dict | None:
            session = self._sessions.get(token)
            if session and time.time() - session["created_at"] < SESSION_TTL:
                return session
            return None
must_include_files:
  - tests/conftest.py
must_include_content_from:
  tests/conftest.py:
    - "service = UserService()"
    - "def admin_user(user_service):"
must_not_include:
  # utils/password_hasher.py: password hashing, no import edge to UserService or conftest
  - PasswordHasher
  - HASH_ALGORITHM
  - hash_password
  # utils/session_store.py: session token storage, no dependency on conftest
  - SessionStore
  - SESSION_TTL
  - create_session
must_not_include_files:
  - utils/password_hasher.py
  - utils/session_store.py
max_fragments: 5
max_files: 3
add_garbage_files: false
skip_garbage_check: true
