name: helm_025_template_secret
tags:
- lang:markdown
- lang:yaml
- change:single_file
- change:config
- context:same_file
- distance:0hop
- difficulty:trivial
repo:
  initial_files:
    chart/Chart.yaml: |
      apiVersion: v2
      name: secret-app
      version: 1.0.0
    chart/values.yaml: |
      db:
        password: ""
    chart/templates/secret.yaml: |
      apiVersion: v1
      kind: Secret
      metadata:
        name: db-secret
      data: {}
    chart/templates/sealed-secret.yaml: |
      apiVersion: bitnami.com/v1alpha1
      kind: SealedSecret
      metadata:
      spec:
        encryptedData:
    chart/docs/secret-rotation.md: |
      # Secret Rotation
      ## Vault Integration
  changed_files:
    chart/Chart.yaml: |
      apiVersion: v2
      name: secret-app
      version: 1.0.0
    chart/values.yaml: |
      db:
        password: ""
    chart/templates/secret.yaml: |
      apiVersion: v1
      kind: Secret
      metadata:
        name: {{ .Release.Name }}-db-secret
      type: Opaque
      data:
        password: {{ .Values.db.password | b64enc }}
    chart/templates/sealed-secret.yaml: |
      apiVersion: bitnami.com/v1alpha1
      kind: SealedSecret
      metadata:
      spec:
        encryptedData:
    chart/docs/secret-rotation.md: |
      # Secret Rotation
      ## Vault Integration
  commit_message: Update files
fragments:
- id: secret_file
  selector:
    path: chart/templates/secret.yaml
- id: req_global_0
  selector:
    anchor: 'name: {{ .Release.Name }}-db-secret'
oracle:
  required:
  - secret_file
  - req_global_0
  allowed: []
  forbidden: []
