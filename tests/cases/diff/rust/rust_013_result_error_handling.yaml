# Scenario: Replacing unwrap() calls with proper Result/Error handling in a parser module
# Changed: src/parser.rs — added ParseError enum, From impl, made parse/parse_multiple return Result, added try_parse_all
# Reviewer questions: Is the From<ParseIntError> conversion correct? Does NumberParser::new validate radix?
# Trap files:
#   - src/validator.rs: Input validation with custom errors — "error handling" pattern overlap but no shared error types
#   - src/decoder.rs: Binary format decoder with errors — "Result-returning parse" vocabulary but independent
initial:
  src/lib.rs: |
    pub mod parser;
    pub mod validator;
    pub mod decoder;
  src/parser.rs: |
    use std::str::FromStr;

    pub fn parse(s: &str) -> i32 {
        s.parse().unwrap()
    }

    pub fn parse_multiple(inputs: &[&str]) -> Vec<i32> {
        inputs.iter().map(|s| parse(s)).collect()
    }

    pub struct NumberParser {
        radix: u32,
    }

    impl NumberParser {
        pub fn new(radix: u32) -> Self {
            Self { radix }
        }

        pub fn parse(&self, s: &str) -> i32 {
            i32::from_str_radix(s, self.radix).unwrap()
        }
    }

    impl Default for NumberParser {
        fn default() -> Self {
            Self::new(10)
        }
    }
  src/validator.rs: |
    #[derive(Debug)]
    pub enum ValidationError {
        TooShort(usize),
        TooLong(usize),
        InvalidFormat(String),
    }

    pub struct InputValidator {
        min_length: usize,
        max_length: usize,
    }

    impl InputValidator {
        pub fn new(min: usize, max: usize) -> Self {
            Self { min_length: min, max_length: max }
        }

        pub fn validate(&self, input: &str) -> Result<(), ValidationError> {
            if input.len() < self.min_length {
                return Err(ValidationError::TooShort(self.min_length));
            }
            if input.len() > self.max_length {
                return Err(ValidationError::TooLong(self.max_length));
            }
            Ok(())
        }
    }
  src/decoder.rs: |
    #[derive(Debug)]
    pub enum DecodeError {
        InvalidHeader,
        UnexpectedEof,
        ChecksumMismatch,
    }

    pub struct BinaryDecoder {
        data: Vec<u8>,
        position: usize,
    }

    impl BinaryDecoder {
        pub fn new(data: Vec<u8>) -> Self {
            Self { data, position: 0 }
        }

        pub fn read_u32(&mut self) -> Result<u32, DecodeError> {
            if self.position + 4 > self.data.len() {
                return Err(DecodeError::UnexpectedEof);
            }
            let bytes = &self.data[self.position..self.position + 4];
            self.position += 4;
            Ok(u32::from_be_bytes([bytes[0], bytes[1], bytes[2], bytes[3]]))
        }

        pub fn remaining(&self) -> usize {
            self.data.len() - self.position
        }
    }
changed:
  src/lib.rs: |
    pub mod parser;
    pub mod validator;
    pub mod decoder;
  src/parser.rs: |
    use std::num::ParseIntError;
    use std::str::FromStr;
    use std::fmt;

    #[derive(Debug)]
    pub enum ParseError {
        InvalidNumber(ParseIntError),
        EmptyInput,
        InvalidRadix(u32),
    }

    impl fmt::Display for ParseError {
        fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
            match self {
                ParseError::InvalidNumber(e) => write!(f, "Invalid number: {}", e),
                ParseError::EmptyInput => write!(f, "Empty input"),
                ParseError::InvalidRadix(r) => write!(f, "Invalid radix: {}", r),
            }
        }
    }

    impl std::error::Error for ParseError {}

    impl From<ParseIntError> for ParseError {
        fn from(err: ParseIntError) -> Self {
            ParseError::InvalidNumber(err)
        }
    }

    pub fn parse(s: &str) -> Result<i32, ParseIntError> {
        s.parse()
    }

    pub fn parse_or_default(s: &str, default: i32) -> i32 {
        parse(s).unwrap_or(default)
    }

    pub fn parse_multiple(inputs: &[&str]) -> Result<Vec<i32>, ParseError> {
        if inputs.is_empty() {
            return Err(ParseError::EmptyInput);
        }
        inputs.iter()
            .map(|s| parse(s).map_err(ParseError::from))
            .collect()
    }

    pub fn try_parse_all(inputs: &[&str]) -> (Vec<i32>, Vec<ParseError>) {
        let mut successes = Vec::new();
        let mut failures = Vec::new();

        for s in inputs {
            match parse(s) {
                Ok(n) => successes.push(n),
                Err(e) => failures.push(ParseError::from(e)),
            }
        }

        (successes, failures)
    }

    pub struct NumberParser {
        radix: u32,
    }

    impl NumberParser {
        pub fn new(radix: u32) -> Result<Self, ParseError> {
            if radix < 2 || radix > 36 {
                return Err(ParseError::InvalidRadix(radix));
            }
            Ok(Self { radix })
        }

        pub fn parse(&self, s: &str) -> Result<i32, ParseError> {
            i32::from_str_radix(s, self.radix).map_err(ParseError::from)
        }

        pub fn parse_or(&self, s: &str, default: i32) -> i32 {
            self.parse(s).unwrap_or(default)
        }
    }

    impl Default for NumberParser {
        fn default() -> Self {
            Self { radix: 10 }
        }
    }
  src/validator.rs: |
    #[derive(Debug)]
    pub enum ValidationError {
        TooShort(usize),
        TooLong(usize),
        InvalidFormat(String),
    }

    pub struct InputValidator {
        min_length: usize,
        max_length: usize,
    }

    impl InputValidator {
        pub fn new(min: usize, max: usize) -> Self {
            Self { min_length: min, max_length: max }
        }

        pub fn validate(&self, input: &str) -> Result<(), ValidationError> {
            if input.len() < self.min_length {
                return Err(ValidationError::TooShort(self.min_length));
            }
            if input.len() > self.max_length {
                return Err(ValidationError::TooLong(self.max_length));
            }
            Ok(())
        }
    }
  src/decoder.rs: |
    #[derive(Debug)]
    pub enum DecodeError {
        InvalidHeader,
        UnexpectedEof,
        ChecksumMismatch,
    }

    pub struct BinaryDecoder {
        data: Vec<u8>,
        position: usize,
    }

    impl BinaryDecoder {
        pub fn new(data: Vec<u8>) -> Self {
            Self { data, position: 0 }
        }

        pub fn read_u32(&mut self) -> Result<u32, DecodeError> {
            if self.position + 4 > self.data.len() {
                return Err(DecodeError::UnexpectedEof);
            }
            let bytes = &self.data[self.position..self.position + 4];
            self.position += 4;
            Ok(u32::from_be_bytes([bytes[0], bytes[1], bytes[2], bytes[3]]))
        }

        pub fn remaining(&self) -> usize {
            self.data.len() - self.position
        }
    }
must_include_files:
- src/parser.rs
must_include_content:
- use std::num::ParseIntError;
- pub enum ParseError {
must_not_include:
- ValidationError
- DecodeError
- BinaryDecoder
- validator.rs
- decoder.rs
must_not_include_files:
- src/validator.rs
- src/decoder.rs
max_files: 3
add_garbage_files: false
skip_garbage_check: true
