# Scenario: Flask database.py adds to_dict(), TimestampMixin, SoftDeleteMixin,
#   and get_db_session() to the extension initialization module.
#
# Reviewer questions:
#   - Are the new mixins composable with the existing BaseModel?
#   - Does factory.py need changes for the new get_db_session()?
#
# Trap files:
#   - extensions/cache_backend.py: Flask extension pattern with init_app,
#     same vocabulary but independent
#   - extensions/mail_service.py: another extension with similar constructor,
#     no dependency on database.py

max_enrichment: 5.8
initial:
  database.py: |
    from flask_sqlalchemy import SQLAlchemy
    from flask_migrate import Migrate
    from typing import Optional, List, Dict, Any
    from datetime import datetime

    db = SQLAlchemy()
    migrate = Migrate()

    class BaseModel(db.Model):
        __abstract__ = True

        id = db.Column(db.Integer, primary_key=True)
        created_at = db.Column(db.DateTime, default=datetime.utcnow)
        updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

        def save(self) -> None:
            db.session.add(self)
            db.session.commit()

        def delete(self) -> None:
            db.session.delete(self)
            db.session.commit()

        @classmethod
        def get_by_id(cls, id: int) -> Optional["BaseModel"]:
            return cls.query.get(id)

        @classmethod
        def get_all(cls) -> List["BaseModel"]:
            return cls.query.all()

    def init_extensions(app) -> None:
        db.init_app(app)
        migrate.init_app(app, db)

        with app.app_context():
            db.create_all()
  factory.py: |
    from flask import Flask
    from database import init_extensions

    def create_app() -> Flask:
        app = Flask(__name__)
        init_extensions(app)
        return app
  extensions/cache_backend.py: |
    from typing import Any, Optional

    class CacheBackend:
        def __init__(self, app=None, default_ttl: int = 300):
            self.default_ttl = default_ttl
            self._store: dict[str, Any] = {}
            if app is not None:
                self.init_app(app)

        def init_app(self, app) -> None:
            app.extensions["cache"] = self

        def get(self, key: str) -> Optional[Any]:
            return self._store.get(key)

        def set(self, key: str, value: Any, ttl: int = None) -> None:
            self._store[key] = value
  extensions/mail_service.py: |
    from typing import Optional

    class MailService:
        def __init__(self, app=None, smtp_host: str = "localhost"):
            self.smtp_host = smtp_host
            if app is not None:
                self.init_app(app)

        def init_app(self, app) -> None:
            self.smtp_host = app.config.get("SMTP_HOST", self.smtp_host)
            app.extensions["mail"] = self

        def send(self, to: str, subject: str, body: str) -> bool:
            return True

changed:
  database.py: |
    from flask_sqlalchemy import SQLAlchemy
    from flask_migrate import Migrate
    from typing import Optional, List, Dict, Any
    from datetime import datetime

    db = SQLAlchemy()
    migrate = Migrate()

    class BaseModel(db.Model):
        __abstract__ = True

        id = db.Column(db.Integer, primary_key=True)
        created_at = db.Column(db.DateTime, default=datetime.utcnow)
        updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

        def save(self) -> None:
            db.session.add(self)
            db.session.commit()

        def delete(self) -> None:
            db.session.delete(self)
            db.session.commit()

        @classmethod
        def get_by_id(cls, id: int) -> Optional["BaseModel"]:
            return cls.query.get(id)

        @classmethod
        def get_all(cls) -> List["BaseModel"]:
            return cls.query.all()

        def to_dict(self) -> Dict[str, Any]:
            return {c.name: getattr(self, c.name) for c in self.__table__.columns}

    class TimestampMixin:
        created_at = db.Column(db.DateTime, default=datetime.utcnow, nullable=False)
        updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    class SoftDeleteMixin:
        deleted_at = db.Column(db.DateTime, nullable=True)
        is_deleted = db.Column(db.Boolean, default=False, nullable=False)

        def soft_delete(self) -> None:
            self.is_deleted = True
            self.deleted_at = datetime.utcnow()
            db.session.commit()

        def restore(self) -> None:
            self.is_deleted = False
            self.deleted_at = None
            db.session.commit()

    def init_extensions(app) -> None:
        db.init_app(app)
        migrate.init_app(app, db)

        with app.app_context():
            db.create_all()

    def get_db_session():
        return db.session
  factory.py: |
    from flask import Flask
    from database import init_extensions

    def create_app() -> Flask:
        app = Flask(__name__)
        init_extensions(app)
        return app
  extensions/cache_backend.py: |
    from typing import Any, Optional

    class CacheBackend:
        def __init__(self, app=None, default_ttl: int = 300):
            self.default_ttl = default_ttl
            self._store: dict[str, Any] = {}
            if app is not None:
                self.init_app(app)

        def init_app(self, app) -> None:
            app.extensions["cache"] = self

        def get(self, key: str) -> Optional[Any]:
            return self._store.get(key)

        def set(self, key: str, value: Any, ttl: int = None) -> None:
            self._store[key] = value
  extensions/mail_service.py: |
    from typing import Optional

    class MailService:
        def __init__(self, app=None, smtp_host: str = "localhost"):
            self.smtp_host = smtp_host
            if app is not None:
                self.init_app(app)

        def init_app(self, app) -> None:
            self.smtp_host = app.config.get("SMTP_HOST", self.smtp_host)
            app.extensions["mail"] = self

        def send(self, to: str, subject: str, body: str) -> bool:
            return True

must_include_files:
- database.py
must_include_content:
- 'def to_dict(self) -> Dict[str, Any]:'
- 'class TimestampMixin:'
must_not_include:
- CacheBackend
- MailService
- smtp_host
- default_ttl
must_not_include_files:
- extensions/cache_backend.py
- extensions/mail_service.py
max_fragments: 6
max_files: 3
add_garbage_files: false
skip_garbage_check: true
