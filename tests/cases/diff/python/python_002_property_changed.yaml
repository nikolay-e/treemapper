# Scenario: User.full_name property changed to support optional title prefix
# Changed: user.py — __init__ gets title param, full_name property adds title logic
# Reviewer needs: user.py (changed) + views.py (unchanged, uses user.full_name extensively)
# Trap: payment.py — financial service with similar __init__ pattern, unrelated domain
# Trap: inventory.py — has similar property-based getters and role checks, no dependency

initial:
  user.py: |
    from typing import Optional, List
    from datetime import datetime, date
    from enum import Enum

    class UserRole(Enum):
        ADMIN = "admin"
        MODERATOR = "moderator"
        USER = "user"
        GUEST = "guest"

    class User:
        def __init__(self, first: str, last: str, email: str = "", role: UserRole = UserRole.USER):
            self.first = first
            self.last = last
            self.email = email
            self.role = role
            self.created_at = datetime.now()
            self._last_login: Optional[datetime] = None
            self._failed_attempts = 0

        @property
        def full_name(self) -> str:
            return f"{self.first} {self.last}"

        @property
        def is_admin(self) -> bool:
            return self.role == UserRole.ADMIN

        @property
        def last_login(self) -> Optional[datetime]:
            return self._last_login

        @last_login.setter
        def last_login(self, value: datetime) -> None:
            self._last_login = value
            self._failed_attempts = 0

        def record_failed_login(self) -> None:
            self._failed_attempts += 1

        def is_locked(self) -> bool:
            return self._failed_attempts >= 5

        def has_permission(self, permission: str) -> bool:
            admin_permissions = ["delete", "ban", "edit_all"]
            if permission in admin_permissions:
                return self.is_admin
            return True
  views.py: |
    from user import User, UserRole

    class UserView:
        def __init__(self, template_engine):
            self.template_engine = template_engine

        def render_user(self, user: User) -> str:
            return f"Name: {user.full_name}"

        def render_user_card(self, user: User) -> str:
            role_badge = self._get_role_badge(user.role)
            return f"<div class='user-card'><h2>{user.full_name}</h2>{role_badge}</div>"

        def render_user_list(self, users: List[User]) -> str:
            items = [f"<li>{u.full_name}</li>" for u in users]
            return f"<ul>{''.join(items)}</ul>"

        def _get_role_badge(self, role: UserRole) -> str:
            badges = {
                UserRole.ADMIN: "<span class='badge admin'>Admin</span>",
                UserRole.MODERATOR: "<span class='badge mod'>Mod</span>",
                UserRole.USER: "",
                UserRole.GUEST: "<span class='badge guest'>Guest</span>",
            }
            return badges.get(role, "")
  payment.py: |
    class PaymentProcessor:
        def __init__(self, api_key: str):
            self.api_key = api_key

        def charge(self, amount: float, card_token: str) -> dict:
            return {"success": True, "transaction_id": "tx_123"}

        def refund(self, transaction_id: str) -> bool:
            return True

    class StripeGateway:
        def create_customer(self, email: str) -> str:
            return "cus_abc123"
  inventory.py: |
    class InventoryManager:
        def __init__(self):
            self.stock = {}

        @property
        def total_items(self) -> int:
            return sum(self.stock.values())

        def check_stock(self, product_id: int) -> int:
            return self.stock.get(product_id, 0)

        def reserve_stock(self, product_id: int, quantity: int) -> bool:
            return True

        def release_stock(self, product_id: int, quantity: int) -> None:
            pass

    def calculate_reorder_point(daily_sales: float, lead_time: int) -> float:
        return daily_sales * lead_time * 1.5

changed:
  user.py: |
    from typing import Optional, List
    from datetime import datetime, date
    from enum import Enum

    class UserRole(Enum):
        ADMIN = "admin"
        MODERATOR = "moderator"
        USER = "user"
        GUEST = "guest"

    class User:
        def __init__(self, first: str, last: str, email: str = "", role: UserRole = UserRole.USER, title: Optional[str] = None):
            self.first = first
            self.last = last
            self.email = email
            self.role = role
            self.title = title
            self.created_at = datetime.now()
            self._last_login: Optional[datetime] = None
            self._failed_attempts = 0

        @property
        def full_name(self) -> str:
            if self.title:
                return f"{self.title} {self.first} {self.last}"
            return f"{self.first} {self.last}"

        @property
        def is_admin(self) -> bool:
            return self.role == UserRole.ADMIN

        @property
        def last_login(self) -> Optional[datetime]:
            return self._last_login

        @last_login.setter
        def last_login(self, value: datetime) -> None:
            self._last_login = value
            self._failed_attempts = 0

        def record_failed_login(self) -> None:
            self._failed_attempts += 1

        def is_locked(self) -> bool:
            return self._failed_attempts >= 5

        def has_permission(self, permission: str) -> bool:
            admin_permissions = ["delete", "ban", "edit_all"]
            if permission in admin_permissions:
                return self.is_admin
            return True
  views.py: |
    from user import User, UserRole

    class UserView:
        def __init__(self, template_engine):
            self.template_engine = template_engine

        def render_user(self, user: User) -> str:
            return f"Name: {user.full_name}"

        def render_user_card(self, user: User) -> str:
            role_badge = self._get_role_badge(user.role)
            return f"<div class='user-card'><h2>{user.full_name}</h2>{role_badge}</div>"

        def render_user_list(self, users: List[User]) -> str:
            items = [f"<li>{u.full_name}</li>" for u in users]
            return f"<ul>{''.join(items)}</ul>"

        def _get_role_badge(self, role: UserRole) -> str:
            badges = {
                UserRole.ADMIN: "<span class='badge admin'>Admin</span>",
                UserRole.MODERATOR: "<span class='badge mod'>Mod</span>",
                UserRole.USER: "",
                UserRole.GUEST: "<span class='badge guest'>Guest</span>",
            }
            return badges.get(role, "")
  payment.py: |
    class PaymentProcessor:
        def __init__(self, api_key: str):
            self.api_key = api_key

        def charge(self, amount: float, card_token: str) -> dict:
            return {"success": True, "transaction_id": "tx_123"}

        def refund(self, transaction_id: str) -> bool:
            return True

    class StripeGateway:
        def create_customer(self, email: str) -> str:
            return "cus_abc123"
  inventory.py: |
    class InventoryManager:
        def __init__(self):
            self.stock = {}

        @property
        def total_items(self) -> int:
            return sum(self.stock.values())

        def check_stock(self, product_id: int) -> int:
            return self.stock.get(product_id, 0)

        def reserve_stock(self, product_id: int, quantity: int) -> bool:
            return True

        def release_stock(self, product_id: int, quantity: int) -> None:
            pass

    def calculate_reorder_point(daily_sales: float, lead_time: int) -> float:
        return daily_sales * lead_time * 1.5

must_include_files:
- user.py

must_include_content_from:
  user.py:
    - 'title: Optional[str] = None'
    - 'if self.title:'

must_not_include:
- PaymentProcessor
- StripeGateway
- InventoryManager
- calculate_reorder_point
- check_stock
- charge

must_not_include_files:
- payment.py
- inventory.py

max_fragments: 6
max_files: 3
add_garbage_files: false
skip_garbage_check: true
