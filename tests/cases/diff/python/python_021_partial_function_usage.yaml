# Scenario: ReportGenerator uses partial functions from formatters module
# Changed: reports.py imports partial-based formatters and uses them for report generation
# Reviewer questions: Are partial args applied correctly? Does format_currency handle negatives?
# Trap 1: chart_renderer.py — renders charts for reports, shares "report" vocabulary but independent
# Trap 2: pdf_exporter.py — exports to PDF, shares "report/format" domain but no dep on formatters
initial:
  formatters.py: |
    import functools
    from typing import Optional, Callable
    from dataclasses import dataclass
    from decimal import Decimal, ROUND_HALF_UP

    @dataclass
    class FormatOptions:
        precision: int = 2
        prefix: str = ""
        suffix: str = ""
        thousands_separator: bool = True

    def format_number(
        value: float,
        precision: int = 2,
        prefix: str = "",
        suffix: str = ""
    ) -> str:
        if not isinstance(value, (int, float, Decimal)):
            return str(value)
        decimal_value = Decimal(str(value))
        quantize_str = "0." + "0" * precision if precision > 0 else "0"
        rounded = decimal_value.quantize(Decimal(quantize_str), rounding=ROUND_HALF_UP)
        return f"{prefix}{rounded}{suffix}"

    format_currency = functools.partial(format_number, precision=2, prefix="$")
    format_percent = functools.partial(format_number, precision=1, suffix="%")

    class FormatterRegistry:
        def __init__(self):
            self._formatters: dict[str, Callable] = {}

        def register(self, name: str, formatter: Callable) -> None:
            self._formatters[name] = formatter

        def get(self, name: str) -> Optional[Callable]:
            return self._formatters.get(name)
  reports.py: |
    def generate():
        pass
  chart_renderer.py: |
    from typing import List, Dict, Any, Optional
    from dataclasses import dataclass

    @dataclass
    class ChartConfig:
        title: str
        chart_type: str = "bar"
        width: int = 800
        height: int = 400

    class ChartRenderer:
        def __init__(self, config: ChartConfig):
            self._config = config

        def render_bar_chart(self, data: Dict[str, float]) -> str:
            return f"<svg>{self._config.title}</svg>"

        def render_line_chart(self, data: List[float]) -> str:
            return f"<svg>line:{len(data)}</svg>"
  pdf_exporter.py: |
    from typing import List, Dict, Any
    from dataclasses import dataclass

    @dataclass
    class PdfConfig:
        page_size: str = "A4"
        margin: int = 72
        font_size: int = 12

    class PdfExporter:
        def __init__(self, config: PdfConfig):
            self._config = config
            self._pages: List[str] = []

        def add_page(self, content: str) -> None:
            self._pages.append(content)

        def export(self, filename: str) -> str:
            return f"{filename}.pdf"

        def get_page_count(self) -> int:
            return len(self._pages)
changed:
  formatters.py: |
    import functools
    from typing import Optional, Callable
    from dataclasses import dataclass
    from decimal import Decimal, ROUND_HALF_UP

    @dataclass
    class FormatOptions:
        precision: int = 2
        prefix: str = ""
        suffix: str = ""
        thousands_separator: bool = True

    def format_number(
        value: float,
        precision: int = 2,
        prefix: str = "",
        suffix: str = ""
    ) -> str:
        if not isinstance(value, (int, float, Decimal)):
            return str(value)
        decimal_value = Decimal(str(value))
        quantize_str = "0." + "0" * precision if precision > 0 else "0"
        rounded = decimal_value.quantize(Decimal(quantize_str), rounding=ROUND_HALF_UP)
        return f"{prefix}{rounded}{suffix}"

    format_currency = functools.partial(format_number, precision=2, prefix="$")
    format_percent = functools.partial(format_number, precision=1, suffix="%")

    class FormatterRegistry:
        def __init__(self):
            self._formatters: dict[str, Callable] = {}

        def register(self, name: str, formatter: Callable) -> None:
            self._formatters[name] = formatter

        def get(self, name: str) -> Optional[Callable]:
            return self._formatters.get(name)
  reports.py: |
    from formatters import format_currency, format_percent, FormatterRegistry

    class FinancialReport:
        def __init__(self):
            self.registry = FormatterRegistry()
            self.registry.register("usd", format_currency)
            self.registry.register("pct", format_percent)

        def format_amount(self, amount: float) -> str:
            formatter = self.registry.get("usd")
            return formatter(amount) if formatter else str(amount)

    def generate(amount: float) -> str:
        return format_currency(amount)

    def generate_summary(amounts: list[float]) -> dict:
        total = sum(amounts)
        return {
            "total": format_currency(total),
            "items": [format_currency(a) for a in amounts]
        }
  chart_renderer.py: |
    from typing import List, Dict, Any, Optional
    from dataclasses import dataclass

    @dataclass
    class ChartConfig:
        title: str
        chart_type: str = "bar"
        width: int = 800
        height: int = 400

    class ChartRenderer:
        def __init__(self, config: ChartConfig):
            self._config = config

        def render_bar_chart(self, data: Dict[str, float]) -> str:
            return f"<svg>{self._config.title}</svg>"

        def render_line_chart(self, data: List[float]) -> str:
            return f"<svg>line:{len(data)}</svg>"
  pdf_exporter.py: |
    from typing import List, Dict, Any
    from dataclasses import dataclass

    @dataclass
    class PdfConfig:
        page_size: str = "A4"
        margin: int = 72
        font_size: int = 12

    class PdfExporter:
        def __init__(self, config: PdfConfig):
            self._config = config
            self._pages: List[str] = []

        def add_page(self, content: str) -> None:
            self._pages.append(content)

        def export(self, filename: str) -> str:
            return f"{filename}.pdf"

        def get_page_count(self) -> int:
            return len(self._pages)
must_include_files:
- reports.py
- formatters.py
must_include_content_from:
  reports.py:
  - 'from formatters import format_currency, format_percent, FormatterRegistry'
  - 'class FinancialReport:'
must_not_include:
- ChartRenderer
- ChartConfig
- render_bar_chart
- PdfExporter
- PdfConfig
- add_page
must_not_include_files:
- chart_renderer.py
- pdf_exporter.py
add_garbage_files: false
skip_garbage_check: true
max_fragments: 10
max_files: 5
