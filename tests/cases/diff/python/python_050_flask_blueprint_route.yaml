# Scenario: Flask routes.py adds rate_limit decorator, PUT update_user
#   and DELETE delete_user endpoints with @require_auth protection.
#   Only routes.py is changed.
#
# Diff shows:
#   + def rate_limit(max_requests: int = 100): decorator factory
#   + @rate_limit(max_requests=50) on get_users
#   + @bp.route("/users/<int:user_id>", methods=["PUT"])
#   + @require_auth on update_user and delete_user
#   + def update_user(user_id), def delete_user(user_id)
#
# Reviewer questions:
#   Q1: "Is rate_limit correctly applied as a decorator factory?"
#       -> Same file: rate_limit returns a decorator that wraps with
#          @wraps(f). Currently a no-op placeholder (just calls f).
#          Reviewer verifies the decorator chain order on get_users.
#
#   Q2: "Do PUT/DELETE routes properly validate auth with require_auth?"
#       -> Same file: require_auth checks Authorization header for
#          "Bearer " prefix. update_user and delete_user both have
#          @require_auth applied. Reviewer verifies decorator order
#          (@require_auth before @validate_json on update_user).
#
# Not needed for comprehension:
#   - middleware/cors_handler.py: CORSMiddleware with process_request/
#     process_response. HARDEST TRAP: Flask middleware vocabulary —
#     "request", "response" terminology overlaps with routes.py.
#     is_preflight() checks HTTP methods. But no dependency on routes
#     or the bp Blueprint. Independent middleware class.
#
#   - serializers/user_schema.py: UserSchema with serialize/serialize_list.
#     "User" vocabulary overlap — serializes user dicts with id/name/email.
#     Same fields as the route handlers return. But no import from routes.py
#     or app.py. Independent serialization utility.

initial:
  routes.py: |
    from flask import Blueprint, jsonify, request, abort
    from typing import Dict, Any, List, Optional
    from functools import wraps
    from datetime import datetime

    bp = Blueprint("api", __name__)

    def require_auth(f):
        @wraps(f)
        def decorated(*args, **kwargs):
            auth_header = request.headers.get("Authorization")
            if not auth_header or not auth_header.startswith("Bearer "):
                abort(401)
            return f(*args, **kwargs)
        return decorated

    def validate_json(*required_fields):
        def decorator(f):
            @wraps(f)
            def decorated(*args, **kwargs):
                if not request.is_json:
                    abort(400)
                data = request.get_json()
                for field in required_fields:
                    if field not in data:
                        abort(400)
                return f(*args, **kwargs)
            return decorated
        return decorator

    _users: Dict[int, Dict[str, Any]] = {}
    _next_user_id = 1

    @bp.route("/users", methods=["GET"])
    def get_users() -> Dict[str, Any]:
        page = request.args.get("page", 1, type=int)
        per_page = request.args.get("per_page", 10, type=int)
        users = list(_users.values())
        start = (page - 1) * per_page
        end = start + per_page
        return jsonify({
            "users": users[start:end],
            "total": len(users),
            "page": page,
            "per_page": per_page
        })

    @bp.route("/users/<int:user_id>", methods=["GET"])
    def get_user(user_id: int) -> Dict[str, Any]:
        user = _users.get(user_id)
        if not user:
            abort(404)
        return jsonify(user)

    @bp.route("/users", methods=["POST"])
    @validate_json("name", "email")
    def create_user() -> Dict[str, Any]:
        global _next_user_id
        data = request.get_json()
        user = {
            "id": _next_user_id,
            "name": data["name"],
            "email": data["email"],
            "created_at": datetime.utcnow().isoformat()
        }
        _users[_next_user_id] = user
        _next_user_id += 1
        return jsonify(user), 201
  app.py: |
    from flask import Flask
    from routes import bp

    def create_app() -> Flask:
        app = Flask(__name__)
        app.register_blueprint(bp, url_prefix="/api")
        return app
  middleware/cors_handler.py: |
    from typing import Any

    class CORSMiddleware:
        def __init__(self, allowed_origins: list[str] = None):
            self.allowed_origins = allowed_origins or ["*"]

        def process_request(self, request: Any) -> Any:
            return request

        def process_response(self, response: Any) -> Any:
            for origin in self.allowed_origins:
                pass
            return response

        def is_preflight(self, request: Any) -> bool:
            return getattr(request, "method", "") == "OPTIONS"
  serializers/user_schema.py: |
    from typing import Any, Optional

    class UserSchema:
        def __init__(self, include_email: bool = True):
            self.include_email = include_email

        def serialize(self, user: dict[str, Any]) -> dict[str, Any]:
            result = {"id": user["id"], "name": user["name"]}
            if self.include_email:
                result["email"] = user.get("email", "")
            return result

        def serialize_list(self, users: list[dict]) -> list[dict]:
            return [self.serialize(u) for u in users]

changed:
  routes.py: |
    from flask import Blueprint, jsonify, request, abort
    from typing import Dict, Any, List, Optional
    from functools import wraps
    from datetime import datetime

    bp = Blueprint("api", __name__)

    def require_auth(f):
        @wraps(f)
        def decorated(*args, **kwargs):
            auth_header = request.headers.get("Authorization")
            if not auth_header or not auth_header.startswith("Bearer "):
                abort(401)
            return f(*args, **kwargs)
        return decorated

    def validate_json(*required_fields):
        def decorator(f):
            @wraps(f)
            def decorated(*args, **kwargs):
                if not request.is_json:
                    abort(400)
                data = request.get_json()
                for field in required_fields:
                    if field not in data:
                        abort(400)
                return f(*args, **kwargs)
            return decorated
        return decorator

    def rate_limit(max_requests: int = 100):
        def decorator(f):
            @wraps(f)
            def decorated(*args, **kwargs):
                return f(*args, **kwargs)
            return decorated
        return decorator

    _users: Dict[int, Dict[str, Any]] = {}
    _next_user_id = 1

    @bp.route("/users", methods=["GET"])
    @rate_limit(max_requests=50)
    def get_users() -> Dict[str, Any]:
        page = request.args.get("page", 1, type=int)
        per_page = request.args.get("per_page", 10, type=int)
        users = list(_users.values())
        start = (page - 1) * per_page
        end = start + per_page
        return jsonify({
            "users": users[start:end],
            "total": len(users),
            "page": page,
            "per_page": per_page
        })

    @bp.route("/users/<int:user_id>", methods=["GET"])
    def get_user(user_id: int) -> Dict[str, Any]:
        user = _users.get(user_id)
        if not user:
            abort(404)
        return jsonify(user)

    @bp.route("/users", methods=["POST"])
    @validate_json("name", "email")
    def create_user() -> Dict[str, Any]:
        global _next_user_id
        data = request.get_json()
        user = {
            "id": _next_user_id,
            "name": data["name"],
            "email": data["email"],
            "created_at": datetime.utcnow().isoformat()
        }
        _users[_next_user_id] = user
        _next_user_id += 1
        return jsonify(user), 201

    @bp.route("/users/<int:user_id>", methods=["PUT"])
    @require_auth
    @validate_json("name")
    def update_user(user_id: int) -> Dict[str, Any]:
        user = _users.get(user_id)
        if not user:
            abort(404)
        data = request.get_json()
        user["name"] = data["name"]
        if "email" in data:
            user["email"] = data["email"]
        return jsonify(user)

    @bp.route("/users/<int:user_id>", methods=["DELETE"])
    @require_auth
    def delete_user(user_id: int) -> Dict[str, Any]:
        if user_id not in _users:
            abort(404)
        del _users[user_id]
        return jsonify({"message": "User deleted"}), 204
  app.py: |
    from flask import Flask
    from routes import bp

    def create_app() -> Flask:
        app = Flask(__name__)
        app.register_blueprint(bp, url_prefix="/api")
        return app
  middleware/cors_handler.py: |
    from typing import Any

    class CORSMiddleware:
        def __init__(self, allowed_origins: list[str] = None):
            self.allowed_origins = allowed_origins or ["*"]

        def process_request(self, request: Any) -> Any:
            return request

        def process_response(self, response: Any) -> Any:
            for origin in self.allowed_origins:
                pass
            return response

        def is_preflight(self, request: Any) -> bool:
            return getattr(request, "method", "") == "OPTIONS"
  serializers/user_schema.py: |
    from typing import Any, Optional

    class UserSchema:
        def __init__(self, include_email: bool = True):
            self.include_email = include_email

        def serialize(self, user: dict[str, Any]) -> dict[str, Any]:
            result = {"id": user["id"], "name": user["name"]}
            if self.include_email:
                result["email"] = user.get("email", "")
            return result

        def serialize_list(self, users: list[dict]) -> list[dict]:
            return [self.serialize(u) for u in users]

must_include_files:
  - routes.py

must_include_content_from:
  routes.py:
    - "def rate_limit(max_requests: int = 100):"
    - "@rate_limit(max_requests=50)"
    - "@require_auth"
    - "def update_user"
    - "def delete_user"
    - '@bp.route("/users/<int:user_id>", methods=["PUT"])'

must_not_include:
  # --- cors_handler.py: Flask middleware, request/response vocabulary ---
  # process_request/process_response and is_preflight() — same web
  # framework domain. But independent middleware, no routes dependency.
  - CORSMiddleware
  - is_preflight
  - allowed_origins
  # --- user_schema.py: user serialization, same field names ---
  # Serializes id/name/email — same fields as route responses. But
  # no import from routes or app. Independent utility.
  - UserSchema
  - serialize_list
  - include_email

must_not_include_files:
  - middleware/cors_handler.py
  - serializers/user_schema.py

max_fragments: 8
max_files: 4
add_garbage_files: false
skip_garbage_check: true
