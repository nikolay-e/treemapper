resolvers.ts: "export const resolvers = {\n    Query: {\n        user: (_: any, { id }: { id: string }) => ({ id, name: 'Test' }),\n    },\n};\n\n// js_garbage_marker_010\n"
schema.ts: "import { resolvers } from './resolvers';\n\nexport const typeDefs = `\n    type User {\n        id: ID!\n        name: String!\n    }\n\n    type Query {\n        user(id: ID!): User\n    }\n\
  `;\n\nexport { resolvers };\n"
