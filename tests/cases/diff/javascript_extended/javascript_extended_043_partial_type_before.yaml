config.ts: |
  export interface FullConfig {
      host: string;
      port: number;
      ssl: boolean;
      timeout: number;
  }

  // ts_ext_garbage_marker_005
  const GARBAGE_CONFIG = { garbage: true };
updater.ts: |
  console.log('initial');
