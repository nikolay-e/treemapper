ExpensiveList.tsx: "import { memo } from 'react';\n\ninterface Item {\n    id: string;\n    name: string;\n}\n\ninterface ExpensiveListProps {\n    items: Item[];\n}\n\nfunction ExpensiveListComponent({\
  \ items }: ExpensiveListProps) {\n    return (\n        <ul>\n            {items.map(item => (\n                <li key={item.id}>{item.name}</li>\n            ))}\n        </ul>\n    );\n}\n\nexport\
  \ const ExpensiveList = memo(ExpensiveListComponent);\n\n// react_adv_garbage_marker_008\n"
Dashboard.tsx: 'console.log(''initial'');

  '
