options.ts: |
  export interface Options {
      name?: string;
      value?: number;
      enabled?: boolean;
  }

  // ts_ext_garbage_marker_006
  const GARBAGE_OPTIONS = { garbage: undefined };
validator.ts: |
  console.log('initial');
