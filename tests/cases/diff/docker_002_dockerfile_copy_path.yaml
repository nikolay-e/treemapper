name: docker_002_dockerfile_copy_path
tags:
- lang:javascript
- lang:shell
- lang:terraform
- change:single_file
- context:config_to_code
- distance:2hop
- difficulty:medium
repo:
  initial_files:
    src/index.js: |
      const express = require('express');
      const cors = require('cors');
      const helmet = require('helmet');

      const app = express();
      app.use(cors());
      app.use(helmet());
      app.use(express.json());

      app.get('/api/health', (req, res) => {
          res.json({ status: 'healthy', timestamp: Date.now() });
      });

      app.get('/api/users', (req, res) => {
          res.json({ users: [] });
      });

      const PORT = process.env.PORT || 3000;
      app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
    src/middleware/auth.js: |
      const jwt = require('jsonwebtoken');

      function authenticateToken(req, res, next) {
          const token = req.headers['authorization']?.split(' ')[1];
          if (!token) return res.sendStatus(401);
          jwt.verify(token, process.env.JWT_SECRET, (err, user) => {
              if (err) return res.sendStatus(403);
              req.user = user;
              next();
          });
      }

      module.exports = { authenticateToken };
    package.json: |
      {
        "name": "api-server",
        "version": "1.0.0",
        "main": "src/index.js",
        "scripts": {
          "start": "node src/index.js",
          "dev": "nodemon src/index.js"
        },
        "dependencies": {
          "express": "^4.18.2",
          "cors": "^2.8.5",
          "helmet": "^7.1.0",
          "jsonwebtoken": "^9.0.0"
        }
      }
    Dockerfile: |
      FROM node:18
      WORKDIR /app
      COPY src/ /app/src/
      CMD ["node", "src/index.js"]
    scripts/deploy.sh: |
      #!/bin/bash
      docker build -t api:latest .
      docker push registry.example.com/api:latest
    tests/api.test.js: |
      const request = require('supertest');
      describe('API', () => {
          it('should return health', async () => {
              const res = await request(app).get('/api/health');
              expect(res.status).toBe(200);
          });
      });
    terraform/main.tf: |
      provider "aws" {
        region = "us-east-1"
      }
      resource "aws_ecs_task_definition" "api" {
        container_definitions = jsonencode([{
          image = "api:latest"
          portMappings = [{ containerPort = 3000 }]
        }])
      }
  changed_files:
    src/index.js: |
      const express = require('express');
      const cors = require('cors');
      const helmet = require('helmet');

      const app = express();
      app.use(cors());
      app.use(helmet());
      app.use(express.json());

      app.get('/api/health', (req, res) => {
          res.json({ status: 'healthy', timestamp: Date.now() });
      });

      app.get('/api/users', (req, res) => {
          res.json({ users: [] });
      });

      const PORT = process.env.PORT || 3000;
      app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
    src/middleware/auth.js: |
      const jwt = require('jsonwebtoken');

      function authenticateToken(req, res, next) {
          const token = req.headers['authorization']?.split(' ')[1];
          if (!token) return res.sendStatus(401);
          jwt.verify(token, process.env.JWT_SECRET, (err, user) => {
              if (err) return res.sendStatus(403);
              req.user = user;
              next();
          });
      }

      module.exports = { authenticateToken };
    package.json: |
      {
        "name": "api-server",
        "version": "1.0.0",
        "main": "src/index.js",
        "scripts": {
          "start": "node src/index.js",
          "dev": "nodemon src/index.js"
        },
        "dependencies": {
          "express": "^4.18.2",
          "cors": "^2.8.5",
          "helmet": "^7.1.0",
          "jsonwebtoken": "^9.0.0"
        }
      }
    Dockerfile: |
      # Build stage - install dependencies
      FROM node:20-alpine AS deps
      WORKDIR /app
      COPY package*.json ./
      RUN npm ci --only=production && \
          npm cache clean --force

      # Build stage - for development dependencies
      FROM node:20-alpine AS builder
      WORKDIR /app
      COPY package*.json ./
      RUN npm ci
      COPY src/ ./src/

      # Production stage
      FROM node:20-alpine AS production
      WORKDIR /app

      # Security: run as non-root
      RUN addgroup -g 1001 -S nodejs && \
          adduser -S nodejs -u 1001

      # Copy production dependencies
      COPY --from=deps --chown=nodejs:nodejs /app/node_modules ./node_modules
      COPY --from=builder --chown=nodejs:nodejs /app/src ./src
      COPY --chown=nodejs:nodejs package*.json ./

      USER nodejs
      EXPOSE 3000

      ENV NODE_ENV=production
      ENV PORT=3000

      HEALTHCHECK --interval=30s --timeout=3s --start-period=5s \
          CMD wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1

      CMD ["node", "src/index.js"]
    scripts/deploy.sh: |
      #!/bin/bash
      docker build -t api:latest .
      docker push registry.example.com/api:latest
    tests/api.test.js: |
      const request = require('supertest');
      describe('API', () => {
          it('should return health', async () => {
              const res = await request(app).get('/api/health');
              expect(res.status).toBe(200);
          });
      });
    terraform/main.tf: |
      provider "aws" {
        region = "us-east-1"
      }
      resource "aws_ecs_task_definition" "api" {
        container_definitions = jsonencode([{
          image = "api:latest"
          portMappings = [{ containerPort = 3000 }]
        }])
      }
  commit_message: Update files
fragments:
- id: dockerfile
  selector:
    path: Dockerfile
    anchor: COPY package*.json ./
- id: dockerfile_1
  selector:
    path: Dockerfile
    anchor: COPY --from=deps --chown=nodejs:nodejs /app/node_modules ./node_modules
- id: dockerfile_2
  selector:
    path: Dockerfile
    anchor: CMD ["node", "src/index.js"]
- id: package
  selector:
    path: package.json
    anchor: '"start": "node src/index.js"'
- id: index
  selector:
    path: src/index.js
    anchor: /api/health
- id: index_1
  selector:
    path: src/index.js
    anchor: process.env.PORT || 3000
- id: no_deploy
  selector:
    path: scripts/deploy.sh
- id: no_api_test
  selector:
    path: tests/api.test.js
- id: no_main
  selector:
    path: terraform/main.tf
- id: no_global_0
  selector:
    anchor: docker push registry.example.com
- id: no_global_1
  selector:
    anchor: docker build -t api
- id: no_global_2
  selector:
    anchor: supertest
- id: no_global_3
  selector:
    anchor: expect(res.status).toBe(200)
- id: no_global_4
  selector:
    anchor: aws_ecs_task_definition
- id: no_global_5
  selector:
    anchor: containerPort
oracle:
  required:
  - dockerfile
  - dockerfile_1
  - dockerfile_2
  - package
  - index
  - index_1
  allowed: []
  forbidden:
  - no_deploy
  - no_api_test
  - no_main
  - no_global_0
  - no_global_1
  - no_global_2
  - no_global_3
  - no_global_4
  - no_global_5
